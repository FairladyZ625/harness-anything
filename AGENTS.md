# AGENTS.md

Engineering discipline for every agent and human working in this repository.
Written in Chinese; the rules bind all branches and worktrees.

## 工程原则（优先级最高；与任何下游标准冲突时以本节为准）

- 不要保留向后兼容性。对于已经过时的路径，直接删除，不要增加兼容层、回退机制或迁移方案。
- 选择能够完整满足当前需求的最简单实现。避免引入基于猜测的抽象、配置和间接层。
- 以可垂直验收的最小切片推进；**每个切片必须在同一变更中删除被它替代的生产路径**（CI 与文档也是生产路径）。除带到期日的外部迁移外，同一能力只允许一个生产实现——「先叠新层、旧层以后再删」不是分层，是欠债。
- 只有存在独立生命周期/owner、两个以上真实调用方、或能删除重复状态时才抽模块；仅把行数搬进新文件不算降低复杂度。
- 不重复实现常见功能：先用项目里已有的依赖，再考虑成熟且维护良好的库；没读过库的文档和类型定义之前，不要断言它缺某能力。
- 从长期角度做架构决策——但「长期性」只以当前已验证的不变量衡量：不得为假设中的未来调用方、迁移或协议预留抽象；临时测量/插桩必须自带删除条件。
- 在设计解决方案之前，研究成熟产品是如何解决同类问题的。优先采用已经被验证的模式和惯例。

**反水面循环**：修复的默认动作是**删除或收窄**，不是新增机制。每加一层兜底/重试/兼容/诊断前先回答：「这层在保护的那个假设，能不能直接修掉？」答不出来就不许加。A 问题引出 B 机制、B 机制再引出 C 兜底的链条，一律回到 A 重新问「最简解是什么」。交付物大小不是工作量的勋章——同等正确性下 diff 越小越好，删除行数与新增行数同等计功。

动手前对照的四条病理：

1. **加法局部安全，删法需要全局知识**——放任这条不对称，系统只长不缩。举证责任倒置：留下一层机制才需要理由，删掉不需要；说不出它保护哪个还成立的假设，就删。
2. **「fail-closed」「加诊断」「补测试」都是加法动词**。执行任何「修复」任务时，先列本次可删除的东西，再列要新增的东西；报告里两个清单并排交。
3. **修复类 PR 必须报告 `production source +/-` 行数**，并列出保留的旧生产路径及其删除日期（本仓 CI 的 production-delta 门机器校验此项）。
4. **执行姿态会偷换目标**。动手前强制切回评审姿态问一句：「如果我是评审这个 diff 的人，会不会问『为什么不直接删掉/直接改源头』？」——会，就照评审的答案做。

## 范围界限（约束你**提议**什么，永不约束你**查**什么）

真实存在的问题一律报告——再罕见，只要本项目真会产生它。但修复与提议要守界：

1. 这不是安全论文。验证欢迎，过度防御不要。除非项目明说，默认操作者是自己机器上的合作者；真有对手时项目会写明，那个范围优先。
2. 不加 hash/校验和/指纹，除非它替代了一个明显更贵的操作，**且**其结果会改变下一步做什么。
3. 不做防御性脚手架：不为这里不会发生的情况加 feature flag、迁移框架、兼容层、包装层。
4. 不痴迷角落：怪异编码、symlink 竞态、RTL 文本、毫秒竞态一律出界——除非该情形经本项目**受支持的用法**（文档化输入、公开接口、真实数据）可达。可达即够，不必复现；「原则上可构造」不算。
5. 需要判断的地方就判断。不许用评分表、检查清单或对已定结论的再验证循环替代判断。
6. 以上不覆盖用户、本项目惯例或更高优先级规则明确要求的安全/迁移/验证/评审——那些是被要求的工作本身，不是范围蔓延。

校准形状（例子不是清单；真 finding 不因长得像它们而被驳回）：

- H：对两张表逐行哈希，回答「比较单元格」就能回答的问题；写出没人读的 checksum 文件
- E：给没有用户、没有部署的应用加固账号体系
- R：整晚审计自己的补丁而功能没写；对一切都给 fail 判定的评审者；对已定结论反复复审
- O：一层 guard 的理由是上一层 guard，而不是需求

反例（长得像上面但**要报告**）：✓ 让你免于重读手头大文件的 digest；✓ 项目自己的文档示例真会产生的罕见输入。

跑任何检查之前先回答：它具体探测哪种失败？真发生了你会做什么不同的事？答不出来就不跑。
东西是对的就明说「是对的」。不制造 findings。

## 本仓机械门

上述原则不是倡议，由 CI 机械执行（`tools/gates/`，声明见 `tools/gates/contracts/`）：

- `line-budget`：每模块生产行数 ceiling 只降不升，超限或抬 ceiling 需带 decision receipt；
- `production-delta`：PR body 必须声明 `Production-Delta: +N/-M` 且与机器计算一致；保留被替代路径必须带到期日与 decision receipt；
- `anti-entropy-review`：重放 PR 须经独立反熵评审签发的 token（分阶段启用）。

门红时读它的报错——报错会告诉你正确的下一步；绕门是缺陷，不是技巧。
