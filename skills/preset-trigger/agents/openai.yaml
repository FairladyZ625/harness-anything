interface:
  display_name: "Preset Trigger"
  short_description: "Choose a Harness task preset before task creation"
  default_prompt: "Use $preset-trigger to choose an appropriate Harness Anything software/coding preset before creating a task package."
