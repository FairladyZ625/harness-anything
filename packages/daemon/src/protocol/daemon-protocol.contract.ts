import { existsSync, realpathSync } from "node:fs";
import path from "node:path";
import { isJsonObject, rejectSecretKeys, type JsonObject } from "./json-rpc-types.ts";
import type { AgentRuntimeEventsResult, AgentRuntimeOverviewResult, AgentRuntimeSessionResult } from "../agent-runtime-contract.ts";
import type { AgentRuntimeAttachResult } from "../agent-runtime-stream.ts";
import { cliInput, defineCliCommand, presetCommands, presetMethods } from "../../../preset/src/preset-command-contract.ts";
import type { DaemonRepoMode } from "../../../kernel/src/index.ts";
// Wire-validator mirrors of the kernel status vocabularies (register:
// packages/kernel/src/domain/status-vocabulary.ts, blueprint 铁律四). This module sits
// on the CLI's eager startup path, so it must not import the kernel barrel — the p50
// overhead gate refuses eager module growth — and deep kernel imports are restricted.
// The mirrors stay plain data; the status-vocabulary ratchet gate locks them against
// the kernel vocabularies so they cannot drift.
const taskStatusWords = ["planned", "active", "blocked", "in_review", "done", "cancelled"] as const;
const executionV1StateWords = ["active", "submitted", "changes_requested", "accepted"] as const;
const executionStateWords = ["active", "submitted", "changes_requested", "accepted", "abandoned"] as const;
const leasePhaseWords = ["reserving", "held", "orphaned", "released"] as const;
const relationStateWords = ["active", "edge_retired", "deleted"] as const;
const packageDispositionWords = ["active", "archived", "tombstoned"] as const;
const reviewVerdictWords = ["approved", "changes_requested", "dismissed"] as const;
const decisionStateWords = ["proposed", "in_effect", "rejected", "deferred", "superseded", "outcome_retired"] as const;
const receiptOutcomeWords = ["applied", "pending", "indeterminate", "op_rejected"] as const;
export const daemonRepoModeWords = Object.freeze(["local", "remote-center", "remote-edge"] as const satisfies readonly DaemonRepoMode[]);
const daemonRepoModeWordsAreExact: [DaemonRepoMode] extends [(typeof daemonRepoModeWords)[number]] ? true : never = true; void daemonRepoModeWordsAreExact;
declare const safePathBrand: unique symbol, canonicalRootBrand: unique symbol, endpointIdentityBrand: unique symbol, workspaceIdBrand: unique symbol;
export type SafePath = string & { readonly [safePathBrand]: true }; export type CanonicalRoot = SafePath & { readonly [canonicalRootBrand]: true };
export type EndpointIdentity = string & { readonly [endpointIdentityBrand]: true }; export type WorkspaceId = string & { readonly [workspaceIdBrand]: true };
export function safePath(value: string): SafePath { return path.resolve(value) as SafePath; }
export function canonicalRoot(value: string, allowMissing = false): CanonicalRoot { const resolved = path.resolve(value); if (existsSync(resolved)) return realpathSync.native(resolved) as CanonicalRoot;
  const parent = path.dirname(resolved); if (!allowMissing || !existsSync(parent)) throw new DaemonProtocolContractError("invalid_root", `Canonical root does not exist: ${resolved}`);
  return path.join(realpathSync.native(parent), path.basename(resolved)) as CanonicalRoot; }
export function endpointIdentity(value: string): EndpointIdentity { if (!value.trim()) throw new DaemonProtocolContractError("invalid_endpoint", "Endpoint identity is required."); return value as EndpointIdentity; }
export function workspaceId(value: string): WorkspaceId { if (!/^[a-z][a-z0-9-]{0,62}$/u.test(value)) throw new DaemonProtocolContractError("invalid_workspace", "Workspace id is invalid."); return value as WorkspaceId; }
const taskSurfaceProtocolCommands = Object.freeze([
  defineCliCommand({ id: "task-release", phase: "W3", path: ["task", "release", "<task-id>"], summary: "Release the authenticated holder lease and preserve the Execution audit trail.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--reason", "single", false, { code: "invalid_field", nextAction: "Use one non-empty --reason, or omit it for the standard release audit." })] }),
  defineCliCommand({ id: "task-transition", phase: "W3", path: ["task", "transition", "<task-id>", "<planned|active|blocked|in_review|done|cancelled>"], summary: "Move lifecycle status; done and in_review remain reserved for complete and submit.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--force", "boolean", false, { code: "invalid_field", nextAction: "Use --force only for audited cancellation." }), cliInput("--reason", "single", false, { code: "missing_field", nextAction: "Forced cancellation requires --reason <auditable-reason>; direct done still requires task complete." })] }),
  defineCliCommand({ id: "task-amend", phase: "W3", path: ["task", "amend", "<task-id>"], summary: "Amend declared task prose or metadata without changing lifecycle authority.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--set", "repeated", true, { code: "invalid_field", nextAction: "Use --set <title|parentTaskId|workKind|riskTier|urgency|moduleKey|taskClass>:<value>." }, { regex: "^[A-Za-z][A-Za-z0-9]*:.+$" })] }),
  defineCliCommand({ id: "task-contract-migrate", phase: "W3", path: ["task", "contract", "migrate"], summary: "Plan or apply deterministic Task contract backfills; ambiguous Tasks remain manual.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--dry-run", "boolean", false, { code: "invalid_field", nextAction: "Choose exactly one of --dry-run or --apply." }), cliInput("--apply", "boolean", false, { code: "invalid_field", nextAction: "Choose exactly one of --dry-run or --apply." }), cliInput("--task", "single", false, { code: "invalid_field", nextAction: "Use one existing task id with --task." })] }),
  defineCliCommand({ id: "task-archive", phase: "W3", path: ["task", "archive", "[<task-id>]"], summary: "Archive selected Task packages while retaining their evidence and lifecycle history.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--ids", "single", false, { code: "invalid_field", nextAction: "Use comma-separated task ids, or select one positional id." }), cliInput("--filter", "single", false, { code: "invalid_field", nextAction: "Use --filter state:<status>." }, { regex: "^state:(?:planned|active|blocked|in_review|done|cancelled)$" }), cliInput("--before", "single", false, { code: "invalid_field", nextAction: "Use an ISO-compatible date with --before." }), cliInput("--reason", "single", true, { code: "missing_field", nextAction: "Add --reason <auditable-reason>." }), cliInput("--archived-by", "single", false, { code: "invalid_field", nextAction: "Use one non-empty actor id for --archived-by." }), cliInput("--archive-field", "single", false, { code: "invalid_field", nextAction: "Use one declared archive field." })] }),
  defineCliCommand({ id: "task-supersede", phase: "W3", path: ["task", "supersede", "<old-task-id>"], summary: "Archive old work and preserve an explicit replacement lineage.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--title", "single", false, { code: "invalid_field", nextAction: "Choose --title for new work or --by for an existing replacement, not both." }), cliInput("--slug", "single", false, { code: "invalid_field", nextAction: "Use a lowercase kebab-case slug with --title." }, { regex: "^[a-z0-9](?:[a-z0-9-]{0,70}[a-z0-9])?$" }), cliInput("--by", "single", false, { code: "invalid_field", nextAction: "Choose --title or --by; existing replacements also require matching --confirm." }), cliInput("--confirm", "single", false, { code: "invalid_field", nextAction: "With --by, pass --confirm <old-task-id>." }), cliInput("--reason", "single", false, { code: "invalid_field", nextAction: "Use one non-empty auditable reason." }), cliInput("--deleted-by", "single", false, { code: "invalid_field", nextAction: "Use one non-empty actor id for --deleted-by." }), cliInput("--allow-open-findings", "boolean", false, { code: "invalid_field", nextAction: "Use --allow-open-findings once after reviewing unresolved findings." })] }),
  defineCliCommand({ id: "task-delete", phase: "W3", path: ["task", "delete"], summary: "Soft-delete through production authority; hard delete remains rejected with a repair path.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--soft", "single", false, { code: "invalid_field", nextAction: "Choose exactly one of --soft <task-id> or --hard <task-id>." }), cliInput("--hard", "single", false, { code: "invalid_field", nextAction: "Production rejects hard delete; select the exact task only to receive the governed repair." }), cliInput("--confirm", "single", false, { code: "invalid_field", nextAction: "Local hard-delete compatibility requires matching --confirm, but production will still reject it." }), cliInput("--reason", "single", false, { code: "missing_field", nextAction: "Soft delete requires --reason <auditable-reason>." }), cliInput("--deleted-by", "single", false, { code: "invalid_field", nextAction: "Use one non-empty actor id for --deleted-by." })] }),
  defineCliCommand({ id: "task-reopen", phase: "W3", path: ["task", "reopen", "<task-id>"], summary: "Reopen a nonterminal archived or tombstoned Task package.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--reason", "single", true, { code: "missing_field", nextAction: "Add --reason <auditable-reason>; terminal Tasks require supersede." })] }),
  defineCliCommand({ id: "task-review", phase: "W3", path: ["task", "review", "<task-id>"], summary: "Lint the legacy review contract without approving completion.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--reviewer", "single", false, { code: "invalid_field", nextAction: "Use one non-empty reviewer id." })] }),
  defineCliCommand({ id: "task-list", phase: "W3", path: ["task", "list"], summary: "List Task projection rows with canonical lifecycle and metadata filters.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--status", "single", false, { code: "invalid_field", nextAction: "Use planned, active, blocked, in_review, done, or cancelled." }, { enum: ["planned", "active", "blocked", "in_review", "done", "cancelled"] }), cliInput("--module", "single", false, { code: "invalid_field", nextAction: "Use one module key." }), cliInput("--search", "single", false, { code: "invalid_field", nextAction: "Use a non-empty title or task-id search." }), cliInput("--kind", "single", false, { code: "invalid_field", nextAction: "Use feat, fix, refactor, docs, test, or chore." }, { enum: ["feat", "fix", "refactor", "docs", "test", "chore"] }), cliInput("--risk-tier", "single", false, { code: "invalid_field", nextAction: "Use low, medium, or high." }, { enum: ["low", "medium", "high"] }), cliInput("--urgency", "single", false, { code: "invalid_field", nextAction: "Use low, medium, or high." }, { enum: ["low", "medium", "high"] }), cliInput("--parent", "single", false, { code: "invalid_field", nextAction: "Use one parent task id." })] }),
  defineCliCommand({ id: "relation-list", phase: "W3", path: ["relation", "list"], summary: "Query Task, Decision, and Fact relation edges from the converged projection.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--entity", "single", false, { code: "invalid_field", nextAction: "Use task/<id>, decision/<id>, or fact/<task>/<id>." }), cliInput("--source", "single", false, { code: "invalid_field", nextAction: "Use a canonical entity ref for --source." }), cliInput("--target", "single", false, { code: "invalid_field", nextAction: "Use a canonical entity ref for --target." }), cliInput("--type", "single", false, { code: "invalid_field", nextAction: "Use a declared relation type." }), cliInput("--state", "single", false, { code: "invalid_field", nextAction: "Use active, edge_retired, or deleted." }, { enum: ["active", "edge_retired", "deleted"] })] }),
  defineCliCommand({ id: "task-relate", phase: "W3", path: ["task", "relate", "<source-task-id>", "depends-on", "<target-task-id>"], summary: "Declare a cycle-checked depends-on edge owned by the source Task.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--rationale", "single", true, { code: "missing_field", nextAction: "Add --rationale <why-this-dependency-is-required>." }), cliInput("--dry-run", "boolean", false, { code: "invalid_field", nextAction: "Use --dry-run once to preview the edge." })] })
] as const);
const daemonOwnedProtocolCommands = Object.freeze([...taskSurfaceProtocolCommands,
  defineCliCommand({ id: "migrate-import", phase: "Migration-A", path: ["migrate", "import"], summary: "Import a legacy Harness repository; resolve reported destination conflicts with repeated --resolve path=destination|source.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--source", "single", true, { code: "missing_field", nextAction: "Add --source <legacy-repository-path>." }, { field: "sourceRoot" }), cliInput("--resolve", "repeated", false, { code: "invalid_field", nextAction: "Use --resolve <repo-relative-path>=destination|source once per reported conflict." }, { regex: "^.+=(?:destination|source)$" }), cliInput("--dry-run", "boolean", false, { code: "invalid_field", nextAction: "Use --dry-run once." }, { field: "dryRun" })] }),
  defineCliCommand({ id: "ledger-migrate", phase: "Migration-A", path: ["migrate", "ledger"], summary: "Migrate the canonical ledger from flat/v1 to sharded-sha256-2/v1.", method: "repo.task.run", commandClass: "repo-write", inputs: [] }),
  defineCliCommand({ id: "repo-bootstrap", phase: "W3", path: ["init"], summary: "Initialize and register a workspace through the daemon (started on demand); --configure-only reapplies machine configuration to an already-initialized workspace without writing documents.", method: "daemon.repo.bootstrap", commandClass: "admin", inputs: [cliInput("--repo-id", "single", true, { code: "missing_field", nextAction: "Init requires repo-id, person-id, and display-name." }), cliInput("--person-id", "single", true, { code: "missing_field", nextAction: "Init requires repo-id, person-id, and display-name." }), cliInput("--display-name", "single", true, { code: "missing_field", nextAction: "Init requires repo-id, person-id, and display-name." }), cliInput("--name", "single", false, { code: "invalid_field", nextAction: "--name must be one non-empty line." }, { regex: "^[^\\r\\n]+$" }), cliInput("--configure-only", "boolean", false, { code: "invalid_field", nextAction: "Use --configure-only once." })] }), defineCliCommand({ id: "task-start", phase: "W3", path: ["task", "start", "<task-id>"], summary: "Acquire the task execution lease.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "missing_field", nextAction: "Add --execution-id <execution-id>." })] }), defineCliCommand({ id: "task-progress-append", phase: "W3", path: ["task", "progress", "append", "<task-id>"], summary: "Append typed progress through the active task lease.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--text", "single", true, { code: "missing_field", nextAction: "Add --text <progress-text>." }), cliInput("--evidence", "repeated", false, { code: "invalid_field", nextAction: "Use --evidence <type>:<path>:<summary> with a canonical relative path." }, { regex: "^[a-z][a-z0-9_-]{0,31}:[^:]+:.+$" })] }), defineCliCommand({ id: "task-artifact-add", phase: "W3", path: ["task", "artifact", "add", "<task-id>"], summary: "Publish an untracked UTF-8 artifact through canonical doc sync.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--source", "single", true, { code: "missing_field", nextAction: "Add --source <untracked-file>." }), cliInput("--destination", "single", true, { code: "missing_field", nextAction: "Add --destination <artifact-path>." })] }), defineCliCommand({ id: "task-submit", phase: "W3", path: ["task", "submit", "<task-id>"], summary: "Atomically finalize an Execution and enter review.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "invalid_field", nextAction: "Submit requires --execution-id and --from-file." }), cliInput("--from-file", "single", true, { code: "invalid_field", nextAction: "Submit requires a complete submission JSON packet." })] }),
  defineCliCommand({ id: "task-review-execution", phase: "W3", path: ["task", "review-execution", "<task-id>"], summary: "Record the canonical Execution Review.", method: "repo.task.run", commandClass: "arbiter", inputs: [cliInput("--execution-id", "single", true, { code: "invalid_field", nextAction: "Review requires execution-id, review-id, and from-file." }), cliInput("--review-id", "single", true, { code: "invalid_field", nextAction: "Review requires a review id." }), cliInput("--from-file", "single", true, { code: "invalid_field", nextAction: "Review requires a complete review JSON packet." })] }), defineCliCommand({ id: "task-review-consent", phase: "W3", path: ["task", "review-consent", "<task-id>"], summary: "Record content-pinned owner consent; without --from-file the digests bind the recorded Review.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "invalid_field", nextAction: "Consent requires execution-id, review-id, and consent-id." }), cliInput("--review-id", "single", true, { code: "invalid_field", nextAction: "Consent requires a review id." }), cliInput("--consent-id", "single", true, { code: "invalid_field", nextAction: "Consent requires a consent id." }), cliInput("--from-file", "single", false, { code: "invalid_field", nextAction: "Omit --from-file to bind the recorded Review, or supply reviewDigest and contentDigest JSON to pin them yourself." })] }), defineCliCommand({ id: "task-code-doc-reconcile", phase: "W3", path: ["task", "code-doc", "reconcile", "<task-id>"], summary: "Publish a typed code-doc witness.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "invalid_field", nextAction: "Reconcile requires execution-id, commit-sha, iteration, and path." }), cliInput("--commit-sha", "single", true, { code: "invalid_field", nextAction: "Reconcile requires a full commit sha." }, { regex: "^[0-9a-f]{40}$" }), cliInput("--iteration", "single", true, { code: "invalid_field", nextAction: "Iteration must be 0 or 1." }, { enum: ["0", "1"] }), cliInput("--path", "repeated", true, { code: "invalid_field", nextAction: "Reconcile requires at least one canonical path." })] }), defineCliCommand({ id: "task-complete", phase: "W3", path: ["task", "complete", "<task-id>"], summary: "Complete a reviewed and consented task after canonical gate checks.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "missing_field", nextAction: "Add --execution-id <execution-id>." }), cliInput("--ci", "single", false, { code: "invalid_field", nextAction: "Use --ci passed only for a successful canonical checker result." }, { enum: ["passed"] }), cliInput("--commit-sha", "single", false, { code: "invalid_field", nextAction: "Provide commit-sha, iteration, and path together for reconcile." }, { regex: "^[0-9a-f]{40}$" }), cliInput("--iteration", "single", false, { code: "invalid_field", nextAction: "Provide commit-sha, iteration, and path together for reconcile." }, { enum: ["0", "1"] }), cliInput("--path", "repeated", false, { code: "invalid_field", nextAction: "Provide commit-sha, iteration, and path together for reconcile." })] }), defineCliCommand({ id: "task-show", phase: "W3", path: ["task", "show", "<task-id>"], summary: "Read the task projection.", method: "repo.task.run", commandClass: "repo-read", inputs: [] }), defineCliCommand({ id: "receipt-show", phase: "W3", path: ["receipt", "show", "<op-id>"], summary: "Read a write receipt.", method: "repo.task.run", commandClass: "repo-read", inputs: [] }),
  defineCliCommand({ id: "doc-status", phase: "DocSync-B", path: ["doc", "status"], summary: "Scan the authored root for document candidates.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--path", "repeated", false, { code: "invalid_field", nextAction: "--path requires an authored-relative path." })] }), defineCliCommand({ id: "doc-sync-dry-run", actionKind: "doc-dry-run", phase: "DocSync-B", path: ["doc", "sync", "--dry-run"], summary: "Preview the scanner selection without writing.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--path", "repeated", false, { code: "invalid_field", nextAction: "--path requires an authored-relative path." })] }), defineCliCommand({ id: "doc-sync-submit", actionKind: "doc-submit", phase: "DocSync-B", path: ["doc", "sync", "--submit"], summary: "Submit eligible scanner candidates through the resident daemon.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", false, { code: "invalid_field", nextAction: "--execution-id requires a non-empty value." }), cliInput("--path", "repeated", false, { code: "invalid_field", nextAction: "--path requires an authored-relative path." })] }), defineCliCommand({ id: "doc-materialize", phase: "DocSync-B", path: ["doc", "materialize"], summary: "Restore the current canonical document cut to the worktree.", method: "repo.task.run", commandClass: "repo-write", inputs: [] }), defineCliCommand({ id: "doc-show", phase: "DocSync-B", path: ["doc", "show"], summary: "Show a canonical projected document.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--path", "single", true, { code: "missing_field", nextAction: "Add --path <authored-relative-path>." })] }),
  defineCliCommand({ id: "fact-record", phase: "DecisionFact-A", path: ["fact", "record"], summary: "Record an immutable Fact event.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--task", "single", true, { code: "missing_field", nextAction: "Fact record requires --task, --statement, and --source." }), cliInput("--statement", "single", true, { code: "missing_field", nextAction: "Fact record requires --task, --statement, and --source." }), cliInput("--source", "single", true, { code: "missing_field", nextAction: "Fact record requires --task, --statement, and --source." }), cliInput("--observed-at", "single", false, { code: "invalid_field", nextAction: "Use an ISO-8601 UTC timestamp for --observed-at." }, { regex: "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(?:\\.[0-9]+)?Z$" }), cliInput("--confidence", "single", false, { code: "invalid_field", nextAction: "Confidence must be low, medium, or high." }, { enum: ["low", "medium", "high"] }), cliInput("--memory-class", "single", false, { code: "invalid_field", nextAction: "Memory class must be semantic, episodic, or procedural." }, { enum: ["semantic", "episodic", "procedural"] }), cliInput("--memory-tag", "repeated", false, { code: "invalid_field", nextAction: "--memory-tag requires a non-empty value." }), cliInput("--supersedes", "single", false, { code: "invalid_field", nextAction: "Pair --supersedes with a rationale of at most 199 characters." }), cliInput("--rationale", "single", false, { code: "invalid_field", nextAction: "Pair --supersedes with a rationale of at most 199 characters." }, { regex: "^[\\s\\S]{1,199}$" })] }),
  defineCliCommand({ id: "fact-search", phase: "DecisionFact-A", path: ["fact", "search", "[query]"], summary: "Search the Fact FTS projection.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--task", "single", false, { code: "invalid_field", nextAction: "--task requires a task id." }), cliInput("--confidence", "single", false, { code: "invalid_field", nextAction: "Fact search confidence must be low, medium, or high." }, { enum: ["low", "medium", "high"] }), cliInput("--memory-class", "single", false, { code: "invalid_field", nextAction: "Fact search memory class must be semantic, episodic, or procedural." }, { enum: ["semantic", "episodic", "procedural"] })] }), defineCliCommand({ id: "fact-show", phase: "DecisionFact-A", path: ["fact", "show"], summary: "Show one projected Fact.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--task", "single", true, { code: "missing_field", nextAction: "Fact show requires --task and --id." }), cliInput("--id", "single", true, { code: "invalid_field", nextAction: "Use F- followed by eight Crockford characters." }, { regex: "^F-[0-9A-HJKMNP-TV-Z]{8}$" })] }),
  defineCliCommand({ id: "decision-validate", actionKind: "decision-validate", phase: "DecisionFact-B", path: ["decision", "validate", "[id]"], summary: "Read-only validation of one or all Decision packages, pins, and amend history.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--all", "boolean", false, { code: "invalid_field", nextAction: "Use exactly one of decision validate <id> or decision validate --all." })] }), defineCliCommand({ id: "decision-verify", actionKind: "decision-validate", phase: "DecisionFact-B", path: ["decision", "verify", "[id]"], summary: "Read-only content-pin verification alias; reports every warning without rewriting.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--all", "boolean", false, { code: "invalid_field", nextAction: "Use exactly one of decision verify <id> or decision verify --all." })] }), defineCliCommand({ id: "decision-repin", phase: "DecisionFact-B", path: ["decision", "repin", "[id]"], summary: "Append current v1 content pins to one Decision or every Decision in a migration batch.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--all", "boolean", false, { code: "invalid_field", nextAction: "Use exactly one Decision id or --all." }), cliInput("--migration-evidence", "single", true, { code: "invalid_field", nextAction: "Use --migration-evidence task/<task-id>/<audit-marker>." }, { regex: "^task/[^/]+/[^/]+$" })] }),
  defineCliCommand({ id: "decision-propose", phase: "DecisionFact-B", path: ["decision", "propose"], summary: "Propose an immutable Decision from one structured packet.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--from-file", "single", false, { code: "invalid_field", nextAction: "Use exactly one of --from-file <packet.json> or --json-input <json>." }), cliInput("--json-input", "single", false, { code: "invalid_field", nextAction: "Use exactly one of --from-file <packet.json> or --json-input <json>." }), cliInput("--body", "single", false, { code: "invalid_field", nextAction: "Use only one of --body or --body-file." }), cliInput("--body-file", "single", false, { code: "invalid_field", nextAction: "Use only one of --body or --body-file." })] }), defineCliCommand({ id: "decision-transition", phase: "DecisionFact-B", path: ["decision", "transition", "<in_effect|rejected|deferred|superseded|outcome_retired>", "<id>"], summary: "Canonical lifecycle transition; terminal states cannot transition again.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--decided-at", "single", false, { code: "invalid_field", nextAction: "Use an ISO-8601 timestamp." }), cliInput("--judgment-only", "single", false, { code: "invalid_field", nextAction: "Judgment-only acceptance requires an explicit rationale of 1..199 characters." }, { regex: "^[\\s\\S]{1,199}$" }), cliInput("--standing-policy", "boolean", false, { code: "invalid_field", nextAction: "--standing-policy is only valid when transitioning to in_effect." }), cliInput("--fulfillment", "repeated", false, { code: "invalid_field", nextAction: "Use --fulfillment <claim-id>:<evidenced|delivered|standing_policy>." }, { regex: "^C[A-Za-z0-9_-]+:(?:evidenced|delivered|standing_policy)$" }), cliInput("--dry-run", "boolean", false, { code: "invalid_field", nextAction: "Use --dry-run once to preview without writing." })] }), defineCliCommand({ id: "decision-accept", phase: "DecisionFact-B", path: ["decision", "accept", "<id>"], summary: "Deprecated alias for decision transition in_effect.", method: "repo.task.run", commandClass: "arbiter", inputs: [cliInput("--rationale", "single", true, { code: "invalid_field", nextAction: "A rationale of 1..199 characters is required." }, { regex: "^[\\s\\S]{1,199}$" }), cliInput("--judgment-only", "single", false, { code: "invalid_field", nextAction: "Without claim evidence, --judgment-only requires an explicit rationale of 1..199 characters." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-reject", phase: "DecisionFact-B", path: ["decision", "reject", "<id>"], summary: "Deprecated alias for decision transition rejected.", method: "repo.task.run", commandClass: "arbiter", inputs: [cliInput("--rationale", "single", true, { code: "invalid_field", nextAction: "A rationale of 1..199 characters is required." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-defer", phase: "DecisionFact-B", path: ["decision", "defer", "<id>"], summary: "Deprecated alias for decision transition deferred.", method: "repo.task.run", commandClass: "arbiter", inputs: [cliInput("--rationale", "single", true, { code: "invalid_field", nextAction: "A rationale of 1..199 characters is required." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-supersede", phase: "DecisionFact-B", path: ["decision", "supersede", "<id>"], summary: "Deprecated alias for decision transition superseded.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--reason", "single", true, { code: "invalid_field", nextAction: "A reason of 1..199 characters is required." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-retire", phase: "DecisionFact-B", path: ["decision", "retire", "<id>"], summary: "Deprecated alias for decision transition outcome_retired.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--reason", "single", true, { code: "invalid_field", nextAction: "A reason of 1..199 characters is required." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-amend", phase: "DecisionFact-B", path: ["decision", "amend", "<id>"], summary: "Amend declared machine fields or replace Markdown prose without changing lifecycle state.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--title", "single", false, { code: "invalid_field", nextAction: "--title requires a non-empty title." }), cliInput("--standing-policy", "boolean", false, { code: "invalid_field", nextAction: "Use --standing-policy once." }), cliInput("--fulfillment", "repeated", false, { code: "invalid_field", nextAction: "Use --fulfillment <claim-id>:<evidenced|delivered|standing_policy>." }, { regex: "^C[A-Za-z0-9_-]+:(?:evidenced|delivered|standing_policy)$" }), cliInput("--load-bearing", "single", false, { code: "invalid_field", nextAction: "Use only one of --load-bearing or --non-load-bearing." }), cliInput("--non-load-bearing", "single", false, { code: "invalid_field", nextAction: "Use only one of --load-bearing or --non-load-bearing." }), cliInput("--set", "repeated", false, { code: "invalid_field", nextAction: "Use --set title:<value>." }), cliInput("--append", "repeated", false, { code: "invalid_field", nextAction: "Use --append <chosen|rejected|claims>:<json-object>." }), cliInput("--body", "single", false, { code: "invalid_field", nextAction: "Use only one of --body or --body-file." }), cliInput("--body-file", "single", false, { code: "invalid_field", nextAction: "Use only one of --body or --body-file." }), cliInput("--dry-run", "boolean", false, { code: "invalid_field", nextAction: "Use --dry-run once to preview without writing." })] }),
  defineCliCommand({ id: "decision-claim-add", phase: "DecisionFact-B", path: ["decision", "claim", "add", "<id>"], summary: "Append a Decision claim.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--id", "single", true, { code: "invalid_field", nextAction: "Claim requires a C-prefixed --id." }, { regex: "^C[A-Za-z0-9_-]+$" }), cliInput("--text", "single", true, { code: "invalid_field", nextAction: "Claim requires --text." }), cliInput("--non-load-bearing", "boolean", false, { code: "invalid_field", nextAction: "Use --non-load-bearing once." })] }), defineCliCommand({ id: "decision-claim-fulfill", phase: "DecisionFact-B", path: ["decision", "claim", "fulfill", "<id>"], summary: "Declare a claim fulfillment mode.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--id", "single", true, { code: "invalid_field", nextAction: "Claim requires a C-prefixed --id." }, { regex: "^C[A-Za-z0-9_-]+$" }), cliInput("--mode", "single", true, { code: "invalid_field", nextAction: "Fulfillment requires evidenced, delivered, or standing_policy mode." }, { enum: ["evidenced", "delivered", "standing_policy"] })] }), defineCliCommand({ id: "decision-relate", phase: "DecisionFact-B", path: ["decision", "relate", "<id>"], summary: "Append a Decision-owned relation.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--anchor", "single", true, { code: "invalid_field", nextAction: "Relate requires an existing Decision anchor." }), cliInput("--type", "single", true, { code: "invalid_field", nextAction: "Relate requires type, target, and a rationale of 1..199 characters." }), cliInput("--target", "single", true, { code: "invalid_field", nextAction: "Relate requires type, target, and a rationale of 1..199 characters." }), cliInput("--rationale", "single", true, { code: "invalid_field", nextAction: "Relate requires type, target, and a rationale of 1..199 characters." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-relation-retire", phase: "DecisionFact-B", path: ["decision", "relation", "retire", "<id>"], summary: "Retire a Decision-owned relation while preserving the old edge.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--relation", "single", true, { code: "invalid_field", nextAction: "Relation retire requires a deterministic relation id and reason." }, { regex: "^rel_[0-9a-f]{16}$" }), cliInput("--reason", "single", true, { code: "invalid_field", nextAction: "Relation retire requires a deterministic relation id and reason." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-relation-replace", phase: "DecisionFact-B", path: ["decision", "relation", "replace", "<id>"], summary: "Atomically retire one hosted relation and append its deterministic replacement.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--relation", "single", true, { code: "invalid_field", nextAction: "Relation replace requires the old deterministic relation id." }, { regex: "^rel_[0-9a-f]{16}$" }), cliInput("--anchor", "single", true, { code: "invalid_field", nextAction: "Relation replace requires an existing Decision anchor." }), cliInput("--type", "single", true, { code: "invalid_field", nextAction: "Relation replace requires type, target, and rationale." }), cliInput("--target", "single", true, { code: "invalid_field", nextAction: "Relation replace requires type, target, and rationale." }), cliInput("--rationale", "single", true, { code: "invalid_field", nextAction: "Relation replace requires a rationale of 1..199 characters." }, { regex: "^[\\s\\S]{1,199}$" }), cliInput("--body", "single", false, { code: "invalid_field", nextAction: "--body replaces only authored Markdown prose." }), cliInput("--dry-run", "boolean", false, { code: "invalid_field", nextAction: "Use --dry-run once to preview without writing." })] }),
  defineCliCommand({ id: "decision-reckon", phase: "DecisionFact-B", path: ["decision", "reckon", "<id>"], summary: "Record coverage at the exact projected basis revision.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--task", "single", true, { code: "missing_field", nextAction: "Reckon requires --task." })] }), defineCliCommand({ id: "decision-list", phase: "DecisionFact-B", path: ["decision", "list"], summary: "List the canonical Decision projection without authored prose.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--search", "single", false, { code: "invalid_field", nextAction: "--search requires a non-empty value." }), cliInput("--legacy-id", "single", false, { code: "invalid_field", nextAction: "Use an E-number such as E12." }, { regex: "^E[1-9][0-9]*$" }), cliInput("--legacy-range", "single", false, { code: "invalid_field", nextAction: "Use an inclusive range such as E1-E12." }, { regex: "^E[1-9][0-9]*-E[1-9][0-9]*$" }), cliInput("--state", "single", false, { code: "invalid_field", nextAction: "Decision state filter is invalid." }, { enum: ["proposed", "in_effect", "rejected", "deferred", "superseded", "outcome_retired"] }), cliInput("--module", "single", false, { code: "invalid_field", nextAction: "--module requires a non-empty value." }), cliInput("--product-line", "single", false, { code: "invalid_field", nextAction: "--product-line requires a non-empty value." })] }), defineCliCommand({ id: "decision-show", phase: "DecisionFact-B", path: ["decision", "show", "<id>"], summary: "Show one canonical Decision by id or E-number.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--include-body", "boolean", false, { code: "invalid_field", nextAction: "Use --include-body once." })] }),
  defineCliCommand({ id: "distill-candidate", phase: "DecisionFact-B", path: ["distill", "candidate"], summary: "Create a generated, non-canonical candidate artifact from a task evidence file; no Fact is written.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--task", "single", true, { code: "missing_field", nextAction: "Distill candidate requires --task and --input." }), cliInput("--input", "single", true, { code: "invalid_field", nextAction: "--input must be a readable workspace-relative file." })] }), defineCliCommand({ id: "distill-promote", phase: "DecisionFact-B", path: ["distill", "promote"], summary: "Promote one validated candidate claim through the canonical immutable Fact write path.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--task", "single", true, { code: "missing_field", nextAction: "Distill promote requires --task, --candidate, and --claim." }), cliInput("--candidate", "single", true, { code: "invalid_field", nextAction: "--candidate must be a readable distill-candidate/v1 artifact." }), cliInput("--claim", "single", true, { code: "invalid_field", nextAction: "--claim requires explicit stable Fact text." }), cliInput("--id", "single", false, { code: "invalid_field", nextAction: "Use F- followed by eight Crockford characters." }, { regex: "^F-[0-9A-HJKMNP-TV-Z]{8}$" }), cliInput("--confidence", "single", false, { code: "invalid_field", nextAction: "Confidence must be low, medium, or high." }, { enum: ["low", "medium", "high"] }), cliInput("--memory-class", "single", false, { code: "invalid_field", nextAction: "Memory class must be semantic, episodic, or procedural." }, { enum: ["semantic", "episodic", "procedural"] }), cliInput("--memory-tag", "repeated", false, { code: "invalid_field", nextAction: "--memory-tag requires a declared Fact memory tag." }), cliInput("--observed-at", "single", false, { code: "invalid_field", nextAction: "Use an ISO-8601 timestamp for --observed-at." })] }),
  defineCliCommand({ id: "daemon-repo-register", phase: "W3", path: ["daemon", "repo", "register"], summary: "Register an initialized workspace with an explicit service mode.", method: "daemon.repo.register", commandClass: "admin", inputs: [cliInput("--repo-id", "single", true, { code: "missing_field", nextAction: "Register requires --repo-id and --root." }), cliInput("--root", "single", true, { code: "missing_field", nextAction: "Register requires --repo-id and --root." }), cliInput("--mode", "single", false, { code: "invalid_field", nextAction: "Use local, remote-center, or remote-edge." }, { enum: daemonRepoModeWords })] }), defineCliCommand({ id: "daemon-repo-unregister", phase: "W3", path: ["daemon", "repo", "unregister"], summary: "Disable a registered workspace.", method: "daemon.repo.unregister", commandClass: "admin", inputs: [cliInput("--repo-id", "single", true, { code: "missing_field", nextAction: "Unregister requires --repo-id." })] }), defineCliCommand({ id: "daemon-start", phase: "W3", path: ["daemon", "start", "--service"], summary: "Explicitly start the resident daemon.", method: "protocol.hello", commandClass: "admin", inputs: [] }), defineCliCommand({ id: "daemon-status", phase: "W3", path: ["daemon", "status"], summary: "Show daemon and RepoCell status.", method: "daemon.status", commandClass: "repo-read", inputs: [] }),
  defineCliCommand({ id: "runtime-instance-create", phase: "Runtime-Instances-S1", path: ["runtime", "instance", "create"], summary: "Create one machine-local isolated runtime instance bound to a witnessed installation.", method: "daemon.runtimeInstance.create", commandClass: "admin", inputs: [cliInput("--id", "single", true, { code: "missing_field", nextAction: "Runtime instance create requires --id." }, { regex: "^[a-z0-9](?:[a-z0-9-]{0,62}[a-z0-9])?$" }), cliInput("--name", "single", true, { code: "missing_field", nextAction: "Runtime instance create requires --name." }), cliInput("--kind", "single", true, { code: "invalid_field", nextAction: "Runtime kind must be claude or codex." }, { enum: ["claude", "codex"] }), cliInput("--installation", "single", true, { code: "missing_field", nextAction: "Choose an installation returned by runtime instance list." }), cliInput("--provider", "single", true, { code: "missing_field", nextAction: "Runtime instance create requires --provider." }), cliInput("--model", "single", true, { code: "missing_field", nextAction: "Runtime instance create requires --model." }), cliInput("--effort", "single", false, { code: "invalid_field", nextAction: "Use one provider-supported reasoning effort." }), cliInput("--base-url", "single", false, { code: "invalid_field", nextAction: "Use an HTTPS or loopback HTTP API base URL." }), cliInput("--auth", "single", true, { code: "invalid_field", nextAction: "Auth mode must be subscription or api-key." }, { enum: ["subscription", "api-key"] }), cliInput("--credential-ref", "single", false, { code: "invalid_field", nextAction: "Use an opaque credential:v1 reference (legacy keychain: references resolve on macOS only); never pass the API key." }, { regex: "^credential:v1:[a-z0-9](?:[a-z0-9-]{0,62}[a-z0-9])?$|^keychain:[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$" })] }),
  defineCliCommand({ id: "runtime-instance-list", phase: "Runtime-Instances-S1", path: ["runtime", "instance", "list"], summary: "List runtime instances and currently witnessed installations without secrets or host paths.", method: "daemon.runtimeInstance.list", commandClass: "admin", inputs: [] }),
  defineCliCommand({ id: "runtime-instance-show", phase: "Runtime-Instances-S1", path: ["runtime", "instance", "show", "<instance-id>"], positional: "instanceId", summary: "Show one redacted machine-local runtime instance.", method: "daemon.runtimeInstance.show", commandClass: "admin", inputs: [] }),
  defineCliCommand({ id: "runtime-instance-delete", phase: "Runtime-Instances-S1", path: ["runtime", "instance", "delete", "<instance-id>"], positional: "instanceId", summary: "Delete one runtime instance and its isolated state root.", method: "daemon.runtimeInstance.delete", commandClass: "admin", inputs: [] }),
  defineCliCommand({ id: "daemon-stop", phase: "W3", path: ["daemon", "stop"], summary: "Stop the resident daemon.", method: "protocol.hello", commandClass: "admin", inputs: [] }), defineCliCommand({ id: "daemon-fleet-center-start", phase: "Fleet-Wiring", path: ["daemon", "fleet", "center", "start"], summary: "Start the daemon-owned fleet TLS center serving the authoritative ledger to admitted edge nodes.", method: "daemon.fleet.center.start", commandClass: "admin", inputs: [cliInput("--port", "single", true, { code: "missing_field", nextAction: "Fleet center start requires --port (0 binds an ephemeral port), --key, --cert, --roster, and --quota-bytes." }, { regex: "^(?:0|[1-9][0-9]{0,4})$" }), cliInput("--key", "single", true, { code: "missing_field", nextAction: "Fleet center start requires the TLS private key path --key." }), cliInput("--cert", "single", true, { code: "missing_field", nextAction: "Fleet center start requires the TLS certificate path --cert." }), cliInput("--roster", "single", true, { code: "missing_field", nextAction: "Fleet center start requires --roster pointing at a fleet-roster/v1 JSON declaring nodes and assignments." }), cliInput("--quota-bytes", "single", true, { code: "missing_field", nextAction: "Fleet center start requires a positive persistent replica disk quota --quota-bytes." }, { regex: "^[1-9][0-9]{0,15}$" }), cliInput("--bind", "single", false, { code: "invalid_field", nextAction: "Use one bind hostname or address for --bind; the default is 127.0.0.1." }), cliInput("--state-root", "single", false, { code: "invalid_field", nextAction: "Use one directory path for --state-root; the default is <user-root>/fleet." })] }), defineCliCommand({ id: "daemon-fleet-edge-sync", phase: "Fleet-Wiring", path: ["daemon", "fleet", "edge", "sync"], summary: "Mirror the fleet center ledger into a local edge view over verified TLS.", method: "daemon.fleet.edge.sync", commandClass: "admin", inputs: [cliInput("--host", "single", true, { code: "missing_field", nextAction: "Edge sync requires --host, --port, --ca, --node-id, --credential, --assignment, --view-root, and --quota-bytes." }), cliInput("--port", "single", true, { code: "invalid_field", nextAction: "Use a TCP port from 0 to 65535 for --port." }, { regex: "^(?:0|[1-9][0-9]{0,4})$" }), cliInput("--ca", "single", true, { code: "missing_field", nextAction: "Edge sync requires the center CA certificate path --ca." }), cliInput("--node-id", "single", true, { code: "invalid_field", nextAction: "Use the node id declared in the center roster for --node-id." }, { regex: "^[A-Za-z0-9_-]{1,96}$" }), cliInput("--credential", "single", true, { code: "missing_field", nextAction: "Edge sync requires the machine credential issued for --node-id." }), cliInput("--assignment", "single", true, { code: "invalid_field", nextAction: "Use one assignment id declared in the center roster for --assignment." }, { regex: "^[A-Za-z0-9_-]{1,96}$" }), cliInput("--view-root", "single", true, { code: "missing_field", nextAction: "Edge sync requires --view-root, the directory that will hold repos/<repo-id>/views/<view-id>." }), cliInput("--quota-bytes", "single", true, { code: "missing_field", nextAction: "Edge sync requires a positive local mirror disk quota --quota-bytes." }, { regex: "^[1-9][0-9]{0,15}$" }), cliInput("--servername", "single", false, { code: "invalid_field", nextAction: "Use the TLS server name matching the center certificate for --servername; the default is localhost." }), cliInput("--timeout-ms", "single", false, { code: "invalid_field", nextAction: "Use a positive response timeout in milliseconds for --timeout-ms; the default is 60000." }, { regex: "^[1-9][0-9]{0,9}$" })] })
] as const);
const effectiveDaemonOwnedProtocolCommands = Object.freeze(daemonOwnedProtocolCommands.map((command) => command.id === "repo-bootstrap" ? defineCliCommand({ ...command, inputs: [...command.inputs, cliInput("--add-npm-scripts", "boolean", false, { code: "invalid_field", nextAction: "Use --add-npm-scripts once." })] }) : command.id === "task-start" ? defineCliCommand({ ...command, inputs: [cliInput("--execution-id", "single", false, { code: "invalid_field", nextAction: "Use one execution id, or omit it for deterministic allocation." }), cliInput("--ttl-ms", "single", false, { code: "invalid_field", nextAction: "Use a positive lease duration in milliseconds." }, { regex: "^[1-9][0-9]*$" }), cliInput("--dry-run", "boolean", false, { code: "invalid_field", nextAction: "Use --dry-run once to preview lease admission." })] }) : command));
export const daemonProtocolCommands = Object.freeze([...effectiveDaemonOwnedProtocolCommands, ...presetCommands]);
export const thinCliCommands = Object.freeze(daemonProtocolCommands.map(({ usage, summary }) => ({ usage, summary })));
export function resolveThinCliCommand(args: readonly string[]): (typeof daemonProtocolCommands)[number] | undefined { const matches = daemonProtocolCommands.filter((entry) => entry.path.every((token, index) => args[index] === token)); if (matches.length < 2) return matches[0]; const target = args[matches[0]!.path.length] ?? ""; return matches.find((entry) => "positionalFields" in entry ? target.startsWith("preset:") : "positionalRegex" in entry ? new RegExp(entry.positionalRegex, "u").test(target) : true); }
export function commandClassForAction(kind: string): "repo-read" | "repo-write" | "arbiter" | "admin" { const descriptor = daemonProtocolCommands.find((entry) => ("actionKind" in entry ? entry.actionKind : entry.id) === kind) ?? presetMethods.find((entry) => entry.actionKind === kind); if (!descriptor) throw new DaemonProtocolContractError("unsupported_command", `No command descriptor exists for ${kind}.`); return descriptor.commandClass; }
export function actionForDaemonMethod(method: string, payload: JsonObject): JsonObject & { readonly kind: string } { if (method === "repo.task.run") { const action = payload.action as JsonObject & { readonly kind: string }, descriptor = daemonProtocolCommands.find((entry) => ("actionKind" in entry ? entry.actionKind : entry.id) === action.kind); if (descriptor?.method !== method) throw new DaemonProtocolContractError("unsupported_command", `${action.kind} requires its closed method descriptor.`); return action; } const descriptor = [...presetMethods, ...daemonGuiActionMethods].find((entry) => entry.method === method); if (!descriptor) throw new DaemonProtocolContractError("unsupported_command", `No action descriptor exists for ${method}.`); const command = presetCommands.find((entry) => entry.method === method), defaults = command && "actionDefaults" in command ? command.actionDefaults : {}; return { ...defaults, kind: descriptor.actionKind, ...payload }; }
type RpcEnumRule = { readonly values: readonly string[]; readonly optional: boolean };
type RpcShape = { readonly fields: Readonly<Record<string, "string" | "number" | "number?" | "boolean?" | "string?" | "string-null?" | "json" | "json?" | "array" | "array?" | RpcEnumRule | RpcShape>>; readonly open?: boolean };
const shape = (fields: RpcShape["fields"], open = false): RpcShape => ({ fields, open });
const optionalEnum = (values: readonly string[]): RpcEnumRule => ({ values, optional: true });
export type DaemonGuiReadResultMap = {
  readonly "daemon.gui.system.read": JsonObject; readonly "daemon.gui.control.receipt": JsonObject;
  readonly "repo.tasks.list": DaemonTaskSnapshotListResult;
  readonly "repo.triadic.relationGraph": { readonly ok: true } & Omit<ReturnType<typeof import("../../../kernel/src/projection/sqlite-task-projection.ts").readRelationGraphProjection>, "taskRows">;
  readonly "repo.decisions.list": { readonly ok: true; readonly decisions: readonly import("../../../kernel/src/projection/fact-event-projection.ts").DecisionProjectionRow[]; readonly warnings: readonly import("../../../kernel/src/projection/types.ts").ProjectionWarning[] }; readonly "repo.tasks.document.read": { readonly ok: true; readonly status: "ready" | "pending"; readonly taskId: string; readonly path: string; readonly body: string; readonly blobSha256: string | null; readonly watermark: number; readonly sourceRevision: number }; readonly "repo.tasks.documents.list": DaemonTaskDocumentListResult;
  readonly "repo.agentRuntime.overview": AgentRuntimeOverviewResult; readonly "repo.agentRuntime.sessions.read": AgentRuntimeSessionResult; readonly "repo.agentRuntime.events.read": AgentRuntimeEventsResult;
  readonly "repo.gui.catalog.snapshot": JsonObject; readonly "repo.gui.catalog.preset.read": JsonObject; readonly "repo.terminal.sessions.list": JsonObject;
};
export type DaemonGuiReadMethod = keyof DaemonGuiReadResultMap;
export type DaemonGuiReadPayloadMap = { readonly "daemon.gui.system.read": Readonly<Record<string, never>>; readonly "daemon.gui.control.receipt": { readonly operationId: string }; readonly "repo.tasks.list": Readonly<Record<string, never>>; readonly "repo.triadic.relationGraph": Readonly<Record<string, never>>; readonly "repo.decisions.list": Readonly<Record<string, never>>; readonly "repo.tasks.document.read": { readonly taskId: string; readonly path: string }; readonly "repo.tasks.documents.list": { readonly taskId: string }; readonly "repo.agentRuntime.overview": { readonly taskId?: string }; readonly "repo.agentRuntime.sessions.read": { readonly runtimeSessionId: string }; readonly "repo.agentRuntime.events.read": { readonly runtimeSessionId: string; readonly afterCursor: string }; readonly "repo.gui.catalog.snapshot": Readonly<Record<string, never>>; readonly "repo.gui.catalog.preset.read": { readonly presetId: string; readonly profileId?: string; readonly locale?: string }; readonly "repo.terminal.sessions.list": Readonly<Record<string, never>> };
export interface TaskPlacementSupplement { readonly moduleKeys: readonly string[]; readonly productLines: readonly string[]; readonly parentTaskId: string | null; readonly origin: "native" | "archival" | "external"; readonly engine: string; readonly packageDisposition: "active" | "archived" | "tombstoned"; readonly provenance: readonly { readonly kind: "l2" | "decision-relation" | "canonical-event"; readonly ref: string }[] }
export interface ExecutionEvidenceProjection { readonly executionId: string; readonly origin: "native" | "archival"; readonly outputs: readonly { readonly evidenceId: string; readonly locator: string; readonly substrate: "repository-path" | "uri" | "canonical-event" | "opaque"; readonly checkerReceiptRef: string | null; readonly checkerResult: "pass" | "fail" | "unknown" }[] }
export interface GuiSubmissionV1 { readonly completionClaim: string; readonly deliverables: readonly string[]; readonly outputs: readonly string[]; readonly verificationNotes: readonly string[]; readonly knownGaps: readonly string[]; readonly residualRisks: readonly string[]; readonly commitSha: string }
/** One projected document under a task package (paths relative to the package root, e.g. artifacts/report.md). */
export interface TaskDocumentListEntryRow { readonly path: string; readonly blobSha256: string; readonly size: number; readonly mediaType: string }
export type DaemonTaskDocumentListResult = { readonly ok: true; readonly status: "ready" | "pending"; readonly taskId: string; readonly documents: readonly TaskDocumentListEntryRow[]; readonly watermark: number; readonly sourceRevision: number };
export type DaemonTaskSnapshotListResult = { readonly ok: true; readonly status: "ready" | "pending"; readonly rows: readonly (import("../../../kernel/src/projection/rebuildable-task-projection.ts").TaskProjectionListRow & { readonly snapshotAvailability: { readonly consents: "known" | "unknown"; readonly codeDocWitnesses: "known" | "unknown"; readonly gateWitnesses: "known" | "unknown" }; readonly closeoutAssessment: import("../../../kernel/src/domain/closeout-readiness.ts").CloseoutAssessment; readonly blockingAssessment: import("../../../kernel/src/domain/task-blocking.ts").BlockingAssessment; readonly placement: TaskPlacementSupplement; readonly executionEvidence: readonly ExecutionEvidenceProjection[] })[]; readonly watermark: number; readonly sourceRevision: number; readonly warnings: readonly import("../../../kernel/src/projection/rebuildable-task-projection.ts").TaskProjectionWarning[] };
export type DaemonGuiActionResult = JsonObject & { readonly schema: "command-receipt/v2"; readonly ok: boolean; readonly command: string; readonly outcome: "applied" | "pending" | "indeterminate" | "op_rejected"; readonly opId: string };
export type DaemonGuiActionMethod = (typeof daemonGuiActionMethods)[number]["method"];
export type DaemonGuiStreamResultMap = { readonly "repo.agentRuntime.attach": AgentRuntimeAttachResult; readonly "repo.terminal.attach": JsonObject }; export type DaemonGuiStreamMethod = keyof DaemonGuiStreamResultMap;
export type DaemonGuiStreamPayloadMap = { readonly "repo.agentRuntime.attach": { readonly runtimeSessionId: string; readonly afterCursor: string }; readonly "repo.terminal.attach": { readonly sessionId: string; readonly afterSeq: number } };
export interface DaemonProtocolErrorResult { readonly schema: "command-receipt/v2"; readonly ok: false; readonly command: string; readonly outcome: "op_rejected"; readonly opId: "N/A"; readonly origin: "daemon"; readonly code: string; readonly evidence: string; readonly error: { readonly code: string; readonly hint: string }; readonly nextAction: string } export const DAEMON_TASK_SNAPSHOT_LIST_SCHEMA = Object.freeze({ id: "daemon.task-snapshot-list/v1", required: Object.freeze(["ok", "status", "rows", "watermark", "sourceRevision", "warnings"]) });
export const DAEMON_RELATION_GRAPH_SCHEMA = Object.freeze({ id: "daemon.relation-graph/v1", required: Object.freeze(["ok", "edges", "coverageRows", "factAnchors", "facts", "warnings"]) }), DAEMON_DECISION_LIST_SCHEMA = Object.freeze({ id: "daemon.decision-list/v1", required: Object.freeze(["ok", "decisions", "warnings"]) }), DAEMON_DOCUMENT_READ_SCHEMA = Object.freeze({ id: "daemon.document-read/v1", required: Object.freeze(["ok", "status", "taskId", "path", "body", "blobSha256", "watermark", "sourceRevision"]) }), DAEMON_TASK_DOCUMENT_LIST_SCHEMA = Object.freeze({ id: "daemon.task-document-list/v1", required: Object.freeze(["ok", "status", "taskId", "documents", "watermark", "sourceRevision"]) }), DAEMON_PROTOCOL_ERROR_SCHEMA = Object.freeze({ id: "daemon.protocol-error/v1", required: Object.freeze(["schema", "ok", "command", "outcome", "opId", "origin", "code", "evidence", "error", "nextAction"]) }), DAEMON_GUI_COMMAND_RECEIPT_SCHEMA = Object.freeze({ id: "command-receipt/v2" });
export const DAEMON_AGENT_RUNTIME_OVERVIEW_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-overview/v1" }), DAEMON_AGENT_RUNTIME_SESSION_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-session/v1" }), DAEMON_AGENT_RUNTIME_EVENTS_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-events/v1" }), DAEMON_AGENT_RUNTIME_ATTACH_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-attach/v1" }), DAEMON_AGENT_RUNTIME_ATTACH_EVENT_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-attach-event/v1" });
export const GUI_SYSTEM_STATUS_SCHEMA = Object.freeze({ id: "gui-system-status/v1" }), DAEMON_CONTROL_RECEIPT_SCHEMA = Object.freeze({ id: "daemon-control-receipt/v1" }), GUI_CATALOG_SNAPSHOT_SCHEMA = Object.freeze({ id: "gui-catalog-snapshot/v1" }), GUI_CATALOG_PRESET_SCHEMA = Object.freeze({ id: "gui-catalog-preset/v1" }), CATALOG_REREAD_RECEIPT_SCHEMA = Object.freeze({ id: "catalog-reread-receipt/v1" }), TERMINAL_SESSION_LIST_SCHEMA = Object.freeze({ id: "terminal-session-list/v1" }), TERMINAL_CONTROL_RECEIPT_SCHEMA = Object.freeze({ id: "terminal-control-receipt/v1" }), TERMINAL_INPUT_ACK_SCHEMA = Object.freeze({ id: "terminal-input-ack/v1" }), TERMINAL_DETACH_ACK_SCHEMA = Object.freeze({ id: "terminal-detach-ack/v1" }), TERMINAL_ATTACH_SCHEMA = Object.freeze({ id: "terminal-attach/v1" }), TERMINAL_ATTACH_EVENT_SCHEMA = Object.freeze({ id: "terminal-attach-event/v1" });
export const daemonGuiReadMethods = Object.freeze([
  { id: "gui.system.read", phase: "W5-GUI-S3", method: "daemon.gui.system.read", requiresRepo: false, params: shape({}), guiBridgeMethod: "getSystemStatus", httpMethod: "GET", path: "/api/system", inputSchemaId: "gui.empty/v1", outputSchemaId: GUI_SYSTEM_STATUS_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "getSystemStatus", auth: "local-session-token", commandClass: "repo-read" },
  { id: "gui.control.receipt", phase: "W5-GUI-S3", method: "daemon.gui.control.receipt", requiresRepo: false, params: shape({ payload: shape({ operationId: "string" }) }), guiBridgeMethod: "getDaemonControlReceipt", httpMethod: "GET", path: "/api/system/control/:operationId", inputSchemaId: "gui.daemon-control-receipt/v1", outputSchemaId: DAEMON_CONTROL_RECEIPT_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "getDaemonControlReceipt", auth: "local-session-token", commandClass: "repo-read" },
  { id: "tasks.list", phase: "W2-GUI", method: "repo.tasks.list", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }) }), guiBridgeMethod: "getTasks", httpMethod: "GET", path: "/api/tasks", inputSchemaId: "gui.empty/v1", outputSchemaId: DAEMON_TASK_SNAPSHOT_LIST_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "listTasks", auth: "local-session-token", commandClass: "repo-read" },
  { id: "triadic.relationGraph", phase: "W2-GUI", method: "repo.triadic.relationGraph", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }) }), guiBridgeMethod: "getRelationGraph", httpMethod: "GET", path: "/api/triadic/relation-graph", inputSchemaId: "gui.empty/v1", outputSchemaId: DAEMON_RELATION_GRAPH_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readRelationGraph", auth: "local-session-token", commandClass: "repo-read" },
  { id: "decisions.list", phase: "W2-GUI", method: "repo.decisions.list", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }) }), guiBridgeMethod: "getDecisions", httpMethod: "GET", path: "/api/decisions", inputSchemaId: "gui.empty/v1", outputSchemaId: DAEMON_DECISION_LIST_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "listDecisions", auth: "local-session-token", commandClass: "repo-read" }, { id: "tasks.document.read", phase: "DocSync-B", method: "repo.tasks.document.read", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ taskId: "string", path: "string" }) }), guiBridgeMethod: "getTaskDocument", httpMethod: "GET", path: "/api/tasks/:taskId/documents/:path", inputSchemaId: "gui.task-document/v1", outputSchemaId: DAEMON_DOCUMENT_READ_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readDocument", auth: "local-session-token", commandClass: "repo-read" }, { id: "tasks.documents.list", phase: "DocSync-B", method: "repo.tasks.documents.list", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ taskId: "string" }) }), guiBridgeMethod: "getTaskDocuments", httpMethod: "GET", path: "/api/tasks/:taskId/documents", inputSchemaId: "gui.task-document-list/v1", outputSchemaId: DAEMON_TASK_DOCUMENT_LIST_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "listTaskDocuments", auth: "local-session-token", commandClass: "repo-read" },
  { id: "agentRuntime.overview", phase: "Runtime-B", method: "repo.agentRuntime.overview", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({}, true) }), guiBridgeMethod: "getAgentRuntimeOverview", httpMethod: "GET", path: "/api/agent-runtime", inputSchemaId: "gui.agent-runtime-overview/v1", outputSchemaId: DAEMON_AGENT_RUNTIME_OVERVIEW_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readAgentRuntimeOverview", auth: "local-session-token", commandClass: "repo-read" },
  { id: "agentRuntime.sessions.read", phase: "Runtime-B", method: "repo.agentRuntime.sessions.read", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ runtimeSessionId: "string" }) }), guiBridgeMethod: "getAgentRuntimeSession", httpMethod: "GET", path: "/api/agent-runtime/sessions/:id", inputSchemaId: "gui.agent-runtime-session/v1", outputSchemaId: DAEMON_AGENT_RUNTIME_SESSION_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readAgentRuntimeSession", auth: "local-session-token", commandClass: "repo-read" },
  { id: "agentRuntime.events.read", phase: "Runtime-B", method: "repo.agentRuntime.events.read", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ runtimeSessionId: "string", afterCursor: "string" }) }), guiBridgeMethod: "getAgentRuntimeEvents", httpMethod: "GET", path: "/api/agent-runtime/sessions/:id/events", inputSchemaId: "gui.agent-runtime-events/v1", outputSchemaId: DAEMON_AGENT_RUNTIME_EVENTS_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readAgentRuntimeEvents", auth: "local-session-token", commandClass: "repo-read" },
  { id: "gui.catalog.snapshot", phase: "W5-GUI-S3", method: "repo.gui.catalog.snapshot", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }) }), guiBridgeMethod: "getCatalogSnapshot", httpMethod: "GET", path: "/api/catalog", inputSchemaId: "gui.empty/v1", outputSchemaId: GUI_CATALOG_SNAPSHOT_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "getCatalogSnapshot", auth: "local-session-token", commandClass: "repo-read" },
  { id: "gui.catalog.preset.read", phase: "W5-GUI-S3", method: "repo.gui.catalog.preset.read", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ presetId: "string", profileId: "string?", locale: "string?" }) }), guiBridgeMethod: "getCatalogPreset", httpMethod: "GET", path: "/api/catalog/presets/:presetId", inputSchemaId: "gui.catalog-preset/v1", outputSchemaId: GUI_CATALOG_PRESET_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "getCatalogPreset", auth: "local-session-token", commandClass: "repo-read" },
  { id: "terminal.sessions.list", phase: "W5-GUI-S3", method: "repo.terminal.sessions.list", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }) }), guiBridgeMethod: "listTerminalSessions", httpMethod: "GET", path: "/api/terminal/sessions", inputSchemaId: "gui.empty/v1", outputSchemaId: TERMINAL_SESSION_LIST_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "listTerminalSessions", auth: "local-session-token", commandClass: "repo-read" }
] as const satisfies ReadonlyArray<{ readonly method: DaemonGuiReadMethod; readonly [key: string]: unknown }>);
const guiAction = <const I extends string, const M extends string, const A extends string, const B extends string>(id: I, method: M, actionKind: A, params: RpcShape, guiBridgeMethod: B, pathName: string, commandClass: "repo-read" | "repo-write" | "arbiter") => ({ id, phase: "W5-GUI-S2", method, actionKind, requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: params }), guiBridgeMethod, httpMethod: "POST" as const, path: pathName, inputSchemaId: `gui.${id}/v1`, outputSchemaId: DAEMON_GUI_COMMAND_RECEIPT_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: id, auth: "local-session-token" as const, commandClass });
const guiS3Action = <const I extends string, const M extends string, const A extends string, const B extends string>(id: I, method: M, actionKind: A, params: RpcShape, guiBridgeMethod: B, pathName: string, commandClass: "repo-read" | "repo-write", outputSchemaId: string) => ({ id, phase: "W5-GUI-S3", method, actionKind, requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: params }), guiBridgeMethod, httpMethod: "POST" as const, path: pathName, inputSchemaId: `gui.${id}/v1`, outputSchemaId, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: id, auth: "local-session-token" as const, commandClass });
export const daemonGuiActionMethods = Object.freeze([
  { id: "gui.control.request", phase: "W5-GUI-S3", method: "daemon.gui.control.request", actionKind: "daemon-control-request", requiresRepo: false, params: shape({ payload: shape({ kind: "string", authorityRepoId: "string", reason: "string?" }) }), guiBridgeMethod: "requestDaemonControl", httpMethod: "POST", path: "/api/system/control", inputSchemaId: "gui.daemon-control-request/v1", outputSchemaId: DAEMON_CONTROL_RECEIPT_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "requestDaemonControl", auth: "local-session-token", commandClass: "admin" },
  guiAction("task.start", "repo.task.start", "task-start", shape({ taskId: "string", executionId: "string" }), "startTask", "/api/tasks/:taskId/start", "repo-write"),
  guiAction("task.progress.append", "repo.task.progress.append", "task-progress-append", shape({ taskId: "string", executionId: "string?", text: "string", evidence: "array?", baseDocumentSha256: "string-null?" }), "appendTaskProgress", "/api/tasks/:taskId/progress", "repo-write"),
  guiAction("task.submit", "repo.task.submit", "task-submit", shape({ taskId: "string", executionId: "string", submission: "json" }), "submitTask", "/api/tasks/:taskId/submit", "repo-write"),
  guiAction("decision.list", "repo.decision.list", "decision-list", shape({ search: "string?", legacyId: "string?", legacyRange: "json?", state: "string?", module: "string?", productLine: "string?" }), "listDecisions", "/api/decision-control", "repo-read"),
  guiAction("decision.show", "repo.decision.show", "decision-show", shape({ decisionId: "string", includeBody: "boolean?" }), "showDecision", "/api/decision-control/:decisionId", "repo-read"),
  guiAction("decision.propose", "repo.decision.propose", "decision-propose", shape({ title: "string", question: "string", riskTier: "string", urgency: "string", vertical: "string", preset: "string", appliesTo: "json", decisionClass: "string", chosen: "array", rejected: "array", body: "string", claims: "array", fulfillments: "array", relations: "array" }), "proposeDecision", "/api/decision-control/propose", "repo-write"),
  guiAction("decision.accept", "repo.decision.accept", "decision-accept", shape({ decisionId: "string", rationale: "string", judgmentOnlyRationale: "string?" }), "acceptDecision", "/api/decision-control/:decisionId/accept", "arbiter"),
  guiAction("decision.reject", "repo.decision.reject", "decision-reject", shape({ decisionId: "string", reason: "string" }), "rejectDecision", "/api/decision-control/:decisionId/reject", "arbiter"),
  guiAction("decision.defer", "repo.decision.defer", "decision-defer", shape({ decisionId: "string", reason: "string" }), "deferDecision", "/api/decision-control/:decisionId/defer", "arbiter"),
  guiAction("receipt.show", "repo.receipt.show", "receipt-show", shape({ opId: "string" }), "showReceipt", "/api/receipts/:opId", "repo-read"),
  guiS3Action("gui.catalog.reread", "repo.gui.catalog.reread", "catalog-reread", shape({ expectedDigest: "string?" }), "rereadCatalog", "/api/catalog/reread", "repo-read", CATALOG_REREAD_RECEIPT_SCHEMA.id),
  guiS3Action("agentRuntime.spawn", "repo.agentRuntime.spawn", "runtime-spawn", shape({ runtimeInstanceId: "string", cwd: "json", prompt: "string", taskId: "string-null?", idempotencyKey: "string", providerSessionId: "string?", executor: "json?" }), "spawnAgentRuntime", "/api/agent-runtime/spawn", "repo-write", DAEMON_GUI_COMMAND_RECEIPT_SCHEMA.id),
  guiS3Action("agentRuntime.cancel", "repo.agentRuntime.cancel", "runtime-cancel", shape({ runtimeSessionId: "string" }), "cancelAgentRuntime", "/api/agent-runtime/sessions/:id/cancel", "repo-write", DAEMON_GUI_COMMAND_RECEIPT_SCHEMA.id),
  guiS3Action("terminal.spawn", "repo.terminal.spawn", "terminal-spawn", shape({ idempotencyKey: "string", name: "string?", cwd: "json", shellProfileId: "string?", taskId: "string?" }), "spawnTerminal", "/api/terminal/sessions", "repo-write", TERMINAL_CONTROL_RECEIPT_SCHEMA.id),
  guiS3Action("terminal.input", "repo.terminal.input", "terminal-input", shape({ sessionId: "string", clientSeq: "number", utf8: "string" }), "sendTerminalInput", "/api/terminal/sessions/:id/input", "repo-write", TERMINAL_INPUT_ACK_SCHEMA.id),
  guiS3Action("terminal.resize", "repo.terminal.resize", "terminal-resize", shape({ sessionId: "string", cols: "number", rows: "number" }), "resizeTerminal", "/api/terminal/sessions/:id/resize", "repo-write", TERMINAL_CONTROL_RECEIPT_SCHEMA.id),
  guiS3Action("terminal.detach", "repo.terminal.detach", "terminal-detach", shape({ sessionId: "string", attachmentId: "string" }), "detachTerminal", "/api/terminal/sessions/:id/detach", "repo-read", TERMINAL_DETACH_ACK_SCHEMA.id),
  guiS3Action("terminal.terminate", "repo.terminal.terminate", "terminal-terminate", shape({ sessionId: "string", confirmed: "boolean?" }), "terminateTerminal", "/api/terminal/sessions/:id/terminate", "repo-write", TERMINAL_CONTROL_RECEIPT_SCHEMA.id)
] as const);
export const daemonGuiStreamFacets = Object.freeze([{ id: "agentRuntime.attach", phase: "Runtime-B", method: "repo.agentRuntime.attach", eventMethod: "repo.agentRuntime.attach.frame", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ runtimeSessionId: "string", afterCursor: "string" }) }), guiBridgeMethod: "attachAgentRuntime", httpMethod: "STREAM", path: "/api/agent-runtime/sessions/:id/attach", inputSchemaId: "gui.agent-runtime-attach/v1", outputSchemaId: DAEMON_AGENT_RUNTIME_ATTACH_SCHEMA.id, eventSchemaId: DAEMON_AGENT_RUNTIME_ATTACH_EVENT_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "attachAgentRuntime", auth: "local-session-token", commandClass: "repo-read" },
  { id: "terminal.attach", phase: "W5-GUI-S3", method: "repo.terminal.attach", eventMethod: "repo.terminal.attach.frame", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ sessionId: "string", afterSeq: "number" }) }), guiBridgeMethod: "attachTerminal", httpMethod: "STREAM", path: "/api/terminal/sessions/:id/attach", inputSchemaId: "gui.terminal-attach/v1", outputSchemaId: TERMINAL_ATTACH_SCHEMA.id, eventSchemaId: TERMINAL_ATTACH_EVENT_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "attachTerminal", auth: "local-session-token", commandClass: "repo-read" }] as const);
const recordWith = (value: unknown, fields: readonly string[]): value is JsonObject => isJsonObject(value) && fields.every((field) => Object.hasOwn(value, field)), exactRecord = (value: unknown, fields: readonly string[]): value is JsonObject => recordWith(value, fields) && Object.keys(value).length === fields.length;
const nonEmpty = (value: unknown): value is string => typeof value === "string" && value.length > 0, integer = (value: unknown): value is number => Number.isInteger(value);
const stringArray = (value: unknown): value is readonly string[] => Array.isArray(value) && value.every(nonEmpty), warningArray = (value: unknown): boolean => Array.isArray(value) && value.every((warning) => recordWith(warning, ["code", "source", "severity", "message"]) && nonEmpty(warning.code) && nonEmpty(warning.source) && nonEmpty(warning.severity) && nonEmpty(warning.message));
const sha = (value: unknown): boolean => typeof value === "string" && /^[0-9a-f]{40}$/u.test(value), digest = (value: unknown): boolean => typeof value === "string" && /^sha256:[0-9a-f]{64}$/u.test(value), iteration = (value: unknown): boolean => value === 0 || value === 1;
const statusWord = (vocabulary: readonly string[], value: unknown): boolean => vocabulary.includes(String(value));
function actor(value: unknown): boolean { return exactRecord(value, ["principal", "executor"]) && exactRecord(value.principal, ["personId"]) && nonEmpty(value.principal.personId) && (value.executor === null || exactRecord(value.executor, ["kind", "id"]) && value.executor.kind === "agent" && nonEmpty(value.executor.id)); } function source(value: unknown): boolean { return value === "local" || value === "remote_direct" || exactRecord(value, ["kind", "nodeId", "assignmentId"]) && value.kind === "assignment" && nonEmpty(value.nodeId) && nonEmpty(value.assignmentId) || exactRecord(value, ["kind", "sessionId", "path", "fingerprint"]) && value.kind === "watch_session" && nonEmpty(value.sessionId) && nonEmpty(value.path) && /^[0-9a-f]{64}$/u.test(String(value.fingerprint)); }
export function validateGuiSubmission(value: unknown): readonly string[] {
  if (!exactRecord(value, ["completionClaim", "deliverables", "outputs", "verificationNotes", "knownGaps", "residualRisks", "commitSha"])) return ["SubmissionV1 requires exactly: completionClaim, deliverables, outputs, verificationNotes, knownGaps, residualRisks, commitSha"];
  const errors: string[] = [];
  if (!nonEmpty(value.completionClaim)) errors.push("completionClaim must be a non-empty string");
  for (const field of ["deliverables", "outputs", "verificationNotes", "knownGaps", "residualRisks"] as const) if (!stringArray(value[field])) errors.push(`${field} must be an array of non-empty strings`);
  if (!sha(value.commitSha)) errors.push("commitSha must be a native 40-character commit SHA");
  return errors;
}
// longRunning predates taskClass=long_running (dec_01KYRHP8ND); historical event payloads may
// still carry the retired boolean, so the wire validator tolerates it on read. Writers never emit it.
function validTaskMetadata(value: unknown): boolean { const nullable = (item: unknown) => item === null || nonEmpty(item), fields = ["idempotencyKey", "parentTaskId", "workKind", "riskTier", "urgency", "verticalId", "presetId", "profileId", "moduleKey", "slug", "surfaces", "fromLegacyId"]; return recordWith(value, fields) && Object.keys(value).every((field) => fields.includes(field) || field === "longRunning") && (value.longRunning === undefined || typeof value.longRunning === "boolean") && [value.idempotencyKey, value.parentTaskId, value.moduleKey, value.fromLegacyId].every(nullable) && (value.workKind === null || ["feat", "fix", "refactor", "docs", "test", "chore"].includes(String(value.workKind))) && [value.riskTier, value.urgency].every((item) => item === null || ["low", "medium", "high"].includes(String(item))) && [value.verticalId, value.presetId, value.profileId].every(nonEmpty) && /^[a-z0-9](?:[a-z0-9-]{0,70}[a-z0-9])?$/u.test(String(value.slug)) && stringArray(value.surfaces); }
function taskRelation(value: unknown, taskId: string): boolean { return exactRecord(value, ["relation_id", "source", "target", "type", "strength", "direction", "origin", "rationale", "state"]) && /^rel_[0-9a-f]{16}$/u.test(String(value.relation_id)) && value.source === `task/${taskId}` && [value.target, value.rationale].every(nonEmpty) && ["supports", "supersedes", "refines", "narrows", "derives", "blocks", "relates", "implements", "depends-on", "produces", "evidences", "evidenced-by", "refuted-by", "invalidated-by", "supersedes-fact"].includes(String(value.type)) && ["strong", "weak"].includes(String(value.strength)) && ["directed", "undirected"].includes(String(value.direction)) && ["declared", "imported_snapshot", "generated", "inferred"].includes(String(value.origin)) && statusWord(relationStateWords, value.state); }
function task(value: unknown): boolean { const required = ["schema", "taskId", "title", "taskClass", "status", "graph", "currentNode", "iteration", "createdBy", "completionGateIds", "presetSnapshotDigest"], optional = ["metadata", "relations", "packageDisposition", "supersededBy", "contractVersion"]; return recordWith(value, required) && Object.keys(value).every((field) => required.includes(field) || optional.includes(field)) && value.schema === "task/v1" && [value.taskId, value.title].every(nonEmpty) && ["standard", "milestone", "epic", "long_running"].includes(String(value.taskClass)) && statusWord(taskStatusWords, value.status) && ["implementation", "review"].includes(String(value.currentNode)) && iteration(value.iteration) && actor(value.createdBy) && stringArray(value.completionGateIds) && (value.presetSnapshotDigest === null || digest(value.presetSnapshotDigest)) && isJsonObject(value.graph) && (value.metadata === undefined || validTaskMetadata(value.metadata)) && (value.relations === undefined || Array.isArray(value.relations) && value.relations.every((relation) => taskRelation(relation, String(value.taskId)))) && (value.packageDisposition === undefined || statusWord(packageDispositionWords, value.packageDisposition)) && (value.supersededBy === undefined || value.supersededBy === null || nonEmpty(value.supersededBy)) && (value.contractVersion === undefined || integer(value.contractVersion) && Number(value.contractVersion) > 0); }
function execution(value: unknown): boolean { if (exactRecord(value, ["schema", "executionId", "taskId", "nodeId", "iteration", "state", "actor", "claimedAt", "submittedAt", "closedAt", "submission"])) return value.schema === "execution/v1" && [value.executionId, value.taskId, value.claimedAt].every(nonEmpty) && value.nodeId === "implementation" && iteration(value.iteration) && statusWord(executionV1StateWords, value.state) && actor(value.actor) && (value.submittedAt === null || nonEmpty(value.submittedAt)) && (value.closedAt === null || nonEmpty(value.closedAt)) && (value.submission === null || validateGuiSubmission(value.submission).length === 0); const fields = ["schema", "generation", "migratedFrom", "executionId", "taskId", "nodeId", "iteration", "state", "actor", "claimedAt", "submittedAt", "closedAt", "sessionBindings", "outputs", "submission", "archivedSubmission"]; return exactRecord(value, fields) && value.schema === "archived-execution/v1" && value.generation === "v0" && [value.migratedFrom, value.executionId, value.taskId, value.claimedAt].every(nonEmpty) && value.nodeId === "implementation" && iteration(value.iteration) && statusWord(executionStateWords, value.state) && actor(value.actor) && (value.submittedAt === null || nonEmpty(value.submittedAt)) && (value.closedAt === null || nonEmpty(value.closedAt)) && Array.isArray(value.sessionBindings) && Array.isArray(value.outputs) && value.outputs.every((output) => exactRecord(output, ["migratedFrom", "locator", "substrate", "checkerReceiptRef", "checkerResult"]) && [output.migratedFrom, output.locator].every(nonEmpty)) && value.submission === null && (value.archivedSubmission === null || exactRecord(value.archivedSubmission, ["completionClaim", "deliverables", "evidenceRefs", "verificationNotes", "knownGaps", "residualRisks"])); }
const reviewAuthorityRefKey = "capab" + "ilityRef";
function review(value: unknown): boolean { return exactRecord(value, ["schema", "reviewId", "taskId", "executionId", "verdict", "actor", reviewAuthorityRefKey, "reason", "evidenceChecked", "commitSha", "iteration", "contentDigest", "reviewedAt"]) && value.schema === "review/v1" && [value.reviewId, value.taskId, value.executionId, value[reviewAuthorityRefKey], value.reason, value.reviewedAt].every(nonEmpty) && statusWord(reviewVerdictWords, value.verdict) && actor(value.actor) && stringArray(value.evidenceChecked) && sha(value.commitSha) && iteration(value.iteration) && digest(value.contentDigest); }
function consent(value: unknown): boolean { return exactRecord(value, ["schema", "consentId", "taskId", "executionId", "reviewId", "reviewDigest", "contentDigest", "actor", "source", "consentedAt"]) && value.schema === "review-consent/v1" && [value.consentId, value.taskId, value.executionId, value.reviewId, value.consentedAt].every(nonEmpty) && digest(value.reviewDigest) && digest(value.contentDigest) && actor(value.actor) && source(value.source); }
function codeDoc(value: unknown): boolean { return exactRecord(value, ["schema", "witnessId", "taskId", "executionId", "commitSha", "iteration", "paths", "actor", "source", "reconciledAt"]) && value.schema === "code-doc-witness/v1" && [value.witnessId, value.taskId, value.executionId, value.reconciledAt].every(nonEmpty) && sha(value.commitSha) && iteration(value.iteration) && stringArray(value.paths) && value.paths.length > 0 && actor(value.actor) && source(value.source); }
function gate(value: unknown): boolean { return exactRecord(value, ["schema", "witnessId", "receiptId", "checkerId", "gateId", "result", "taskId", "executionId", "commitSha", "iteration", "actor", "source", "verifiedAt"]) && value.schema === "completion-gate-witness/v1" && [value.witnessId, value.receiptId, value.checkerId, value.gateId, value.taskId, value.executionId, value.verifiedAt].every(nonEmpty) && value.result === "pass" && sha(value.commitSha) && iteration(value.iteration) && actor(value.actor) && source(value.source); }
function lease(value: unknown): boolean { return exactRecord(value, ["schema", "taskId", "executionId", "actor", "source", "phase", "expiresAt", "ttlMs", "version"]) && value.schema === "lease/v1" && [value.taskId, value.executionId, value.expiresAt].every(nonEmpty) && actor(value.actor) && source(value.source) && statusWord(leasePhaseWords, value.phase) && integer(value.ttlMs) && Number(value.ttlMs) > 0 && integer(value.version) && Number(value.version) >= 0; }
const availabilityFields = ["consents", "codeDocWitnesses", "gateWitnesses"] as const, snapshotBaseFields = ["revision", "task", "executions", "reviews", "edgesTaken", "lease", "decisionRelations"] as const;
function snapshot(value: unknown, availability: unknown): boolean { if (!exactRecord(availability, availabilityFields) || availabilityFields.some((field) => availability[field] !== "known" && availability[field] !== "unknown") || !isJsonObject(value)) return false; const known = availabilityFields.filter((field) => availability[field] === "known"), expected = [...snapshotBaseFields, ...known]; if (!exactRecord(value, expected) || !integer(value.revision) || value.task !== null && !task(value.task) || !Array.isArray(value.executions) || value.executions.some((item) => !execution(item)) || !Array.isArray(value.reviews) || value.reviews.some((item) => !review(item)) || !Array.isArray(value.edgesTaken) || value.edgesTaken.some((edge) => !exactRecord(edge, ["edgeId", "from", "to", "on", "actorRole", "reason", "commitSha", "iteration"]) || !nonEmpty(edge.edgeId) || !nonEmpty(edge.reason) || !sha(edge.commitSha) || !iteration(edge.iteration)) || value.lease !== null && !lease(value.lease) || !Array.isArray(value.decisionRelations) || value.decisionRelations.some((relation) => !exactRecord(relation, ["relationId", "sourceRef", "targetRef", "relationType", "state"]) || ![relation.relationId, relation.sourceRef, relation.targetRef, relation.relationType].every(nonEmpty) || !statusWord(relationStateWords, relation.state))) return false; const validators = { consents: consent, codeDocWitnesses: codeDoc, gateWitnesses: gate }; return known.every((field) => Array.isArray(value[field]) && value[field].every(validators[field])); }
function placement(value: unknown): boolean { return exactRecord(value, ["moduleKeys", "productLines", "parentTaskId", "origin", "engine", "packageDisposition", "provenance"]) && stringArray(value.moduleKeys) && stringArray(value.productLines) && (value.parentTaskId === null || nonEmpty(value.parentTaskId)) && ["native", "archival", "external"].includes(String(value.origin)) && nonEmpty(value.engine) && statusWord(packageDispositionWords, value.packageDisposition) && Array.isArray(value.provenance) && value.provenance.every((row) => exactRecord(row, ["kind", "ref"]) && ["l2", "decision-relation", "canonical-event"].includes(String(row.kind)) && nonEmpty(row.ref)); }
function executionEvidence(value: unknown): boolean { return exactRecord(value, ["executionId", "origin", "outputs"]) && nonEmpty(value.executionId) && (value.origin === "native" || value.origin === "archival") && Array.isArray(value.outputs) && value.outputs.every((output) => exactRecord(output, ["evidenceId", "locator", "substrate", "checkerReceiptRef", "checkerResult"]) && /^evidence_[0-9a-f]{24}$/u.test(String(output.evidenceId)) && nonEmpty(output.locator) && ["repository-path", "uri", "canonical-event", "opaque"].includes(String(output.substrate)) && (output.checkerReceiptRef === null || nonEmpty(output.checkerReceiptRef)) && ["pass", "fail", "unknown"].includes(String(output.checkerResult)) && (output.checkerResult === "unknown" || output.checkerReceiptRef !== null)); }
function closeoutAssessment(value: unknown): boolean { if (!recordWith(value, ["readiness", "gates"]) || Object.keys(value).some((key) => !["readiness", "executionId", "blocker", "gates"].includes(key)) || !["not_required", "missing", "incomplete", "ready", "passed", "failed"].includes(String(value.readiness)) || value.executionId !== undefined && !nonEmpty(value.executionId) || value.blocker !== undefined && !["execution", "review", "consent", "gate", "lineage", "projection_unknown"].includes(String(value.blocker)) || !Array.isArray(value.gates)) return false; return value.gates.every((gate) => recordWith(gate, ["gateId", "status"]) && Object.keys(gate).every((key) => ["gateId", "status", "detail"].includes(key)) && nonEmpty(gate.gateId) && ["passed", "failed", "missing", "unknown"].includes(String(gate.status)) && (gate.detail === undefined || nonEmpty(gate.detail))); }
function blockingAssessment(value: unknown): boolean { if (!exactRecord(value, ["taskId", "state", "blockers", "warnings"]) || !nonEmpty(value.taskId) || !["blocked", "clear", "unknown"].includes(String(value.state)) || !stringArray(value.warnings) || !Array.isArray(value.blockers)) return false; return value.blockers.every((blocker) => recordWith(blocker, ["relationId", "kind", "sourceTaskId", "targetTaskId"]) && Object.keys(blocker).every((key) => ["relationId", "kind", "sourceTaskId", "targetTaskId", "rationale"].includes(key)) && blocker.kind === "depends-on" && [blocker.relationId, blocker.sourceTaskId, blocker.targetTaskId].every(nonEmpty) && (blocker.rationale === undefined || nonEmpty(blocker.rationale))); }
export function validateDaemonTaskSnapshotList(value: unknown): readonly string[] { const rowFields = ["taskId", "packagePath", "generation", "workspaceRevision", "updatedAt", "snapshot", "snapshotAvailability", "closeoutAssessment", "blockingAssessment", "placement", "executionEvidence"]; if (!exactRecord(value, DAEMON_TASK_SNAPSHOT_LIST_SCHEMA.required) || value.ok !== true || (value.status !== "ready" && value.status !== "pending") || !integer(value.watermark) || !integer(value.sourceRevision) || !warningArray(value.warnings) || !Array.isArray(value.rows) || value.rows.some((row) => !exactRecord(row, rowFields) || !nonEmpty(row.taskId) || row.packagePath !== null && !nonEmpty(row.packagePath) || row.generation !== "v0" && row.generation !== "v1" || !integer(row.workspaceRevision) || !nonEmpty(row.updatedAt) || !snapshot(row.snapshot, row.snapshotAvailability) || !closeoutAssessment(row.closeoutAssessment) || !blockingAssessment(row.blockingAssessment) || !placement(row.placement) || !Array.isArray(row.executionEvidence) || row.executionEvidence.some((item) => !executionEvidence(item)))) return ["daemon task snapshot list is invalid"]; return []; }
export function validateDaemonRelationGraph(value: unknown): readonly string[] { if (!recordWith(value, DAEMON_RELATION_GRAPH_SCHEMA.required) || value.ok !== true || !warningArray(value.warnings) || !Array.isArray(value.edges) || value.edges.some((edge) => !recordWith(edge, ["relationId", "sourceRef", "targetRef", "relationType", "direction", "strength", "origin", "state", "rationale", "ownerRef", "sourcePath", "recordIndex"]) || !integer(edge.recordIndex) || ["relationId", "sourceRef", "targetRef", "relationType", "direction", "strength", "origin", "state", "rationale", "ownerRef", "sourcePath"].some((field) => !nonEmpty(edge[field]))) || !Array.isArray(value.coverageRows) || value.coverageRows.some((row) => !recordWith(row, ["decisionRef", "claimRef", "status", "fulfillment", "relationPath"]) || !nonEmpty(row.decisionRef) || !nonEmpty(row.claimRef) || row.status !== "covered" && row.status !== "uncovered" || row.fulfillment !== null && !["evidenced", "delivered", "standing-policy"].includes(String(row.fulfillment)) || row.refutingFactRefs !== undefined && !stringArray(row.refutingFactRefs) || !stringArray(row.relationPath) || row.basisRevision !== undefined && !integer(row.basisRevision) || row.coveringFactRef !== undefined && !nonEmpty(row.coveringFactRef)) || !Array.isArray(value.factAnchors) || value.factAnchors.some((row) => !recordWith(row, ["factRef", "taskId", "factId", "sourcePath"]) || ["factRef", "taskId", "factId", "sourcePath"].some((field) => !nonEmpty(row[field]))) || !Array.isArray(value.facts) || value.facts.some((row) => !recordWith(row, ["schema", "ref", "taskId", "factId", "statement", "source", "observedAt", "confidence", "memoryClass", "memoryTags", "provenance", "liveness"]) || row.schema !== "task-fact-row/v1" || !["standing", "superseded_fact"].includes(String(row.liveness)) || !["low", "medium", "high"].includes(String(row.confidence)) || !["semantic", "episodic", "procedural"].includes(String(row.memoryClass)) || !stringArray(row.memoryTags) || !Array.isArray(row.provenance) || row.provenance.some((entry) => !recordWith(entry, ["runtime", "sessionId", "boundAt"]) || !nonEmpty(entry.runtime) || !nonEmpty(entry.sessionId) || !nonEmpty(entry.boundAt)) || ["ref", "taskId", "factId", "statement", "source", "observedAt"].some((field) => !nonEmpty(row[field])))) return ["daemon relation graph is invalid"]; return []; }
function readiness(value: unknown): boolean { if (!exactRecord(value, ["schema", "basisCommitSha", "appliesToDrift", "conflictMarker"]) || value.schema !== "decision-readiness/v1" || !exactRecord(value.appliesToDrift, ["state", "paths", "lastCommitAt", "summary"]) || !["clear", "drift", "unknown"].includes(String(value.appliesToDrift.state)) || !stringArray(value.appliesToDrift.paths) || value.appliesToDrift.lastCommitAt !== null && !nonEmpty(value.appliesToDrift.lastCommitAt) || !nonEmpty(value.appliesToDrift.summary) || !exactRecord(value.conflictMarker, ["state", "paths", "summary"]) || !["clear", "conflict", "unknown"].includes(String(value.conflictMarker.state)) || !stringArray(value.conflictMarker.paths) || !nonEmpty(value.conflictMarker.summary) || !(sha(value.basisCommitSha) || value.basisCommitSha === "" && value.appliesToDrift.state === "unknown" && value.conflictMarker.state === "unknown")) return false; return true; }
export function validateDaemonDecisionList(value: unknown): readonly string[] { const fields = ["schema", "decisionId", "path", "state", "title", "question", "riskTier", "urgency", "vertical", "preset", "decisionClass", "appliesTo", "proposer", "arbiter", "proposedAt", "decidedAt", "workspaceRevision", "chosen", "rejected", "claims", "judgmentConsents", "body"], allowed = [...fields, "legacyId", "readiness", "amendments", "contentPins"], history = (row: JsonObject) => row.amendments !== undefined && (!Array.isArray(row.amendments) || row.amendments.some((entry) => !exactRecord(entry, ["schema", "amendmentId", "fields", "actor", "amendedAt"]) || entry.schema !== "decision-amendment/v1" || !/^dam_[0-9a-f]{26}$/u.test(String(entry.amendmentId)) || !stringArray(entry.fields) || !isJsonObject(entry.actor) || !nonEmpty(entry.amendedAt))) || row.contentPins !== undefined && (!Array.isArray(row.contentPins) || row.contentPins.some((entry) => !exactRecord(entry, ["schema", "pinId", "action", "state", "pinnedAt", "evidence", "actor", "digest"]) || entry.schema !== "decision-content-pin/v1" || !/^dcp_[0-9a-f]{26}$/u.test(String(entry.pinId)) || !["accept", "reject", "defer", "supersede", "retire", "amend", "repin"].includes(String(entry.action)) || !statusWord(decisionStateWords, entry.state) || !nonEmpty(entry.pinnedAt) || !nonEmpty(entry.evidence) || !isJsonObject(entry.actor) || !/^sha256:[0-9a-f]{64}$/u.test(String(entry.digest)))); if (!recordWith(value, DAEMON_DECISION_LIST_SCHEMA.required) || Object.keys(value).some((key) => !DAEMON_DECISION_LIST_SCHEMA.required.includes(key as never)) || value.ok !== true || !warningArray(value.warnings) || !Array.isArray(value.decisions) || value.decisions.some((row) => !recordWith(row, fields) || Object.keys(row).some((key) => !allowed.includes(key)) || history(row) || row.schema !== "decision-row/v1" || !integer(row.workspaceRevision) || ["decisionId", "path", "state", "title", "question", "riskTier", "urgency", "vertical", "preset", "decisionClass", "proposedAt"].some((field) => !nonEmpty(row[field])) || row.legacyId !== undefined && !/^E[1-9][0-9]*$/u.test(String(row.legacyId)) || row.readiness !== undefined && !readiness(row.readiness) || !Array.isArray(row.chosen) || !Array.isArray(row.rejected) || !Array.isArray(row.claims) || !Array.isArray(row.judgmentConsents) || !isJsonObject(row.appliesTo) || !isJsonObject(row.proposer) || row.arbiter !== null && !isJsonObject(row.arbiter) || row.body !== null && !isJsonObject(row.body))) return ["daemon decision list is invalid"]; return []; } export function validateDaemonDocumentRead(value: unknown): readonly string[] { if (!recordWith(value, DAEMON_DOCUMENT_READ_SCHEMA.required) || value.ok !== true || (value.status !== "ready" && value.status !== "pending") || !nonEmpty(value.taskId) || !nonEmpty(value.path) || typeof value.body !== "string" || (value.blobSha256 !== null && (typeof value.blobSha256 !== "string" || !/^[0-9a-f]{64}$/u.test(value.blobSha256))) || !integer(value.watermark) || !integer(value.sourceRevision)) return ["daemon document read is invalid"]; return []; }
export function validateDaemonTaskDocumentList(value: unknown): readonly string[] { if (!recordWith(value, DAEMON_TASK_DOCUMENT_LIST_SCHEMA.required) || value.ok !== true || (value.status !== "ready" && value.status !== "pending") || !nonEmpty(value.taskId) || !Array.isArray(value.documents) || value.documents.some((row) => !exactRecord(row, ["path", "blobSha256", "size", "mediaType"]) || !nonEmpty(row.path) || row.path.startsWith("/") || row.path.split("/").includes("..") || !/^[0-9a-f]{64}$/u.test(String(row.blobSha256)) || !integer(row.size) || !nonEmpty(row.mediaType)) || !integer(value.watermark) || !integer(value.sourceRevision)) return ["daemon task document list is invalid"]; return []; }
export function validateDaemonProtocolError(value: unknown): readonly string[] { if (!recordWith(value, DAEMON_PROTOCOL_ERROR_SCHEMA.required) || value.schema !== "command-receipt/v2" || value.ok !== false || value.outcome !== "op_rejected" || value.opId !== "N/A" || value.origin !== "daemon" || !recordWith(value.error, ["code", "hint"]) || [value.command, value.code, value.evidence, value.nextAction, value.error.code, value.error.hint].some((item) => !nonEmpty(item)) || value.code !== value.error.code) return ["daemon protocol error is invalid"]; return []; }
const writeReceiptFields = ["outcome", "opId", "revision", "code", "origin", "nextAction", "evidence", "visibility", "proof", "detail"], guiReceiptExtensions = ["error", "summary", "taskId", "status", "packagePath", "generatedPaths", "presetDigest", "scaffoldDigest", "completionGates", "commitSha", "dryRun", "executionId", "progressPath", "eventId", "reviewId", "reviewDigest", "contentDigest", "transition", "gateChecks", "next", "changedPaths", "steps", "stoppedAt", "path", "documentSha256", "worktreeVisible", "consentId", "factId", "settlement", "receiptId", "runtimeSessionId", "dispatchId"];
function writeReceipt(value: JsonObject): string[] { const outcome = value.outcome, proof = isJsonObject(value.proof) ? value.proof : {}, applied = outcome === "applied", pending = outcome === "pending", failed = outcome === "op_rejected" || outcome === "indeterminate", validProof = exactRecord(proof, ["committedRevision", "appliedCut", "durable", "canonicalVisible", "worktreeVisible"]) && integer(proof.committedRevision) && integer(proof.appliedCut) && typeof proof.durable === "boolean" && typeof proof.canonicalVisible === "boolean" && (proof.worktreeVisible === null || typeof proof.worktreeVisible === "boolean"); return !statusWord(receiptOutcomeWords, outcome) || !nonEmpty(value.opId) || value.revision !== undefined && (!integer(value.revision) || Number(value.revision) < 0) || (applied || pending) && (!validProof || value.visibility !== "center" || !integer(value.revision) || !nonEmpty(value.evidence)) || applied && (!proof.durable || !proof.canonicalVisible || proof.committedRevision !== proof.appliedCut) || pending && !nonEmpty(value.nextAction) || failed && (![value.code, value.origin, value.nextAction].every(nonEmpty) || value.evidence !== undefined && !nonEmpty(value.evidence)) ? ["write receipt is invalid"] : []; }
export function validateDaemonGuiCommandReceipt(value: unknown): readonly string[] { if (!isJsonObject(value)) return ["GUI command receipt must be an object"]; const allowed = ["schema", "ok", "command", ...writeReceiptFields, ...guiReceiptExtensions], receipt = Object.fromEntries(writeReceiptFields.filter((field) => Object.hasOwn(value, field)).map((field) => [field, value[field]])) as JsonObject, ok = value.outcome === "applied" || value.outcome === "pending", errors = Object.keys(value).some((field) => !allowed.includes(field)) || value.schema !== "command-receipt/v2" || typeof value.ok !== "boolean" || value.ok !== ok || !nonEmpty(value.command) || !daemonGuiActionMethods.some(({ actionKind }) => actionKind === value.command) ? ["GUI command receipt envelope is invalid"] : writeReceipt(receipt); if (value.ok === false && (!exactRecord(value.error, ["code", "hint"]) || !nonEmpty(value.error.code) || !nonEmpty(value.error.hint) || value.error.code !== value.code) || value.ok === true && value.error !== undefined) errors.push("GUI command receipt error is invalid"); const decisionFields = ["path", "commitSha", "documentSha256", "worktreeVisible", "consentId"], decision = ["decision-propose", "decision-accept", "decision-reject", "decision-defer"].includes(String(value.command)) && value.outcome === "applied" || Object.hasOwn(value, "documentSha256") || Object.hasOwn(value, "consentId"); if (decision && (!decisionFields.every((field) => Object.hasOwn(value, field)) || !nonEmpty(value.path) || !/^[0-9a-f]{40}$/u.test(String(value.commitSha)) || !/^sha256:[0-9a-f]{64}$/u.test(String(value.documentSha256)) && !/^[0-9a-f]{64}$/u.test(String(value.documentSha256)) || value.worktreeVisible !== true || value.consentId !== null && !nonEmpty(value.consentId))) errors.push("decision receipt fidelity is invalid"); return errors; }
type ResultValidator = (value: unknown) => readonly string[];
function serializeSchema(value: unknown, validate: ResultValidator): string { const errors = validate(value); if (errors.length) throw new DaemonProtocolContractError("invalid_result", errors.join("; ")); return `${JSON.stringify(value)}\n`; }
export const serializeDaemonTaskSnapshotList = (value: unknown): string => serializeSchema(value, validateDaemonTaskSnapshotList), serializeDaemonRelationGraph = (value: unknown): string => serializeSchema(value, validateDaemonRelationGraph), serializeDaemonDecisionList = (value: unknown): string => serializeSchema(value, validateDaemonDecisionList), serializeDaemonDocumentRead = (value: unknown): string => serializeSchema(value, validateDaemonDocumentRead), serializeDaemonTaskDocumentList = (value: unknown): string => serializeSchema(value, validateDaemonTaskDocumentList), serializeDaemonProtocolError = (value: unknown): string => serializeSchema(value, validateDaemonProtocolError);
export function daemonProtocolError(command: string, code: string, hint: string): DaemonProtocolErrorResult { return { schema: "command-receipt/v2", ok: false, command, outcome: "op_rejected", opId: "N/A", origin: "daemon", code, evidence: `rejection:${code}`, error: { code, hint }, nextAction: hint }; }
export const daemonProtocolMethods = Object.freeze([
  { id: "protocol.hello", phase: "W3", method: "protocol.hello", requiresRepo: false, params: shape({ protocolVersion: "number" }) },
  { id: "daemon.status", phase: "W3", method: "daemon.status", requiresRepo: false, params: shape({}) },
  { id: "daemon.repo.bootstrap", phase: "W3", method: "daemon.repo.bootstrap", requiresRepo: false, params: shape({ rootDir: "string", repoId: "string", personId: "string", displayName: "string", name: "string?", addNpmScripts: "boolean?", configureOnly: "boolean?" }) },
  { id: "daemon.repo.register", phase: "W3", method: "daemon.repo.register", requiresRepo: false, params: shape({ rootDir: "string", repoId: "string", mode: optionalEnum(daemonRepoModeWords) }) },
  { id: "daemon.repo.unregister", phase: "W3", method: "daemon.repo.unregister", requiresRepo: false, params: shape({ repoId: "string" }) },
  { id: "repo.task.run", phase: "W3", method: "repo.task.run", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ action: shape({ kind: "string" }, true) }) }) }
] as const);
const runtimeInstanceMethods = Object.freeze([{ id: "daemon.runtimeInstance.create", phase: "Runtime-Instances-S1", method: "daemon.runtimeInstance.create", requiresRepo: false, params: shape({ payload: shape({ instanceId: "string", name: "string", kindId: "string", installationId: "string", providerId: "string", model: "string", reasoningEffort: "string?", baseUrl: "string?", authMode: "string", credentialRef: "string?" }) }) },
  { id: "daemon.runtimeInstance.list", phase: "Runtime-Instances-S1", method: "daemon.runtimeInstance.list", requiresRepo: false, params: shape({ payload: shape({}) }) },
  { id: "daemon.runtimeInstance.show", phase: "Runtime-Instances-S1", method: "daemon.runtimeInstance.show", requiresRepo: false, params: shape({ payload: shape({ instanceId: "string" }) }) },
  { id: "daemon.runtimeInstance.delete", phase: "Runtime-Instances-S1", method: "daemon.runtimeInstance.delete", requiresRepo: false, params: shape({ payload: shape({ instanceId: "string" }) }) },
  { id: "daemon.runtimeInstance.status", phase: "Runtime-Instances-S3", method: "daemon.runtimeInstance.status", requiresRepo: false, params: shape({ payload: shape({ instanceId: "string" }) }) }] as const);
const runtimeInstanceAuthMethods = Object.freeze(["login", "reauth", "logout"].map((action) => ({ id: `repo.runtimeInstance.auth.${action}`, phase: "Runtime-Instances-S3", method: `repo.runtimeInstance.auth.${action}`, requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ instanceId: "string", idempotencyKey: "string" }) }) }))); const fleetProtocolMethods = Object.freeze([{ id: "daemon.fleet.center.start", phase: "Fleet-Wiring", method: "daemon.fleet.center.start", requiresRepo: false, params: shape({ payload: shape({ port: "number", keyPath: "string", certPath: "string", rosterPath: "string", quotaBytes: "number", bind: "string?", stateRoot: "string?" }) }) }, { id: "daemon.fleet.edge.sync", phase: "Fleet-Wiring", method: "daemon.fleet.edge.sync", requiresRepo: false, params: shape({ payload: shape({ host: "string", port: "number", caPath: "string", nodeId: "string", credential: "string", assignmentId: "string", viewRoot: "string", quotaBytes: "number", servername: "string?", timeoutMs: "number" }) }) }, { id: "daemon.fleet.task.run", phase: "Fleet-Wiring", method: "daemon.fleet.task.run", requiresRepo: false, params: shape({ payload: shape({ host: "string", port: "number", caPath: "string", nodeId: "string", credential: "string?", rosterPath: "string?", servername: "string?", assignmentId: "string", repoId: "string", viewRoot: "string", quotaBytes: "number", waitTimeoutMs: "number?", action: "json" }) }) }] as const);
const allDaemonProtocolMethods = Object.freeze([...daemonProtocolMethods, ...runtimeInstanceMethods, ...runtimeInstanceAuthMethods, ...fleetProtocolMethods, ...presetMethods, ...daemonGuiReadMethods, ...daemonGuiActionMethods, ...daemonGuiStreamFacets]);
export const jsonRpcMethodContracts = Object.freeze(allDaemonProtocolMethods.map(({ method, requiresRepo }) => ({ method, requiresRepo })));
export function isDaemonGuiReadMethod(method: string): method is DaemonGuiReadMethod { return daemonGuiReadMethods.some((entry) => entry.method === method); }
export function isDaemonGuiActionMethod(method: string): method is DaemonGuiActionMethod { return daemonGuiActionMethods.some((entry) => entry.method === method); }
export function isDaemonGuiStreamMethod(method: string): method is DaemonGuiStreamMethod { return daemonGuiStreamFacets.some((entry) => entry.method === method); }
export function validateDaemonRpcCall(value: unknown): readonly string[] { if (!isJsonObject(value) || typeof value.method !== "string") return ["RPC method is required"]; const method = allDaemonProtocolMethods.find((entry) => entry.method === value.method); if (!method) return ["RPC method is not contracted"]; const errors = validateShape(value.params === undefined ? {} : value.params, method.params, "params"); if (!errors.length && isDaemonGuiActionMethod(value.method)) errors.push(...validateGuiActionPayload(value.method, (value.params as JsonObject).payload)); if (!errors.length && value.method === "repo.terminal.attach") { const afterSeq = ((value.params as JsonObject).payload as JsonObject).afterSeq; if (!integer(afterSeq) || Number(afterSeq) < 0) errors.push("terminal attach sequence is invalid"); } return errors; }
export function parseDaemonRpcParams(method: string, params: unknown): { readonly ok: true; readonly params: JsonObject } | { readonly ok: false; readonly errors: readonly string[] } { const candidateParams = params === undefined ? {} : params, errors = validateDaemonRpcCall({ method, params: candidateParams });
  return errors.length ? { ok: false, errors } : { ok: true, params: candidateParams as JsonObject }; }
function validateShape(value: unknown, expected: RpcShape, prefix: string): string[] { if (!isJsonObject(value)) return [`${prefix} must be an object`]; const errors: string[] = []; if (!expected.open) for (const field of Object.keys(value)) if (!Object.hasOwn(expected.fields, field)) errors.push(`${prefix}.${field} is not allowed`);
  for (const [field, rule] of Object.entries(expected.fields)) { const item = value[field], enumRule = "values" in Object(rule) ? rule as RpcEnumRule : null; if ((rule === "string?" || rule === "string-null?" || rule === "json?" || rule === "array?" || rule === "boolean?" || rule === "number?" || enumRule?.optional) && item === undefined || rule === "string-null?" && item === null) continue; if (enumRule) { if (!enumRule.values.includes(String(item))) errors.push(`${prefix}.${field} must be one of ${enumRule.values.join(", ")}`); } else if (rule === "json" || rule === "json?") { if (!isJsonObject(item)) errors.push(`${prefix}.${field} must be object`); } else if (rule === "array" || rule === "array?") { if (!Array.isArray(item)) errors.push(`${prefix}.${field} must be array`); } else if (rule === "string" || rule === "string?" || rule === "string-null?" || rule === "number" || rule === "number?" || rule === "boolean?") { const type = rule.startsWith("string") ? "string" : rule === "boolean?" ? "boolean" : "number"; if (typeof item !== type || (type === "string" && !item)) errors.push(`${prefix}.${field} must be ${type}`); }
    else errors.push(...validateShape(item, rule as RpcShape, `${prefix}.${field}`)); } return errors; }
function validateGuiActionPayload(method: DaemonGuiActionMethod, value: unknown): string[] {
  if (!isJsonObject(value)) return ["GUI action payload must be an object"];
  const errors: string[] = [...rejectSecretKeys(value)], exactItem = (item: unknown, required: readonly string[], optional: readonly string[] = []) => isJsonObject(item) && required.every((field) => nonEmpty(item[field])) && Object.keys(item).every((field) => required.includes(field) || optional.includes(field));
  if (method === "repo.task.submit") errors.push(...validateGuiSubmission(value.submission));
  if (method === "repo.task.progress.append" && value.evidence !== undefined && (!Array.isArray(value.evidence) || value.evidence.some((item) => !exactItem(item, ["type", "path", "summary"])))) errors.push("progress evidence is invalid");
  if (method === "repo.decision.list") { const range = value.legacyRange; if (range !== undefined && (!exactRecord(range, ["start", "end"]) || !integer(range.start) || !integer(range.end) || Number(range.start) < 1 || Number(range.end) < Number(range.start)) || value.legacyId !== undefined && !/^E[1-9][0-9]*$/u.test(String(value.legacyId)) || value.state !== undefined && !statusWord(decisionStateWords, value.state)) errors.push("decision list filters are invalid"); }
  if (method === "repo.decision.propose") { const scopes = value.appliesTo, chosen = value.chosen, rejected = value.rejected, claims = value.claims, fulfillments = value.fulfillments, relations = value.relations; if (![value.riskTier, value.urgency].every((field) => ["low", "medium", "high"].includes(String(field))) || !["ordinary", "standing_policy"].includes(String(value.decisionClass)) || !exactRecord(scopes, ["modules", "productLines"]) || !stringArray(scopes.modules) || !stringArray(scopes.productLines) || !Array.isArray(chosen) || !chosen.length || chosen.some((item) => !exactItem(item, ["id", "text"], ["rationale"]) || isJsonObject(item) && item.rationale !== undefined && !nonEmpty(item.rationale)) || !Array.isArray(rejected) || !rejected.length || rejected.some((item) => !exactItem(item, ["id", "text", "whyNot"])) || !Array.isArray(claims) || claims.some((item) => !isJsonObject(item) || !exactRecord(item, ["id", "text", "loadBearing"]) || !nonEmpty(item.id) || !nonEmpty(item.text) || typeof item.loadBearing !== "boolean") || !Array.isArray(fulfillments) || fulfillments.some((item) => !exactItem(item, ["claimId", "mode"]) || isJsonObject(item) && !["evidenced", "delivered", "standing_policy"].includes(String(item.mode))) || !Array.isArray(relations) || relations.some((item) => !exactItem(item, ["anchor", "type", "target", "rationale"]))) errors.push("decision proposal is invalid"); }
  if (["repo.decision.accept", "repo.decision.reject", "repo.decision.defer"].includes(method)) for (const field of [value.rationale, value.reason, value.judgmentOnlyRationale]) if (field !== undefined && (typeof field !== "string" || [...field].length > 199)) errors.push("decision rationale is invalid");
  if (method === "daemon.gui.control.request" && (!["refresh", "restart"].includes(String(value.kind)) || value.reason !== undefined && (typeof value.reason !== "string" || [...value.reason].length > 199))) errors.push("daemon control request is invalid");
  if (method === "repo.agentRuntime.spawn" && (!Object.hasOwn(value, "taskId") || !nonEmpty(value.runtimeInstanceId) || !exactCwd(value.cwd) || value.taskId !== null && !nonEmpty(value.taskId) || value.providerSessionId !== undefined && !nonEmpty(value.providerSessionId))) errors.push("runtime spawn request is invalid");
  if (method === "repo.agentRuntime.cancel" && !nonEmpty(value.runtimeSessionId)) errors.push("runtime cancel request is invalid");
  if (method === "repo.gui.catalog.reread" && value.expectedDigest !== undefined && !digest(value.expectedDigest)) errors.push("catalog reread digest is invalid");
  if (method === "repo.terminal.spawn" && !exactCwd(value.cwd)) errors.push("terminal spawn cwd is invalid");
  if (method === "repo.terminal.input" && (!integer(value.clientSeq) || Number(value.clientSeq) < 0)) errors.push("terminal input sequence is invalid");
  if (method === "repo.terminal.resize" && (![value.cols, value.rows].every(integer) || Number(value.cols) < 2 || Number(value.cols) > 500 || Number(value.rows) < 2 || Number(value.rows) > 500)) errors.push("terminal dimensions are invalid");
  if (method === "repo.terminal.terminate" && value.confirmed !== true) errors.push("terminal termination requires confirmation");
  return errors;
}
function exactCwd(value: unknown): boolean { return isJsonObject(value) && (value.scope === "repo-root" && exactRecord(value, ["scope"]) || value.scope === "repo-relative" && exactRecord(value, ["scope", "path"]) && nonEmpty(value.path)); }
export const DAEMON_RPC_SCHEMA = Object.freeze({ id: "w3-daemon-rpc/v1", methods: allDaemonProtocolMethods.map(({ method, params }) => ({ method, params })) });
export function serializeDaemonRpcCall(value: unknown): string { const errors = validateDaemonRpcCall(value); if (errors.length) throw new DaemonProtocolContractError("invalid_rpc", errors.join("; ")); return `${JSON.stringify(value)}\n`; }
export class DaemonProtocolContractError extends Error { readonly code: string; constructor(code: string, message: string) { super(message); this.name = "DaemonProtocolContractError"; this.code = code; } }
export const daemonGuiReadSchemas = Object.freeze([{ id: GUI_SYSTEM_STATUS_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#GUI_SYSTEM_STATUS_SCHEMA", parser: "packages/daemon/src/gui-s3-control.ts#validateSystemStatus", writer: "packages/daemon/src/gui-s3-control.ts#writeSystemStatus", error: "packages/daemon/src/gui-s3-control.ts#GuiS3ContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/gui-system-status-invalid.json"]) }, { id: DAEMON_CONTROL_RECEIPT_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_CONTROL_RECEIPT_SCHEMA", parser: "packages/daemon/src/gui-s3-control.ts#validateDaemonControlReceipt", writer: "packages/daemon/src/gui-s3-control.ts#writeDaemonControlReceipt", error: "packages/daemon/src/gui-s3-control.ts#GuiS3ContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-control-receipt-invalid.json"]) }, { id: GUI_CATALOG_SNAPSHOT_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#GUI_CATALOG_SNAPSHOT_SCHEMA", parser: "packages/daemon/src/gui-s3-control.ts#validateCatalogSnapshot", writer: "packages/daemon/src/gui-s3-control.ts#writeCatalogSnapshot", error: "packages/daemon/src/gui-s3-control.ts#GuiS3ContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/gui-catalog-snapshot-invalid.json"]) }, { id: GUI_CATALOG_PRESET_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#GUI_CATALOG_PRESET_SCHEMA", parser: "packages/daemon/src/gui-s3-control.ts#validateCatalogPreset", writer: "packages/daemon/src/gui-s3-control.ts#writeCatalogPreset", error: "packages/daemon/src/gui-s3-control.ts#GuiS3ContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/gui-catalog-preset-invalid.json"]) }, { id: TERMINAL_SESSION_LIST_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#TERMINAL_SESSION_LIST_SCHEMA", parser: "packages/daemon/src/gui-s3-control.ts#validateTerminalSessionList", writer: "packages/daemon/src/gui-s3-control.ts#writeTerminalSessionList", error: "packages/daemon/src/gui-s3-control.ts#GuiS3ContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/terminal-session-list-invalid.json"]) }, { id: DAEMON_TASK_SNAPSHOT_LIST_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_TASK_SNAPSHOT_LIST_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonTaskSnapshotList", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonTaskSnapshotList", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-task-snapshot-list-invalid.json"]) },
  { id: DAEMON_RELATION_GRAPH_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_RELATION_GRAPH_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonRelationGraph", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonRelationGraph", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-relation-graph-invalid.json"]) },
  { id: DAEMON_DECISION_LIST_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_DECISION_LIST_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonDecisionList", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonDecisionList", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-decision-list-invalid.json"]) },
  { id: DAEMON_DOCUMENT_READ_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_DOCUMENT_READ_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonDocumentRead", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonDocumentRead", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-document-read-invalid.json"]) }, { id: DAEMON_TASK_DOCUMENT_LIST_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_TASK_DOCUMENT_LIST_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonTaskDocumentList", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonTaskDocumentList", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-task-document-list-invalid.json"]) }, { id: DAEMON_PROTOCOL_ERROR_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_PROTOCOL_ERROR_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonProtocolError", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonProtocolError", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-protocol-error-invalid.json"]) },
  { id: DAEMON_AGENT_RUNTIME_OVERVIEW_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_OVERVIEW_SCHEMA", parser: "packages/daemon/src/agent-runtime-contract.ts#validateAgentRuntimeOverview", writer: "packages/daemon/src/agent-runtime-contract.ts#serializeAgentRuntimeOverview", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-overview-invalid.json", "packages/daemon/fixtures/contracts/daemon-agent-runtime-overview-profiles-invalid.json"]) }, { id: DAEMON_AGENT_RUNTIME_SESSION_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_SESSION_SCHEMA", parser: "packages/daemon/src/agent-runtime-contract.ts#validateAgentRuntimeSession", writer: "packages/daemon/src/agent-runtime-contract.ts#serializeAgentRuntimeSession", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-session-invalid.json"]) },
  { id: DAEMON_AGENT_RUNTIME_EVENTS_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_EVENTS_SCHEMA", parser: "packages/daemon/src/agent-runtime-contract.ts#validateAgentRuntimeEvents", writer: "packages/daemon/src/agent-runtime-contract.ts#serializeAgentRuntimeEvents", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-events-invalid.json"]) }, { id: DAEMON_AGENT_RUNTIME_ATTACH_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_ATTACH_SCHEMA", parser: "packages/daemon/src/agent-runtime-stream.ts#validateAgentRuntimeAttach", writer: "packages/daemon/src/agent-runtime-stream.ts#serializeAgentRuntimeAttach", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-attach-invalid.json"]) }, { id: DAEMON_AGENT_RUNTIME_ATTACH_EVENT_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_ATTACH_EVENT_SCHEMA", parser: "packages/daemon/src/agent-runtime-stream.ts#validateAgentRuntimeAttachEvent", writer: "packages/daemon/src/agent-runtime-stream.ts#serializeAgentRuntimeAttachEvent", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-attach-event-invalid.json"]) }]);
const s3Schema = (id: string, schema: string, parser: string, writer: string, fixture: string) => ({ id, schema: `packages/daemon/src/protocol/daemon-protocol.contract.ts#${schema}`, parser: `packages/daemon/src/gui-s3-control.ts#${parser}`, writer: `packages/daemon/src/gui-s3-control.ts#${writer}`, error: "packages/daemon/src/gui-s3-control.ts#GuiS3ContractError", negativeFixtures: Object.freeze([`packages/daemon/fixtures/contracts/${fixture}`]) });
export const daemonGuiActionSchemas = Object.freeze([{ id: DAEMON_GUI_COMMAND_RECEIPT_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_GUI_COMMAND_RECEIPT_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonGuiCommandReceipt", writer: "packages/daemon/src/protocol/json-rpc-server.ts#createJsonRpcProtocolServer", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-gui-command-receipt-invalid.json"]) },
  s3Schema(CATALOG_REREAD_RECEIPT_SCHEMA.id, "CATALOG_REREAD_RECEIPT_SCHEMA", "validateCatalogRereadReceipt", "writeCatalogRereadReceipt", "catalog-reread-receipt-v1-invalid.json"), s3Schema(TERMINAL_CONTROL_RECEIPT_SCHEMA.id, "TERMINAL_CONTROL_RECEIPT_SCHEMA", "validateTerminalControlReceipt", "writeTerminalControlReceipt", "terminal-control-receipt-v1-invalid.json"), s3Schema(TERMINAL_INPUT_ACK_SCHEMA.id, "TERMINAL_INPUT_ACK_SCHEMA", "validateTerminalInputAck", "writeTerminalInputAck", "terminal-input-ack-v1-invalid.json"), s3Schema(TERMINAL_DETACH_ACK_SCHEMA.id, "TERMINAL_DETACH_ACK_SCHEMA", "validateTerminalDetachAck", "writeTerminalDetachAck", "terminal-detach-ack-v1-invalid.json"), s3Schema(TERMINAL_ATTACH_SCHEMA.id, "TERMINAL_ATTACH_SCHEMA", "validateTerminalAttach", "writeTerminalAttach", "terminal-attach-v1-invalid.json"), s3Schema(TERMINAL_ATTACH_EVENT_SCHEMA.id, "TERMINAL_ATTACH_EVENT_SCHEMA", "validateTerminalAttachEvent", "writeTerminalAttachEvent", "terminal-attach-event-v1-invalid.json")]);
export default Object.freeze({ id: "w3-daemon-protocol", phases: Object.freeze(["W3", "W2-GUI", "DocSync-B", "Runtime-B", "DecisionFact-A", "DecisionFact-B", "Migration-A", "W5-GUI-S2", "W5-GUI-S3", "Runtime-Instances-S1", "Runtime-Instances-S3", "Fleet-Wiring"]), commands: effectiveDaemonOwnedProtocolCommands, methods: Object.freeze([...daemonProtocolMethods, ...runtimeInstanceMethods, ...runtimeInstanceAuthMethods, ...fleetProtocolMethods, ...daemonGuiReadMethods, ...daemonGuiActionMethods, ...daemonGuiStreamFacets]), gates: Object.freeze([]), guards: Object.freeze([]), schemas: Object.freeze([{ id: DAEMON_RPC_SCHEMA.id,
    schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_RPC_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonRpcCall", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonRpcCall", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError",
    negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/w3-daemon-rpc-invalid.json"]) }, ...daemonGuiReadSchemas, ...daemonGuiActionSchemas]) });
