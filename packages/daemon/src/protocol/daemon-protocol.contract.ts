import { existsSync, realpathSync } from "node:fs";
import path from "node:path";
import { isJsonObject, type JsonObject } from "./json-rpc-types.ts";
import type { AgentRuntimeEventsResult, AgentRuntimeOverviewResult, AgentRuntimeSessionResult } from "../agent-runtime-contract.ts";
import type { AgentRuntimeAttachResult } from "../agent-runtime-stream.ts";
import { cliInput, defineCliCommand, presetCommands, presetMethods } from "../../../preset/src/preset-command-contract.ts";
declare const safePathBrand: unique symbol, canonicalRootBrand: unique symbol, endpointIdentityBrand: unique symbol, workspaceIdBrand: unique symbol;
export type SafePath = string & { readonly [safePathBrand]: true }; export type CanonicalRoot = SafePath & { readonly [canonicalRootBrand]: true };
export type EndpointIdentity = string & { readonly [endpointIdentityBrand]: true }; export type WorkspaceId = string & { readonly [workspaceIdBrand]: true };
export function safePath(value: string): SafePath { return path.resolve(value) as SafePath; }
export function canonicalRoot(value: string, allowMissing = false): CanonicalRoot { const resolved = path.resolve(value); if (existsSync(resolved)) return realpathSync.native(resolved) as CanonicalRoot;
  const parent = path.dirname(resolved); if (!allowMissing || !existsSync(parent)) throw new DaemonProtocolContractError("invalid_root", `Canonical root does not exist: ${resolved}`);
  return path.join(realpathSync.native(parent), path.basename(resolved)) as CanonicalRoot; }
export function endpointIdentity(value: string): EndpointIdentity { if (!value.trim()) throw new DaemonProtocolContractError("invalid_endpoint", "Endpoint identity is required."); return value as EndpointIdentity; }
export function workspaceId(value: string): WorkspaceId { if (!/^[a-z][a-z0-9-]{0,62}$/u.test(value)) throw new DaemonProtocolContractError("invalid_workspace", "Workspace id is invalid."); return value as WorkspaceId; }
const daemonOwnedProtocolCommands = Object.freeze([
  defineCliCommand({ id: "repo-bootstrap", phase: "W3", path: ["init"], summary: "Initialize and register a workspace through the explicit daemon.", method: "daemon.repo.bootstrap", commandClass: "admin", inputs: [cliInput("--repo-id", "single", true, { code: "missing_field", nextAction: "Init requires repo-id, person-id, and display-name." }), cliInput("--person-id", "single", true, { code: "missing_field", nextAction: "Init requires repo-id, person-id, and display-name." }), cliInput("--display-name", "single", true, { code: "missing_field", nextAction: "Init requires repo-id, person-id, and display-name." }), cliInput("--name", "single", false, { code: "invalid_field", nextAction: "--name must be one non-empty line." }, { regex: "^[^\\r\\n]+$" })] }), defineCliCommand({ id: "task-start", phase: "W3", path: ["task", "start", "<task-id>"], summary: "Acquire the task execution lease.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "missing_field", nextAction: "Add --execution-id <execution-id>." })] }), defineCliCommand({ id: "task-progress-append", phase: "W3", path: ["task", "progress", "append", "<task-id>"], summary: "Append typed progress through the active task lease.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--text", "single", true, { code: "missing_field", nextAction: "Add --text <progress-text>." }), cliInput("--evidence", "repeated", false, { code: "invalid_field", nextAction: "Use --evidence <type>:<path>:<summary> with a canonical relative path." }, { regex: "^[a-z][a-z0-9_-]{0,31}:[^:]+:.+$" })] }), defineCliCommand({ id: "task-artifact-add", phase: "W3", path: ["task", "artifact", "add", "<task-id>"], summary: "Publish an untracked UTF-8 artifact through canonical doc sync.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--source", "single", true, { code: "missing_field", nextAction: "Add --source <untracked-file>." }), cliInput("--destination", "single", true, { code: "missing_field", nextAction: "Add --destination <artifact-path>." })] }), defineCliCommand({ id: "task-submit", phase: "W3", path: ["task", "submit", "<task-id>"], summary: "Atomically finalize an Execution and enter review.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "invalid_field", nextAction: "Submit requires --execution-id and --from-file." }), cliInput("--from-file", "single", true, { code: "invalid_field", nextAction: "Submit requires a complete submission JSON packet." })] }),
  defineCliCommand({ id: "task-review-execution", phase: "W3", path: ["task", "review-execution", "<task-id>"], summary: "Record the canonical Execution Review.", method: "repo.task.run", commandClass: "arbiter", inputs: [cliInput("--execution-id", "single", true, { code: "invalid_field", nextAction: "Review requires execution-id, review-id, and from-file." }), cliInput("--review-id", "single", true, { code: "invalid_field", nextAction: "Review requires a review id." }), cliInput("--from-file", "single", true, { code: "invalid_field", nextAction: "Review requires a complete review JSON packet." })] }), defineCliCommand({ id: "task-review-consent", phase: "W3", path: ["task", "review-consent", "<task-id>"], summary: "Record content-pinned owner consent.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "invalid_field", nextAction: "Consent requires execution-id, review-id, consent-id, and from-file." }), cliInput("--review-id", "single", true, { code: "invalid_field", nextAction: "Consent requires a review id." }), cliInput("--consent-id", "single", true, { code: "invalid_field", nextAction: "Consent requires a consent id." }), cliInput("--from-file", "single", true, { code: "invalid_field", nextAction: "Consent requires reviewDigest and contentDigest JSON." })] }), defineCliCommand({ id: "task-code-doc-reconcile", phase: "W3", path: ["task", "code-doc", "reconcile", "<task-id>"], summary: "Publish a typed code-doc witness.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "invalid_field", nextAction: "Reconcile requires execution-id, commit-sha, iteration, and path." }), cliInput("--commit-sha", "single", true, { code: "invalid_field", nextAction: "Reconcile requires a full commit sha." }, { regex: "^[0-9a-f]{40}$" }), cliInput("--iteration", "single", true, { code: "invalid_field", nextAction: "Iteration must be 0 or 1." }, { enum: ["0", "1"] }), cliInput("--path", "repeated", true, { code: "invalid_field", nextAction: "Reconcile requires at least one canonical path." })] }), defineCliCommand({ id: "task-complete", phase: "W3", path: ["task", "complete", "<task-id>"], summary: "Complete a reviewed and consented task after canonical gate checks.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", true, { code: "missing_field", nextAction: "Add --execution-id <execution-id>." }), cliInput("--ci", "single", false, { code: "invalid_field", nextAction: "Use --ci passed only for a successful canonical checker result." }, { enum: ["passed"] }), cliInput("--commit-sha", "single", false, { code: "invalid_field", nextAction: "Provide commit-sha, iteration, and path together for reconcile." }, { regex: "^[0-9a-f]{40}$" }), cliInput("--iteration", "single", false, { code: "invalid_field", nextAction: "Provide commit-sha, iteration, and path together for reconcile." }, { enum: ["0", "1"] }), cliInput("--path", "repeated", false, { code: "invalid_field", nextAction: "Provide commit-sha, iteration, and path together for reconcile." })] }), defineCliCommand({ id: "task-show", phase: "W3", path: ["task", "show", "<task-id>"], summary: "Read the task projection.", method: "repo.task.run", commandClass: "repo-read", inputs: [] }), defineCliCommand({ id: "receipt-show", phase: "W3", path: ["receipt", "show", "<op-id>"], summary: "Read a write receipt.", method: "repo.task.run", commandClass: "repo-read", inputs: [] }),
  defineCliCommand({ id: "doc-status", phase: "DocSync-B", path: ["doc", "status"], summary: "Scan the authored root for document candidates.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--path", "repeated", false, { code: "invalid_field", nextAction: "--path requires an authored-relative path." })] }), defineCliCommand({ id: "doc-sync-dry-run", actionKind: "doc-dry-run", phase: "DocSync-B", path: ["doc", "sync", "--dry-run"], summary: "Preview the scanner selection without writing.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--path", "repeated", false, { code: "invalid_field", nextAction: "--path requires an authored-relative path." })] }), defineCliCommand({ id: "doc-sync-submit", actionKind: "doc-submit", phase: "DocSync-B", path: ["doc", "sync", "--submit"], summary: "Submit eligible scanner candidates through the resident daemon.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--execution-id", "single", false, { code: "invalid_field", nextAction: "--execution-id requires a non-empty value." }), cliInput("--path", "repeated", false, { code: "invalid_field", nextAction: "--path requires an authored-relative path." })] }), defineCliCommand({ id: "doc-materialize", phase: "DocSync-B", path: ["doc", "materialize"], summary: "Restore the current canonical document cut to the worktree.", method: "repo.task.run", commandClass: "repo-write", inputs: [] }), defineCliCommand({ id: "doc-show", phase: "DocSync-B", path: ["doc", "show"], summary: "Show a canonical projected document.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--path", "single", true, { code: "missing_field", nextAction: "Add --path <authored-relative-path>." })] }),
  defineCliCommand({ id: "fact-record", phase: "DecisionFact-A", path: ["fact", "record"], summary: "Record an immutable Fact event.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--task", "single", true, { code: "missing_field", nextAction: "Fact record requires --task, --statement, and --source." }), cliInput("--statement", "single", true, { code: "missing_field", nextAction: "Fact record requires --task, --statement, and --source." }), cliInput("--source", "single", true, { code: "missing_field", nextAction: "Fact record requires --task, --statement, and --source." }), cliInput("--observed-at", "single", false, { code: "invalid_field", nextAction: "Use an ISO-8601 UTC timestamp for --observed-at." }, { regex: "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(?:\\.[0-9]+)?Z$" }), cliInput("--confidence", "single", false, { code: "invalid_field", nextAction: "Confidence must be low, medium, or high." }, { enum: ["low", "medium", "high"] }), cliInput("--memory-class", "single", false, { code: "invalid_field", nextAction: "Memory class must be semantic, episodic, or procedural." }, { enum: ["semantic", "episodic", "procedural"] }), cliInput("--memory-tag", "repeated", false, { code: "invalid_field", nextAction: "--memory-tag requires a non-empty value." }), cliInput("--supersedes", "single", false, { code: "invalid_field", nextAction: "Pair --supersedes with a rationale of at most 199 characters." }), cliInput("--rationale", "single", false, { code: "invalid_field", nextAction: "Pair --supersedes with a rationale of at most 199 characters." }, { regex: "^[\\s\\S]{1,199}$" })] }),
  defineCliCommand({ id: "fact-search", phase: "DecisionFact-A", path: ["fact", "search", "[query]"], summary: "Search the Fact FTS projection.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--task", "single", false, { code: "invalid_field", nextAction: "--task requires a task id." }), cliInput("--confidence", "single", false, { code: "invalid_field", nextAction: "Fact search confidence must be low, medium, or high." }, { enum: ["low", "medium", "high"] }), cliInput("--memory-class", "single", false, { code: "invalid_field", nextAction: "Fact search memory class must be semantic, episodic, or procedural." }, { enum: ["semantic", "episodic", "procedural"] })] }), defineCliCommand({ id: "fact-show", phase: "DecisionFact-A", path: ["fact", "show"], summary: "Show one projected Fact.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--task", "single", true, { code: "missing_field", nextAction: "Fact show requires --task and --id." }), cliInput("--id", "single", true, { code: "invalid_field", nextAction: "Use F- followed by eight Crockford characters." }, { regex: "^F-[0-9A-HJKMNP-TV-Z]{8}$" })] }),
  defineCliCommand({ id: "decision-propose", phase: "DecisionFact-B", path: ["decision", "propose"], summary: "Propose an immutable Decision from one structured packet.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--from-file", "single", false, { code: "invalid_field", nextAction: "Use exactly one of --from-file <packet.json> or --json-input <json>." }), cliInput("--json-input", "single", false, { code: "invalid_field", nextAction: "Use exactly one of --from-file <packet.json> or --json-input <json>." }), cliInput("--body", "single", false, { code: "invalid_field", nextAction: "Use only one of --body or --body-file." }), cliInput("--body-file", "single", false, { code: "invalid_field", nextAction: "Use only one of --body or --body-file." })] }), defineCliCommand({ id: "decision-accept", phase: "DecisionFact-B", path: ["decision", "accept", "<id>"], summary: "Accept a proposed Decision as transport-bound arbiter.", method: "repo.task.run", commandClass: "arbiter", inputs: [cliInput("--rationale", "single", true, { code: "invalid_field", nextAction: "A rationale of 1..199 characters is required." }, { regex: "^[\\s\\S]{1,199}$" }), cliInput("--judgment-only", "single", false, { code: "invalid_field", nextAction: "Without claim evidence, --judgment-only requires an explicit rationale of 1..199 characters." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-reject", phase: "DecisionFact-B", path: ["decision", "reject", "<id>"], summary: "Reject a proposed Decision as transport-bound arbiter.", method: "repo.task.run", commandClass: "arbiter", inputs: [cliInput("--rationale", "single", true, { code: "invalid_field", nextAction: "A rationale of 1..199 characters is required." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-defer", phase: "DecisionFact-B", path: ["decision", "defer", "<id>"], summary: "Defer a proposed Decision as transport-bound arbiter.", method: "repo.task.run", commandClass: "arbiter", inputs: [cliInput("--rationale", "single", true, { code: "invalid_field", nextAction: "A rationale of 1..199 characters is required." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-retire", phase: "DecisionFact-B", path: ["decision", "retire", "<id>"], summary: "Retire an active Decision.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--reason", "single", true, { code: "invalid_field", nextAction: "A reason of 1..199 characters is required." }, { regex: "^[\\s\\S]{1,199}$" })] }),
  defineCliCommand({ id: "decision-claim-add", phase: "DecisionFact-B", path: ["decision", "claim", "add", "<id>"], summary: "Append a Decision claim.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--id", "single", true, { code: "invalid_field", nextAction: "Claim requires a C-prefixed --id." }, { regex: "^C[A-Za-z0-9_-]+$" }), cliInput("--text", "single", true, { code: "invalid_field", nextAction: "Claim requires --text." }), cliInput("--non-load-bearing", "boolean", false, { code: "invalid_field", nextAction: "Use --non-load-bearing once." })] }), defineCliCommand({ id: "decision-claim-fulfill", phase: "DecisionFact-B", path: ["decision", "claim", "fulfill", "<id>"], summary: "Declare a claim fulfillment mode.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--id", "single", true, { code: "invalid_field", nextAction: "Claim requires a C-prefixed --id." }, { regex: "^C[A-Za-z0-9_-]+$" }), cliInput("--mode", "single", true, { code: "invalid_field", nextAction: "Fulfillment requires evidenced, delivered, or standing_policy mode." }, { enum: ["evidenced", "delivered", "standing_policy"] })] }), defineCliCommand({ id: "decision-relate", phase: "DecisionFact-B", path: ["decision", "relate", "<id>"], summary: "Append a Decision-owned relation.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--anchor", "single", true, { code: "invalid_field", nextAction: "Relate requires an existing Decision anchor." }), cliInput("--type", "single", true, { code: "invalid_field", nextAction: "Relate requires type, target, and a rationale of 1..199 characters." }), cliInput("--target", "single", true, { code: "invalid_field", nextAction: "Relate requires type, target, and a rationale of 1..199 characters." }), cliInput("--rationale", "single", true, { code: "invalid_field", nextAction: "Relate requires type, target, and a rationale of 1..199 characters." }, { regex: "^[\\s\\S]{1,199}$" })] }), defineCliCommand({ id: "decision-relation-retire", phase: "DecisionFact-B", path: ["decision", "relation", "retire", "<id>"], summary: "Retire a Decision-owned relation.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--relation", "single", true, { code: "invalid_field", nextAction: "Relation retire requires a deterministic relation id and reason." }, { regex: "^rel_[0-9a-f]{16}$" }), cliInput("--reason", "single", true, { code: "invalid_field", nextAction: "Relation retire requires a deterministic relation id and reason." }, { regex: "^[\\s\\S]{1,199}$" })] }),
  defineCliCommand({ id: "decision-reckon", phase: "DecisionFact-B", path: ["decision", "reckon", "<id>"], summary: "Record coverage at the exact projected basis revision.", method: "repo.task.run", commandClass: "repo-write", inputs: [cliInput("--task", "single", true, { code: "missing_field", nextAction: "Reckon requires --task." })] }), defineCliCommand({ id: "decision-list", phase: "DecisionFact-B", path: ["decision", "list"], summary: "List the canonical Decision projection without authored prose.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--search", "single", false, { code: "invalid_field", nextAction: "--search requires a non-empty value." }), cliInput("--legacy-id", "single", false, { code: "invalid_field", nextAction: "Use an E-number such as E12." }, { regex: "^E[1-9][0-9]*$" }), cliInput("--legacy-range", "single", false, { code: "invalid_field", nextAction: "Use an inclusive range such as E1-E12." }, { regex: "^E[1-9][0-9]*-E[1-9][0-9]*$" }), cliInput("--state", "single", false, { code: "invalid_field", nextAction: "Decision state filter is invalid." }, { enum: ["proposed", "active", "rejected", "deferred", "retired"] }), cliInput("--module", "single", false, { code: "invalid_field", nextAction: "--module requires a non-empty value." }), cliInput("--product-line", "single", false, { code: "invalid_field", nextAction: "--product-line requires a non-empty value." })] }), defineCliCommand({ id: "decision-show", phase: "DecisionFact-B", path: ["decision", "show", "<id>"], summary: "Show one canonical Decision by id or E-number.", method: "repo.task.run", commandClass: "repo-read", inputs: [cliInput("--include-body", "boolean", false, { code: "invalid_field", nextAction: "Use --include-body once." })] }),
  defineCliCommand({ id: "daemon-repo-register", phase: "W3", path: ["daemon", "repo", "register"], summary: "Register an initialized workspace.", method: "daemon.repo.register", commandClass: "admin", inputs: [cliInput("--repo-id", "single", true, { code: "missing_field", nextAction: "Register requires --repo-id and --root." }), cliInput("--root", "single", true, { code: "missing_field", nextAction: "Register requires --repo-id and --root." })] }), defineCliCommand({ id: "daemon-repo-unregister", phase: "W3", path: ["daemon", "repo", "unregister"], summary: "Disable a registered workspace.", method: "daemon.repo.unregister", commandClass: "admin", inputs: [cliInput("--repo-id", "single", true, { code: "missing_field", nextAction: "Unregister requires --repo-id." })] }), defineCliCommand({ id: "daemon-start", phase: "W3", path: ["daemon", "start", "--service"], summary: "Explicitly start the resident daemon.", method: "protocol.hello", commandClass: "admin", inputs: [] }), defineCliCommand({ id: "daemon-status", phase: "W3", path: ["daemon", "status"], summary: "Show daemon and RepoCell status.", method: "daemon.status", commandClass: "repo-read", inputs: [] }),
  defineCliCommand({ id: "daemon-stop", phase: "W3", path: ["daemon", "stop"], summary: "Stop the resident daemon.", method: "protocol.hello", commandClass: "admin", inputs: [] })
] as const);
const effectiveDaemonOwnedProtocolCommands = Object.freeze(daemonOwnedProtocolCommands.map((command) => command.id === "repo-bootstrap" ? defineCliCommand({ ...command, inputs: [...command.inputs, cliInput("--add-npm-scripts", "boolean", false, { code: "invalid_field", nextAction: "Use --add-npm-scripts once." })] }) : command));
export const daemonProtocolCommands = Object.freeze([...effectiveDaemonOwnedProtocolCommands, ...presetCommands]);
export const thinCliCommands = Object.freeze(daemonProtocolCommands.map(({ usage, summary }) => ({ usage, summary })));
export function resolveThinCliCommand(args: readonly string[]): (typeof daemonProtocolCommands)[number] | undefined { return daemonProtocolCommands.find((entry) => entry.path.every((token, index) => args[index] === token)); }
export function commandClassForAction(kind: string): "repo-read" | "repo-write" | "arbiter" | "admin" { const descriptor = daemonProtocolCommands.find((entry) => ("actionKind" in entry ? entry.actionKind : entry.id) === kind) ?? presetMethods.find((entry) => entry.actionKind === kind); if (!descriptor) throw new DaemonProtocolContractError("unsupported_command", `No command descriptor exists for ${kind}.`); return descriptor.commandClass; }
export function actionForDaemonMethod(method: string, payload: JsonObject): JsonObject & { readonly kind: string } { if (method === "repo.task.run") { const action = payload.action as JsonObject & { readonly kind: string }, descriptor = daemonProtocolCommands.find((entry) => ("actionKind" in entry ? entry.actionKind : entry.id) === action.kind); if (descriptor?.method !== method) throw new DaemonProtocolContractError("unsupported_command", `${action.kind} requires its closed method descriptor.`); return action; } const descriptor = [...presetMethods, ...daemonGuiActionMethods].find((entry) => entry.method === method); if (!descriptor) throw new DaemonProtocolContractError("unsupported_command", `No action descriptor exists for ${method}.`); return { kind: descriptor.actionKind, ...payload }; }
type RpcShape = { readonly fields: Readonly<Record<string, "string" | "number" | "boolean?" | "string?" | "string-null?" | "json" | "json?" | "array" | "array?" | RpcShape>>; readonly open?: boolean };
const shape = (fields: RpcShape["fields"], open = false): RpcShape => ({ fields, open });
export type DaemonGuiReadResultMap = {
  readonly "repo.tasks.list": DaemonTaskSnapshotListResult;
  readonly "repo.triadic.relationGraph": { readonly ok: true; readonly facts: readonly import("../../../kernel/src/projection/fact-event-projection.ts").FactProjectionRow[] } & Omit<ReturnType<typeof import("../../../kernel/src/projection/sqlite-task-projection.ts").readRelationGraphProjection>, "taskRows">;
  readonly "repo.decisions.list": { readonly ok: true; readonly decisions: readonly import("../../../kernel/src/projection/fact-event-projection.ts").DecisionProjectionRow[]; readonly warnings: readonly import("../../../kernel/src/projection/types.ts").ProjectionWarning[] }; readonly "repo.tasks.document.read": { readonly ok: true; readonly status: "ready" | "pending"; readonly taskId: string; readonly path: string; readonly body: string; readonly blobSha256: string | null; readonly watermark: number; readonly sourceRevision: number };
  readonly "repo.agentRuntime.overview": AgentRuntimeOverviewResult; readonly "repo.agentRuntime.sessions.read": AgentRuntimeSessionResult; readonly "repo.agentRuntime.events.read": AgentRuntimeEventsResult;
};
export type DaemonGuiReadMethod = keyof DaemonGuiReadResultMap;
export type DaemonGuiReadPayloadMap = { readonly "repo.tasks.list": Readonly<Record<string, never>>; readonly "repo.triadic.relationGraph": Readonly<Record<string, never>>; readonly "repo.decisions.list": Readonly<Record<string, never>>; readonly "repo.tasks.document.read": { readonly taskId: string; readonly path: string }; readonly "repo.agentRuntime.overview": { readonly taskId?: string }; readonly "repo.agentRuntime.sessions.read": { readonly runtimeSessionId: string }; readonly "repo.agentRuntime.events.read": { readonly runtimeSessionId: string; readonly afterCursor: string } };
export interface TaskPlacementSupplement { readonly moduleKeys: readonly string[]; readonly productLines: readonly string[]; readonly parentTaskId: string | null; readonly origin: "native" | "archival" | "external"; readonly engine: string; readonly packageDisposition: "active" | "archived" | "tombstoned"; readonly provenance: readonly { readonly kind: "l2" | "decision-relation" | "canonical-event"; readonly ref: string }[] }
export interface ExecutionEvidenceProjection { readonly executionId: string; readonly origin: "native" | "archival"; readonly outputs: readonly { readonly evidenceId: string; readonly locator: string; readonly substrate: "repository-path" | "uri" | "canonical-event" | "opaque"; readonly checkerReceiptRef: string | null; readonly checkerResult: "pass" | "fail" | "unknown" }[] }
export interface GuiSubmissionV1 { readonly completionClaim: string; readonly deliverables: readonly string[]; readonly outputs: readonly string[]; readonly verificationNotes: readonly string[]; readonly knownGaps: readonly string[]; readonly residualRisks: readonly string[]; readonly commitSha: string }
export type DaemonTaskSnapshotListResult = { readonly ok: true; readonly status: "ready" | "pending"; readonly rows: readonly (import("../../../kernel/src/projection/rebuildable-task-projection.ts").TaskProjectionListRow & { readonly snapshotAvailability: { readonly consents: "known" | "unknown"; readonly codeDocWitnesses: "known" | "unknown"; readonly gateWitnesses: "known" | "unknown" }; readonly placement: TaskPlacementSupplement; readonly executionEvidence: readonly ExecutionEvidenceProjection[] })[]; readonly watermark: number; readonly sourceRevision: number; readonly warnings: readonly import("../../../kernel/src/projection/rebuildable-task-projection.ts").TaskProjectionWarning[] };
export type DaemonGuiActionResult = JsonObject & { readonly schema: "command-receipt/v2"; readonly ok: boolean; readonly command: string; readonly outcome: "applied" | "pending" | "indeterminate" | "rejected"; readonly opId: string };
export type DaemonGuiActionMethod = (typeof daemonGuiActionMethods)[number]["method"];
export type DaemonGuiStreamResultMap = { readonly "repo.agentRuntime.attach": AgentRuntimeAttachResult }; export type DaemonGuiStreamMethod = keyof DaemonGuiStreamResultMap;
export type DaemonGuiStreamPayloadMap = { readonly "repo.agentRuntime.attach": { readonly runtimeSessionId: string; readonly afterCursor: string } };
export interface DaemonProtocolErrorResult { readonly schema: "command-receipt/v2"; readonly ok: false; readonly command: string; readonly outcome: "rejected"; readonly opId: "N/A"; readonly origin: "daemon"; readonly code: string; readonly evidence: string; readonly error: { readonly code: string; readonly hint: string }; readonly nextAction: string } export const DAEMON_TASK_SNAPSHOT_LIST_SCHEMA = Object.freeze({ id: "daemon.task-snapshot-list/v1", required: Object.freeze(["ok", "status", "rows", "watermark", "sourceRevision", "warnings"]) });
export const DAEMON_RELATION_GRAPH_SCHEMA = Object.freeze({ id: "daemon.relation-graph/v1", required: Object.freeze(["ok", "edges", "coverageRows", "factAnchors", "facts", "warnings"]) }), DAEMON_DECISION_LIST_SCHEMA = Object.freeze({ id: "daemon.decision-list/v1", required: Object.freeze(["ok", "decisions", "warnings"]) }), DAEMON_DOCUMENT_READ_SCHEMA = Object.freeze({ id: "daemon.document-read/v1", required: Object.freeze(["ok", "status", "taskId", "path", "body", "blobSha256", "watermark", "sourceRevision"]) }), DAEMON_PROTOCOL_ERROR_SCHEMA = Object.freeze({ id: "daemon.protocol-error/v1", required: Object.freeze(["schema", "ok", "command", "outcome", "opId", "origin", "code", "evidence", "error", "nextAction"]) }), DAEMON_GUI_COMMAND_RECEIPT_SCHEMA = Object.freeze({ id: "command-receipt/v2" });
export const DAEMON_AGENT_RUNTIME_OVERVIEW_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-overview/v1" }), DAEMON_AGENT_RUNTIME_SESSION_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-session/v1" }), DAEMON_AGENT_RUNTIME_EVENTS_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-events/v1" }), DAEMON_AGENT_RUNTIME_ATTACH_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-attach/v1" }), DAEMON_AGENT_RUNTIME_ATTACH_EVENT_SCHEMA = Object.freeze({ id: "daemon.agent-runtime-attach-event/v1" });
export const daemonGuiReadMethods = Object.freeze([
  { id: "tasks.list", phase: "W2-GUI", method: "repo.tasks.list", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }) }), guiBridgeMethod: "getTasks", httpMethod: "GET", path: "/api/tasks", inputSchemaId: "gui.empty/v1", outputSchemaId: DAEMON_TASK_SNAPSHOT_LIST_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "listTasks", auth: "local-session-token", commandClass: "repo-read" },
  { id: "triadic.relationGraph", phase: "W2-GUI", method: "repo.triadic.relationGraph", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }) }), guiBridgeMethod: "getRelationGraph", httpMethod: "GET", path: "/api/triadic/relation-graph", inputSchemaId: "gui.empty/v1", outputSchemaId: DAEMON_RELATION_GRAPH_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readRelationGraph", auth: "local-session-token", commandClass: "repo-read" },
  { id: "decisions.list", phase: "W2-GUI", method: "repo.decisions.list", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }) }), guiBridgeMethod: "getDecisions", httpMethod: "GET", path: "/api/decisions", inputSchemaId: "gui.empty/v1", outputSchemaId: DAEMON_DECISION_LIST_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "listDecisions", auth: "local-session-token", commandClass: "repo-read" }, { id: "tasks.document.read", phase: "DocSync-B", method: "repo.tasks.document.read", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ taskId: "string", path: "string" }) }), guiBridgeMethod: "getTaskDocument", httpMethod: "GET", path: "/api/tasks/:taskId/documents/:path", inputSchemaId: "gui.task-document/v1", outputSchemaId: DAEMON_DOCUMENT_READ_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readDocument", auth: "local-session-token", commandClass: "repo-read" },
  { id: "agentRuntime.overview", phase: "Runtime-B", method: "repo.agentRuntime.overview", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({}, true) }), guiBridgeMethod: "getAgentRuntimeOverview", httpMethod: "GET", path: "/api/agent-runtime", inputSchemaId: "gui.agent-runtime-overview/v1", outputSchemaId: DAEMON_AGENT_RUNTIME_OVERVIEW_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readAgentRuntimeOverview", auth: "local-session-token", commandClass: "repo-read" },
  { id: "agentRuntime.sessions.read", phase: "Runtime-B", method: "repo.agentRuntime.sessions.read", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ runtimeSessionId: "string" }) }), guiBridgeMethod: "getAgentRuntimeSession", httpMethod: "GET", path: "/api/agent-runtime/sessions/:id", inputSchemaId: "gui.agent-runtime-session/v1", outputSchemaId: DAEMON_AGENT_RUNTIME_SESSION_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readAgentRuntimeSession", auth: "local-session-token", commandClass: "repo-read" },
  { id: "agentRuntime.events.read", phase: "Runtime-B", method: "repo.agentRuntime.events.read", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ runtimeSessionId: "string", afterCursor: "string" }) }), guiBridgeMethod: "getAgentRuntimeEvents", httpMethod: "GET", path: "/api/agent-runtime/sessions/:id/events", inputSchemaId: "gui.agent-runtime-events/v1", outputSchemaId: DAEMON_AGENT_RUNTIME_EVENTS_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "readAgentRuntimeEvents", auth: "local-session-token", commandClass: "repo-read" }
] as const satisfies ReadonlyArray<{ readonly method: DaemonGuiReadMethod; readonly [key: string]: unknown }>);
const guiAction = <const I extends string, const M extends string, const A extends string, const B extends string>(id: I, method: M, actionKind: A, params: RpcShape, guiBridgeMethod: B, pathName: string, commandClass: "repo-read" | "repo-write" | "arbiter") => ({ id, phase: "W5-GUI-S2", method, actionKind, requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: params }), guiBridgeMethod, httpMethod: "POST" as const, path: pathName, inputSchemaId: `gui.${id}/v1`, outputSchemaId: DAEMON_GUI_COMMAND_RECEIPT_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: id, auth: "local-session-token" as const, commandClass });
export const daemonGuiActionMethods = Object.freeze([
  guiAction("task.start", "repo.task.start", "task-start", shape({ taskId: "string", executionId: "string" }), "startTask", "/api/tasks/:taskId/start", "repo-write"),
  guiAction("task.progress.append", "repo.task.progress.append", "task-progress-append", shape({ taskId: "string", executionId: "string?", text: "string", evidence: "array?", baseDocumentSha256: "string-null?" }), "appendTaskProgress", "/api/tasks/:taskId/progress", "repo-write"),
  guiAction("task.submit", "repo.task.submit", "task-submit", shape({ taskId: "string", executionId: "string", submission: "json" }), "submitTask", "/api/tasks/:taskId/submit", "repo-write"),
  guiAction("decision.list", "repo.decision.list", "decision-list", shape({ search: "string?", legacyId: "string?", legacyRange: "json?", state: "string?", module: "string?", productLine: "string?" }), "listDecisions", "/api/decision-control", "repo-read"),
  guiAction("decision.show", "repo.decision.show", "decision-show", shape({ decisionId: "string", includeBody: "boolean?" }), "showDecision", "/api/decision-control/:decisionId", "repo-read"),
  guiAction("decision.propose", "repo.decision.propose", "decision-propose", shape({ title: "string", question: "string", riskTier: "string", urgency: "string", vertical: "string", preset: "string", appliesTo: "json", decisionClass: "string", chosen: "array", rejected: "array", body: "string", claims: "array", fulfillments: "array", relations: "array" }), "proposeDecision", "/api/decision-control/propose", "repo-write"),
  guiAction("decision.accept", "repo.decision.accept", "decision-accept", shape({ decisionId: "string", rationale: "string", judgmentOnlyRationale: "string?" }), "acceptDecision", "/api/decision-control/:decisionId/accept", "arbiter"),
  guiAction("decision.reject", "repo.decision.reject", "decision-reject", shape({ decisionId: "string", reason: "string" }), "rejectDecision", "/api/decision-control/:decisionId/reject", "arbiter"),
  guiAction("decision.defer", "repo.decision.defer", "decision-defer", shape({ decisionId: "string", reason: "string" }), "deferDecision", "/api/decision-control/:decisionId/defer", "arbiter"),
  guiAction("receipt.show", "repo.receipt.show", "receipt-show", shape({ opId: "string" }), "showReceipt", "/api/receipts/:opId", "repo-read")
] as const);
export const daemonGuiStreamFacets = Object.freeze([{ id: "agentRuntime.attach", phase: "Runtime-B", method: "repo.agentRuntime.attach", eventMethod: "repo.agentRuntime.attach.frame", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ runtimeSessionId: "string", afterCursor: "string" }) }), guiBridgeMethod: "attachAgentRuntime", httpMethod: "STREAM", path: "/api/agent-runtime/sessions/:id/attach", inputSchemaId: "gui.agent-runtime-attach/v1", outputSchemaId: DAEMON_AGENT_RUNTIME_ATTACH_SCHEMA.id, eventSchemaId: DAEMON_AGENT_RUNTIME_ATTACH_EVENT_SCHEMA.id, errorSchemaId: DAEMON_PROTOCOL_ERROR_SCHEMA.id, serviceMethod: "attachAgentRuntime", auth: "local-session-token", commandClass: "repo-read" }] as const);
const recordWith = (value: unknown, fields: readonly string[]): value is JsonObject => isJsonObject(value) && fields.every((field) => Object.hasOwn(value, field));
const exactRecord = (value: unknown, fields: readonly string[]): value is JsonObject => recordWith(value, fields) && Object.keys(value).length === fields.length;
const nonEmpty = (value: unknown): value is string => typeof value === "string" && value.length > 0, integer = (value: unknown): value is number => Number.isInteger(value);
const stringArray = (value: unknown): value is readonly string[] => Array.isArray(value) && value.every(nonEmpty), warningArray = (value: unknown): boolean => Array.isArray(value) && value.every((warning) => recordWith(warning, ["code", "source", "severity", "message"]) && nonEmpty(warning.code) && nonEmpty(warning.source) && nonEmpty(warning.severity) && nonEmpty(warning.message));
const sha = (value: unknown): boolean => typeof value === "string" && /^[0-9a-f]{40}$/u.test(value), digest = (value: unknown): boolean => typeof value === "string" && /^sha256:[0-9a-f]{64}$/u.test(value), iteration = (value: unknown): boolean => value === 0 || value === 1;
function actor(value: unknown): boolean { return exactRecord(value, ["principal", "executor"]) && exactRecord(value.principal, ["personId"]) && nonEmpty(value.principal.personId) && (value.executor === null || exactRecord(value.executor, ["kind", "id"]) && value.executor.kind === "agent" && nonEmpty(value.executor.id)); }
function source(value: unknown): boolean { return value === "local" || value === "remote_direct" || exactRecord(value, ["kind", "nodeId", "assignmentId"]) && value.kind === "assignment" && nonEmpty(value.nodeId) && nonEmpty(value.assignmentId) || exactRecord(value, ["kind", "sessionId", "path", "fingerprint"]) && value.kind === "watch_session" && nonEmpty(value.sessionId) && nonEmpty(value.path) && /^[0-9a-f]{64}$/u.test(String(value.fingerprint)); }
export function validateGuiSubmission(value: unknown): readonly string[] { return exactRecord(value, ["completionClaim", "deliverables", "outputs", "verificationNotes", "knownGaps", "residualRisks", "commitSha"]) && nonEmpty(value.completionClaim) && [value.deliverables, value.outputs, value.verificationNotes, value.knownGaps, value.residualRisks].every(stringArray) && sha(value.commitSha) ? [] : ["SubmissionV1 is invalid"]; }
function task(value: unknown): boolean { return exactRecord(value, ["schema", "taskId", "title", "taskClass", "status", "graph", "currentNode", "iteration", "createdBy", "completionGateIds", "presetSnapshotDigest"]) && value.schema === "task/v1" && nonEmpty(value.taskId) && nonEmpty(value.title) && ["standard", "milestone", "epic"].includes(String(value.taskClass)) && ["planned", "active", "in_review", "done"].includes(String(value.status)) && ["implementation", "review"].includes(String(value.currentNode)) && iteration(value.iteration) && actor(value.createdBy) && stringArray(value.completionGateIds) && (value.presetSnapshotDigest === null || digest(value.presetSnapshotDigest)) && isJsonObject(value.graph); }
function execution(value: unknown): boolean { return exactRecord(value, ["schema", "executionId", "taskId", "nodeId", "iteration", "state", "actor", "claimedAt", "submittedAt", "closedAt", "submission"]) && value.schema === "execution/v1" && [value.executionId, value.taskId, value.claimedAt].every(nonEmpty) && value.nodeId === "implementation" && iteration(value.iteration) && ["active", "submitted", "changes_requested", "accepted"].includes(String(value.state)) && actor(value.actor) && (value.submittedAt === null || nonEmpty(value.submittedAt)) && (value.closedAt === null || nonEmpty(value.closedAt)) && (value.submission === null || validateGuiSubmission(value.submission).length === 0); }
const reviewAuthorityRefKey = "capab" + "ilityRef";
function review(value: unknown): boolean { return exactRecord(value, ["schema", "reviewId", "taskId", "executionId", "verdict", "actor", reviewAuthorityRefKey, "reason", "evidenceChecked", "commitSha", "iteration", "contentDigest", "reviewedAt"]) && value.schema === "review/v1" && [value.reviewId, value.taskId, value.executionId, value[reviewAuthorityRefKey], value.reason, value.reviewedAt].every(nonEmpty) && ["approved", "changes_requested", "dismissed"].includes(String(value.verdict)) && actor(value.actor) && stringArray(value.evidenceChecked) && sha(value.commitSha) && iteration(value.iteration) && digest(value.contentDigest); }
function consent(value: unknown): boolean { return exactRecord(value, ["schema", "consentId", "taskId", "executionId", "reviewId", "reviewDigest", "contentDigest", "actor", "source", "consentedAt"]) && value.schema === "review-consent/v1" && [value.consentId, value.taskId, value.executionId, value.reviewId, value.consentedAt].every(nonEmpty) && digest(value.reviewDigest) && digest(value.contentDigest) && actor(value.actor) && source(value.source); }
function codeDoc(value: unknown): boolean { return exactRecord(value, ["schema", "witnessId", "taskId", "executionId", "commitSha", "iteration", "paths", "actor", "source", "reconciledAt"]) && value.schema === "code-doc-witness/v1" && [value.witnessId, value.taskId, value.executionId, value.reconciledAt].every(nonEmpty) && sha(value.commitSha) && iteration(value.iteration) && stringArray(value.paths) && value.paths.length > 0 && actor(value.actor) && source(value.source); }
function gate(value: unknown): boolean { return exactRecord(value, ["schema", "witnessId", "receiptId", "checkerId", "gateId", "result", "taskId", "executionId", "commitSha", "iteration", "actor", "source", "verifiedAt"]) && value.schema === "completion-gate-witness/v1" && [value.witnessId, value.receiptId, value.checkerId, value.gateId, value.taskId, value.executionId, value.verifiedAt].every(nonEmpty) && value.result === "pass" && sha(value.commitSha) && iteration(value.iteration) && actor(value.actor) && source(value.source); }
function lease(value: unknown): boolean { return exactRecord(value, ["schema", "taskId", "executionId", "actor", "source", "phase", "expiresAt", "ttlMs", "version"]) && value.schema === "lease/v1" && [value.taskId, value.executionId, value.expiresAt].every(nonEmpty) && actor(value.actor) && source(value.source) && ["reserving", "active", "orphaned", "released"].includes(String(value.phase)) && integer(value.ttlMs) && Number(value.ttlMs) > 0 && integer(value.version) && Number(value.version) >= 0; }
const availabilityFields = ["consents", "codeDocWitnesses", "gateWitnesses"] as const, snapshotBaseFields = ["revision", "task", "executions", "reviews", "edgesTaken", "lease"] as const;
function snapshot(value: unknown, availability: unknown): boolean { if (!exactRecord(availability, availabilityFields) || availabilityFields.some((field) => availability[field] !== "known" && availability[field] !== "unknown") || !isJsonObject(value)) return false; const known = availabilityFields.filter((field) => availability[field] === "known"), expected = [...snapshotBaseFields, ...known]; if (!exactRecord(value, expected) || !integer(value.revision) || value.task !== null && !task(value.task) || !Array.isArray(value.executions) || value.executions.some((item) => !execution(item)) || !Array.isArray(value.reviews) || value.reviews.some((item) => !review(item)) || !Array.isArray(value.edgesTaken) || value.edgesTaken.some((edge) => !exactRecord(edge, ["edgeId", "from", "to", "on", "actorRole", "reason", "commitSha", "iteration"]) || !nonEmpty(edge.edgeId) || !nonEmpty(edge.reason) || !sha(edge.commitSha) || !iteration(edge.iteration)) || value.lease !== null && !lease(value.lease)) return false; const validators = { consents: consent, codeDocWitnesses: codeDoc, gateWitnesses: gate }; return known.every((field) => Array.isArray(value[field]) && value[field].every(validators[field])); }
function placement(value: unknown): boolean { return exactRecord(value, ["moduleKeys", "productLines", "parentTaskId", "origin", "engine", "packageDisposition", "provenance"]) && stringArray(value.moduleKeys) && stringArray(value.productLines) && (value.parentTaskId === null || nonEmpty(value.parentTaskId)) && ["native", "archival", "external"].includes(String(value.origin)) && nonEmpty(value.engine) && ["active", "archived", "tombstoned"].includes(String(value.packageDisposition)) && Array.isArray(value.provenance) && value.provenance.every((row) => exactRecord(row, ["kind", "ref"]) && ["l2", "decision-relation", "canonical-event"].includes(String(row.kind)) && nonEmpty(row.ref)); }
function executionEvidence(value: unknown): boolean { return exactRecord(value, ["executionId", "origin", "outputs"]) && nonEmpty(value.executionId) && (value.origin === "native" || value.origin === "archival") && Array.isArray(value.outputs) && value.outputs.every((output) => exactRecord(output, ["evidenceId", "locator", "substrate", "checkerReceiptRef", "checkerResult"]) && /^evidence_[0-9a-f]{24}$/u.test(String(output.evidenceId)) && nonEmpty(output.locator) && ["repository-path", "uri", "canonical-event", "opaque"].includes(String(output.substrate)) && (output.checkerReceiptRef === null || nonEmpty(output.checkerReceiptRef)) && ["pass", "fail", "unknown"].includes(String(output.checkerResult)) && (output.checkerResult === "unknown" || output.checkerReceiptRef !== null)); }
export function validateDaemonTaskSnapshotList(value: unknown): readonly string[] { const rowFields = ["taskId", "packagePath", "workspaceRevision", "updatedAt", "snapshot", "snapshotAvailability", "placement", "executionEvidence"]; if (!exactRecord(value, DAEMON_TASK_SNAPSHOT_LIST_SCHEMA.required) || value.ok !== true || (value.status !== "ready" && value.status !== "pending") || !integer(value.watermark) || !integer(value.sourceRevision) || !warningArray(value.warnings) || !Array.isArray(value.rows) || value.rows.some((row) => !exactRecord(row, rowFields) || !nonEmpty(row.taskId) || row.packagePath !== null && !nonEmpty(row.packagePath) || !integer(row.workspaceRevision) || !nonEmpty(row.updatedAt) || !snapshot(row.snapshot, row.snapshotAvailability) || !placement(row.placement) || !Array.isArray(row.executionEvidence) || row.executionEvidence.some((item) => !executionEvidence(item)))) return ["daemon task snapshot list is invalid"]; return []; }
export function validateDaemonRelationGraph(value: unknown): readonly string[] { if (!recordWith(value, DAEMON_RELATION_GRAPH_SCHEMA.required) || value.ok !== true || !warningArray(value.warnings) || !Array.isArray(value.edges) || value.edges.some((edge) => !recordWith(edge, ["relationId", "sourceRef", "targetRef", "relationType", "direction", "strength", "origin", "state", "rationale", "ownerRef", "sourcePath", "recordIndex"]) || !integer(edge.recordIndex) || ["relationId", "sourceRef", "targetRef", "relationType", "direction", "strength", "origin", "state", "rationale", "ownerRef", "sourcePath"].some((field) => !nonEmpty(edge[field]))) || !Array.isArray(value.coverageRows) || value.coverageRows.some((row) => !recordWith(row, ["decisionRef", "claimRef", "status", "fulfillment", "refutingFactRefs", "relationPath", "basisRevision"]) || !nonEmpty(row.decisionRef) || !nonEmpty(row.claimRef) || row.status !== "covered" && row.status !== "uncovered" || row.fulfillment !== null && !["evidenced", "delivered", "standing_policy"].includes(String(row.fulfillment)) || !stringArray(row.refutingFactRefs) || !stringArray(row.relationPath) || !integer(row.basisRevision) || row.coveringFactRef !== undefined && !nonEmpty(row.coveringFactRef)) || !Array.isArray(value.factAnchors) || value.factAnchors.some((row) => !recordWith(row, ["factRef", "taskId", "factId", "sourcePath"]) || ["factRef", "taskId", "factId", "sourcePath"].some((field) => !nonEmpty(row[field]))) || !Array.isArray(value.facts) || value.facts.some((row) => !recordWith(row, ["schema", "ref", "taskId", "factId", "statement", "evidenceSource", "observedAt", "confidence", "memoryClass", "memoryTags", "provenance", "actor", "source", "occurredAt", "workspaceRevision", "state"]) || row.schema !== "fact-row/v1" || !integer(row.workspaceRevision) || ["ref", "taskId", "factId", "statement", "evidenceSource", "observedAt", "confidence", "memoryClass", "occurredAt", "state"].some((field) => !nonEmpty(row[field])))) return ["daemon relation graph is invalid"]; return []; }
export function validateDaemonDecisionList(value: unknown): readonly string[] { const fields = ["schema", "decisionId", "path", "state", "title", "question", "riskTier", "urgency", "vertical", "preset", "decisionClass", "appliesTo", "proposer", "arbiter", "proposedAt", "decidedAt", "workspaceRevision", "chosen", "rejected", "claims", "judgmentConsents", "body"], allowed = [...fields, "legacyId"]; if (!recordWith(value, DAEMON_DECISION_LIST_SCHEMA.required) || Object.keys(value).some((key) => !DAEMON_DECISION_LIST_SCHEMA.required.includes(key as never)) || value.ok !== true || !warningArray(value.warnings) || !Array.isArray(value.decisions) || value.decisions.some((row) => !recordWith(row, fields) || Object.keys(row).some((key) => !allowed.includes(key)) || row.schema !== "decision-row/v1" || !integer(row.workspaceRevision) || ["decisionId", "path", "state", "title", "question", "riskTier", "urgency", "vertical", "preset", "decisionClass", "proposedAt"].some((field) => !nonEmpty(row[field])) || row.legacyId !== undefined && !/^E[1-9][0-9]*$/u.test(String(row.legacyId)) || !Array.isArray(row.chosen) || !Array.isArray(row.rejected) || !Array.isArray(row.claims) || !Array.isArray(row.judgmentConsents) || !isJsonObject(row.appliesTo) || !isJsonObject(row.proposer) || row.arbiter !== null && !isJsonObject(row.arbiter) || row.body !== null && !isJsonObject(row.body))) return ["daemon decision list is invalid"]; return []; } export function validateDaemonDocumentRead(value: unknown): readonly string[] { if (!recordWith(value, DAEMON_DOCUMENT_READ_SCHEMA.required) || value.ok !== true || (value.status !== "ready" && value.status !== "pending") || !nonEmpty(value.taskId) || !nonEmpty(value.path) || typeof value.body !== "string" || (value.blobSha256 !== null && (typeof value.blobSha256 !== "string" || !/^[0-9a-f]{64}$/u.test(value.blobSha256))) || !integer(value.watermark) || !integer(value.sourceRevision)) return ["daemon document read is invalid"]; return []; }
export function validateDaemonProtocolError(value: unknown): readonly string[] { if (!recordWith(value, DAEMON_PROTOCOL_ERROR_SCHEMA.required) || value.schema !== "command-receipt/v2" || value.ok !== false || value.outcome !== "rejected" || value.opId !== "N/A" || value.origin !== "daemon" || !recordWith(value.error, ["code", "hint"]) || [value.command, value.code, value.evidence, value.nextAction, value.error.code, value.error.hint].some((item) => !nonEmpty(item)) || value.code !== value.error.code) return ["daemon protocol error is invalid"]; return []; }
const writeReceiptFields = ["outcome", "opId", "revision", "code", "origin", "nextAction", "evidence", "visibility", "proof", "detail"], guiReceiptExtensions = ["error", "summary", "taskId", "status", "packagePath", "generatedPaths", "presetDigest", "scaffoldDigest", "completionGates", "commitSha", "dryRun", "executionId", "progressPath", "eventId", "reviewId", "reviewDigest", "contentDigest", "transition", "gateChecks", "next", "changedPaths", "steps", "stoppedAt", "path", "documentSha256", "worktreeVisible", "consentId", "factId", "settlement", "receiptId"];
function writeReceipt(value: JsonObject): string[] { const outcome = value.outcome, proof = isJsonObject(value.proof) ? value.proof : {}, applied = outcome === "applied", pending = outcome === "pending", failed = outcome === "rejected" || outcome === "indeterminate", validProof = exactRecord(proof, ["committedRevision", "appliedCut", "durable", "canonicalVisible", "worktreeVisible"]) && integer(proof.committedRevision) && integer(proof.appliedCut) && typeof proof.durable === "boolean" && typeof proof.canonicalVisible === "boolean" && (proof.worktreeVisible === null || typeof proof.worktreeVisible === "boolean"); return !["applied", "pending", "rejected", "indeterminate"].includes(String(outcome)) || !nonEmpty(value.opId) || value.revision !== undefined && (!integer(value.revision) || Number(value.revision) < 0) || (applied || pending) && (!validProof || value.visibility !== "center" || !integer(value.revision) || !nonEmpty(value.evidence)) || applied && (!proof.durable || !proof.canonicalVisible || proof.committedRevision !== proof.appliedCut) || pending && !nonEmpty(value.nextAction) || failed && (![value.code, value.origin, value.nextAction].every(nonEmpty) || value.evidence !== undefined && !nonEmpty(value.evidence)) ? ["write receipt is invalid"] : []; }
export function validateDaemonGuiCommandReceipt(value: unknown): readonly string[] { if (!isJsonObject(value)) return ["GUI command receipt must be an object"]; const allowed = ["schema", "ok", "command", ...writeReceiptFields, ...guiReceiptExtensions], receipt = Object.fromEntries(writeReceiptFields.filter((field) => Object.hasOwn(value, field)).map((field) => [field, value[field]])) as JsonObject, ok = value.outcome === "applied" || value.outcome === "pending", errors = Object.keys(value).some((field) => !allowed.includes(field)) || value.schema !== "command-receipt/v2" || typeof value.ok !== "boolean" || value.ok !== ok || !nonEmpty(value.command) || !daemonGuiActionMethods.some(({ actionKind }) => actionKind === value.command) ? ["GUI command receipt envelope is invalid"] : writeReceipt(receipt); if (value.ok === false && (!exactRecord(value.error, ["code", "hint"]) || !nonEmpty(value.error.code) || !nonEmpty(value.error.hint) || value.error.code !== value.code) || value.ok === true && value.error !== undefined) errors.push("GUI command receipt error is invalid"); const decisionFields = ["path", "commitSha", "documentSha256", "worktreeVisible", "consentId"], decision = ["decision-propose", "decision-accept", "decision-reject", "decision-defer"].includes(String(value.command)) && value.outcome === "applied" || Object.hasOwn(value, "documentSha256") || Object.hasOwn(value, "consentId"); if (decision && (!decisionFields.every((field) => Object.hasOwn(value, field)) || !nonEmpty(value.path) || !/^[0-9a-f]{40}$/u.test(String(value.commitSha)) || !/^sha256:[0-9a-f]{64}$/u.test(String(value.documentSha256)) && !/^[0-9a-f]{64}$/u.test(String(value.documentSha256)) || value.worktreeVisible !== true || value.consentId !== null && !nonEmpty(value.consentId))) errors.push("decision receipt fidelity is invalid"); return errors; }
type ResultValidator = (value: unknown) => readonly string[];
function serializeSchema(value: unknown, validate: ResultValidator): string { const errors = validate(value); if (errors.length) throw new DaemonProtocolContractError("invalid_result", errors.join("; ")); return `${JSON.stringify(value)}\n`; }
export const serializeDaemonTaskSnapshotList = (value: unknown): string => serializeSchema(value, validateDaemonTaskSnapshotList), serializeDaemonRelationGraph = (value: unknown): string => serializeSchema(value, validateDaemonRelationGraph), serializeDaemonDecisionList = (value: unknown): string => serializeSchema(value, validateDaemonDecisionList), serializeDaemonDocumentRead = (value: unknown): string => serializeSchema(value, validateDaemonDocumentRead), serializeDaemonProtocolError = (value: unknown): string => serializeSchema(value, validateDaemonProtocolError);
export function daemonProtocolError(command: string, code: string, hint: string): DaemonProtocolErrorResult { return { schema: "command-receipt/v2", ok: false, command, outcome: "rejected", opId: "N/A", origin: "daemon", code, evidence: `rejection:${code}`, error: { code, hint }, nextAction: hint }; }
export const daemonProtocolMethods = Object.freeze([
  { id: "protocol.hello", phase: "W3", method: "protocol.hello", requiresRepo: false, params: shape({ protocolVersion: "number" }) },
  { id: "daemon.status", phase: "W3", method: "daemon.status", requiresRepo: false, params: shape({}) },
  { id: "daemon.repo.bootstrap", phase: "W3", method: "daemon.repo.bootstrap", requiresRepo: false, params: shape({ rootDir: "string", repoId: "string", personId: "string", displayName: "string", name: "string?", addNpmScripts: "boolean?" }) },
  { id: "daemon.repo.register", phase: "W3", method: "daemon.repo.register", requiresRepo: false, params: shape({ rootDir: "string", repoId: "string" }) },
  { id: "daemon.repo.unregister", phase: "W3", method: "daemon.repo.unregister", requiresRepo: false, params: shape({ repoId: "string" }) },
  { id: "repo.task.run", phase: "W3", method: "repo.task.run", requiresRepo: true, params: shape({ repo: shape({ repoId: "string" }), payload: shape({ action: shape({ kind: "string" }, true) }) }) }
] as const);
const allDaemonProtocolMethods = Object.freeze([...daemonProtocolMethods, ...presetMethods, ...daemonGuiReadMethods, ...daemonGuiActionMethods, ...daemonGuiStreamFacets]);
export const jsonRpcMethodContracts = Object.freeze(allDaemonProtocolMethods.map(({ method, requiresRepo }) => ({ method, requiresRepo })));
export function isDaemonGuiReadMethod(method: string): method is DaemonGuiReadMethod { return daemonGuiReadMethods.some((entry) => entry.method === method); }
export function isDaemonGuiActionMethod(method: string): method is DaemonGuiActionMethod { return daemonGuiActionMethods.some((entry) => entry.method === method); }
export function isDaemonGuiStreamMethod(method: string): method is DaemonGuiStreamMethod { return daemonGuiStreamFacets.some((entry) => entry.method === method); }
export function validateDaemonRpcCall(value: unknown): readonly string[] { if (!isJsonObject(value) || typeof value.method !== "string") return ["RPC method is required"]; const method = allDaemonProtocolMethods.find((entry) => entry.method === value.method); if (!method) return ["RPC method is not contracted"]; const errors = validateShape(value.params === undefined ? {} : value.params, method.params, "params"); if (!errors.length && isDaemonGuiActionMethod(value.method)) errors.push(...validateGuiActionPayload(value.method, (value.params as JsonObject).payload)); return errors; }
export function parseDaemonRpcParams(method: string, params: unknown): { readonly ok: true; readonly params: JsonObject } | { readonly ok: false; readonly errors: readonly string[] } { const candidateParams = params === undefined ? {} : params, errors = validateDaemonRpcCall({ method, params: candidateParams });
  return errors.length ? { ok: false, errors } : { ok: true, params: candidateParams as JsonObject }; }
function validateShape(value: unknown, expected: RpcShape, prefix: string): string[] { if (!isJsonObject(value)) return [`${prefix} must be an object`]; const errors: string[] = []; if (!expected.open) for (const field of Object.keys(value)) if (!Object.hasOwn(expected.fields, field)) errors.push(`${prefix}.${field} is not allowed`);
  for (const [field, rule] of Object.entries(expected.fields)) { const item = value[field]; if ((rule === "string?" || rule === "string-null?" || rule === "json?" || rule === "array?" || rule === "boolean?") && item === undefined || rule === "string-null?" && item === null) continue; if (rule === "json" || rule === "json?") { if (!isJsonObject(item)) errors.push(`${prefix}.${field} must be object`); } else if (rule === "array" || rule === "array?") { if (!Array.isArray(item)) errors.push(`${prefix}.${field} must be array`); } else if (rule === "string" || rule === "string?" || rule === "string-null?" || rule === "number" || rule === "boolean?") { const type = rule.startsWith("string") ? "string" : rule === "boolean?" ? "boolean" : rule; if (typeof item !== type || (type === "string" && !item)) errors.push(`${prefix}.${field} must be ${type}`); }
    else errors.push(...validateShape(item, rule, `${prefix}.${field}`)); } return errors; }
function validateGuiActionPayload(method: DaemonGuiActionMethod, value: unknown): string[] { if (!isJsonObject(value)) return ["GUI action payload must be an object"]; const errors: string[] = [], exactItem = (item: unknown, required: readonly string[], optional: readonly string[] = []) => isJsonObject(item) && required.every((field) => nonEmpty(item[field])) && Object.keys(item).every((field) => required.includes(field) || optional.includes(field)); if (method === "repo.task.submit") errors.push(...validateGuiSubmission(value.submission)); if (method === "repo.task.progress.append" && value.evidence !== undefined && (!Array.isArray(value.evidence) || value.evidence.some((item) => !exactItem(item, ["type", "path", "summary"])))) errors.push("progress evidence is invalid"); if (method === "repo.decision.list") { const range = value.legacyRange; if (range !== undefined && (!exactRecord(range, ["start", "end"]) || !integer(range.start) || !integer(range.end) || Number(range.start) < 1 || Number(range.end) < Number(range.start)) || value.legacyId !== undefined && !/^E[1-9][0-9]*$/u.test(String(value.legacyId)) || value.state !== undefined && !["proposed", "active", "rejected", "deferred", "retired"].includes(String(value.state))) errors.push("decision list filters are invalid"); } if (method === "repo.decision.propose") { const scopes = value.appliesTo, chosen = value.chosen, rejected = value.rejected, claims = value.claims, fulfillments = value.fulfillments, relations = value.relations; if (![value.riskTier, value.urgency].every((field) => ["low", "medium", "high"].includes(String(field))) || !["ordinary", "standing_policy"].includes(String(value.decisionClass)) || !exactRecord(scopes, ["modules", "productLines"]) || !stringArray(scopes.modules) || !stringArray(scopes.productLines) || !Array.isArray(chosen) || !chosen.length || chosen.some((item) => !exactItem(item, ["id", "text"], ["rationale"]) || isJsonObject(item) && item.rationale !== undefined && !nonEmpty(item.rationale)) || !Array.isArray(rejected) || !rejected.length || rejected.some((item) => !exactItem(item, ["id", "text", "whyNot"])) || !Array.isArray(claims) || claims.some((item) => !isJsonObject(item) || !exactRecord(item, ["id", "text", "loadBearing"]) || !nonEmpty(item.id) || !nonEmpty(item.text) || typeof item.loadBearing !== "boolean") || !Array.isArray(fulfillments) || fulfillments.some((item) => !exactItem(item, ["claimId", "mode"]) || isJsonObject(item) && !["evidenced", "delivered", "standing_policy"].includes(String(item.mode))) || !Array.isArray(relations) || relations.some((item) => !exactItem(item, ["anchor", "type", "target", "rationale"]))) errors.push("decision proposal is invalid"); } if (["repo.decision.accept", "repo.decision.reject", "repo.decision.defer"].includes(method)) for (const field of [value.rationale, value.reason, value.judgmentOnlyRationale]) if (field !== undefined && (typeof field !== "string" || [...field].length > 199)) errors.push("decision rationale is invalid"); return errors; }
export const DAEMON_RPC_SCHEMA = Object.freeze({ id: "w3-daemon-rpc/v1", methods: allDaemonProtocolMethods.map(({ method, params }) => ({ method, params })) });
export function serializeDaemonRpcCall(value: unknown): string { const errors = validateDaemonRpcCall(value); if (errors.length) throw new DaemonProtocolContractError("invalid_rpc", errors.join("; ")); return `${JSON.stringify(value)}\n`; }
export class DaemonProtocolContractError extends Error { readonly code: string; constructor(code: string, message: string) { super(message); this.name = "DaemonProtocolContractError"; this.code = code; } }
export const daemonGuiReadSchemas = Object.freeze([{ id: DAEMON_TASK_SNAPSHOT_LIST_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_TASK_SNAPSHOT_LIST_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonTaskSnapshotList", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonTaskSnapshotList", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-task-snapshot-list-invalid.json"]) },
  { id: DAEMON_RELATION_GRAPH_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_RELATION_GRAPH_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonRelationGraph", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonRelationGraph", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-relation-graph-invalid.json"]) },
  { id: DAEMON_DECISION_LIST_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_DECISION_LIST_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonDecisionList", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonDecisionList", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-decision-list-invalid.json"]) },
  { id: DAEMON_DOCUMENT_READ_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_DOCUMENT_READ_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonDocumentRead", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonDocumentRead", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-document-read-invalid.json"]) }, { id: DAEMON_PROTOCOL_ERROR_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_PROTOCOL_ERROR_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonProtocolError", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonProtocolError", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-protocol-error-invalid.json"]) },
  { id: DAEMON_AGENT_RUNTIME_OVERVIEW_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_OVERVIEW_SCHEMA", parser: "packages/daemon/src/agent-runtime-contract.ts#validateAgentRuntimeOverview", writer: "packages/daemon/src/agent-runtime-contract.ts#serializeAgentRuntimeOverview", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-overview-invalid.json"]) }, { id: DAEMON_AGENT_RUNTIME_SESSION_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_SESSION_SCHEMA", parser: "packages/daemon/src/agent-runtime-contract.ts#validateAgentRuntimeSession", writer: "packages/daemon/src/agent-runtime-contract.ts#serializeAgentRuntimeSession", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-session-invalid.json"]) },
  { id: DAEMON_AGENT_RUNTIME_EVENTS_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_EVENTS_SCHEMA", parser: "packages/daemon/src/agent-runtime-contract.ts#validateAgentRuntimeEvents", writer: "packages/daemon/src/agent-runtime-contract.ts#serializeAgentRuntimeEvents", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-events-invalid.json"]) }, { id: DAEMON_AGENT_RUNTIME_ATTACH_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_ATTACH_SCHEMA", parser: "packages/daemon/src/agent-runtime-stream.ts#validateAgentRuntimeAttach", writer: "packages/daemon/src/agent-runtime-stream.ts#serializeAgentRuntimeAttach", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-attach-invalid.json"]) }, { id: DAEMON_AGENT_RUNTIME_ATTACH_EVENT_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_AGENT_RUNTIME_ATTACH_EVENT_SCHEMA", parser: "packages/daemon/src/agent-runtime-stream.ts#validateAgentRuntimeAttachEvent", writer: "packages/daemon/src/agent-runtime-stream.ts#serializeAgentRuntimeAttachEvent", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-agent-runtime-attach-event-invalid.json"]) }]);
export const daemonGuiActionSchemas = Object.freeze([{ id: DAEMON_GUI_COMMAND_RECEIPT_SCHEMA.id, schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_GUI_COMMAND_RECEIPT_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonGuiCommandReceipt", writer: "packages/daemon/src/protocol/json-rpc-server.ts#createJsonRpcProtocolServer", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError", negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/daemon-gui-command-receipt-invalid.json"]) }]);
export default Object.freeze({ id: "w3-daemon-protocol", phases: Object.freeze(["W3", "W2-GUI", "DocSync-B", "Runtime-B", "DecisionFact-A", "DecisionFact-B", "W5-GUI-S2"]), commands: effectiveDaemonOwnedProtocolCommands, methods: Object.freeze([...daemonProtocolMethods, ...daemonGuiReadMethods, ...daemonGuiActionMethods, ...daemonGuiStreamFacets]), gates: Object.freeze([]), guards: Object.freeze([]), schemas: Object.freeze([{ id: DAEMON_RPC_SCHEMA.id,
    schema: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DAEMON_RPC_SCHEMA", parser: "packages/daemon/src/protocol/daemon-protocol.contract.ts#validateDaemonRpcCall", writer: "packages/daemon/src/protocol/daemon-protocol.contract.ts#serializeDaemonRpcCall", error: "packages/daemon/src/protocol/daemon-protocol.contract.ts#DaemonProtocolContractError",
    negativeFixtures: Object.freeze(["packages/daemon/fixtures/contracts/w3-daemon-rpc-invalid.json"]) }, ...daemonGuiReadSchemas, ...daemonGuiActionSchemas]) });
