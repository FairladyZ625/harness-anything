import type { AgentDefinitionSnapshot, AgentRuntimeEventV1 } from "../../kernel/src/index.ts"; export interface AgentRuntimeInstallationDto { readonly installationId: string; readonly kindId: "claude" | "codex"; readonly protocolFamily: "claude-compatible" | "codex"; readonly version: string; readonly attachCapability: "supported" | "unsupported"; readonly lastObservedAt: string }
export interface AgentRuntimeInstanceDto { readonly schemaVersion: 1; readonly instanceId: string; readonly name: string; readonly kindId: "claude" | "codex"; readonly installationId: string; readonly providerId: string; readonly model: string; readonly reasoningEffort: string | null; readonly baseUrl: string | null; readonly authMode: "subscription" | "api-key"; readonly authState: "configured" | "unknown"; readonly authReadiness: { readonly status: "ready" | "not-ready"; readonly code: string | null; readonly hint: string | null }; readonly baseUrlConfigured: boolean; readonly isolationState: "enforced" } export interface AgentRuntimeAssociationDto { readonly taskId: string; readonly executionId: string; readonly holder: { readonly personId: string; readonly executorId: string | null } | null; readonly lease: { readonly phase: "reserving" | "held" | "released" | "orphaned"; readonly expiresAt: string } | null }
export interface AgentRuntimeSessionDto { readonly runtimeSessionId: string; readonly providerSessionId: string | null; readonly instanceId: string; readonly installationId: string; readonly kindId: "claude" | "codex"; readonly definitionSnapshotRef: string; readonly definitionSnapshot: AgentDefinitionSnapshot; readonly liveness: "live" | "stale" | "unknown" | "exited"; readonly attachCapability: "supported" | "unsupported"; readonly streamCursor: string; readonly associations: readonly AgentRuntimeAssociationDto[]; readonly activity: { readonly lastObservedAt: string; readonly outcome: "succeeded" | "failed" | "unknown" | null; readonly exitCode: number | null; readonly resultRef: string | null } }
export interface AgentRuntimeLifecycleDto { readonly cursor: string; readonly runtimeSessionId: string; readonly type: AgentRuntimeEventV1["type"]; readonly occurredAt: string }
export type AgentRuntimeOverviewResult = { readonly ok: true; readonly status: "ready" | "pending"; readonly installations: readonly AgentRuntimeInstallationDto[]; readonly instances: readonly AgentRuntimeInstanceDto[]; readonly sessions: readonly AgentRuntimeSessionDto[]; readonly watermark: number; readonly sourceRevision: number };
export type AgentRuntimeSessionResult = { readonly ok: true; readonly status: "ready" | "pending"; readonly session: AgentRuntimeSessionDto; readonly result: { readonly ref: string; readonly text: string } | null; readonly watermark: number; readonly sourceRevision: number };
export type AgentRuntimeEventsResult = { readonly ok: true; readonly runtimeSessionId: string; readonly events: readonly AgentRuntimeLifecycleDto[]; readonly cursor: string; readonly sourceCursor: string; readonly done: boolean };
export function coded(code: string, message: string): Error { const error = new Error(message) as Error & { code: string }; error.code = code; return error; }
const liveness = ["live", "stale", "unknown", "exited"], attachCapabilities = ["supported", "unsupported"];
export function validateAgentRuntimeOverview(value: unknown): readonly string[] { return isAgentRuntimeContractRecord(value) && hasExactAgentRuntimeContractFields(value, ["ok", "status", "installations", "instances", "sessions", "watermark", "sourceRevision"]) && value.ok === true && ["ready", "pending"].includes(String(value.status)) && Array.isArray(value.installations) && value.installations.every(validInstallation) && Array.isArray(value.instances) && value.instances.every(validInstance) && Array.isArray(value.sessions) && value.sessions.every(validSession) && Number.isInteger(value.watermark) && Number.isInteger(value.sourceRevision) && safeKeys(value) ? [] : ["agent runtime overview is invalid"]; }
export function validateAgentRuntimeSession(value: unknown): readonly string[] { return isAgentRuntimeContractRecord(value) && hasExactAgentRuntimeContractFields(value, ["ok", "status", "session", "result", "watermark", "sourceRevision"]) && value.ok === true && ["ready", "pending"].includes(String(value.status)) && validSession(value.session) && (value.result === null || isAgentRuntimeContractRecord(value.result) && hasExactAgentRuntimeContractFields(value.result, ["ref", "text"]) && typeof value.result.ref === "string" && typeof value.result.text === "string") && Number.isInteger(value.watermark) && Number.isInteger(value.sourceRevision) && safeKeys(value) ? [] : ["agent runtime session is invalid"]; }
export function validateAgentRuntimeEvents(value: unknown): readonly string[] { return isAgentRuntimeContractRecord(value) && hasExactAgentRuntimeContractFields(value, ["ok", "runtimeSessionId", "events", "cursor", "sourceCursor", "done"]) && value.ok === true && typeof value.runtimeSessionId === "string" && Array.isArray(value.events) && value.events.every((event) => isAgentRuntimeContractRecord(event) && hasExactAgentRuntimeContractFields(event, ["cursor", "runtimeSessionId", "type", "occurredAt"]) && /^lifecycle:\d+$/u.test(String(event.cursor)) && event.runtimeSessionId === value.runtimeSessionId && typeof event.type === "string" && typeof event.occurredAt === "string") && /^lifecycle:\d+$/u.test(String(value.cursor)) && /^lifecycle:\d+$/u.test(String(value.sourceCursor)) && typeof value.done === "boolean" && safeKeys(value) ? [] : ["agent runtime lifecycle events are invalid"]; }
function validInstallation(value: unknown): value is AgentRuntimeInstallationDto { return isAgentRuntimeContractRecord(value) && hasExactAgentRuntimeContractFields(value, ["installationId", "kindId", "protocolFamily", "version", "attachCapability", "lastObservedAt"]) && typeof value.installationId === "string" && ["claude", "codex"].includes(String(value.kindId)) && ["claude-compatible", "codex"].includes(String(value.protocolFamily)) && typeof value.version === "string" && attachCapabilities.includes(String(value.attachCapability)) && typeof value.lastObservedAt === "string"; }
function validInstance(value: unknown): value is AgentRuntimeInstanceDto { return isAgentRuntimeContractRecord(value) && hasExactAgentRuntimeContractFields(value, ["schemaVersion", "instanceId", "name", "kindId", "installationId", "providerId", "model", "reasoningEffort", "baseUrl", "authMode", "authState", "authReadiness", "baseUrlConfigured", "isolationState"]) && value.schemaVersion === 1 && [value.instanceId, value.name, value.installationId, value.providerId, value.model].every((item) => typeof item === "string" && item.length > 0) && ["claude", "codex"].includes(String(value.kindId)) && (value.reasoningEffort === null || typeof value.reasoningEffort === "string") && (value.baseUrl === null || typeof value.baseUrl === "string") && ["subscription", "api-key"].includes(String(value.authMode)) && ["configured", "unknown"].includes(String(value.authState)) && validReadiness(value.authReadiness) && typeof value.baseUrlConfigured === "boolean" && value.isolationState === "enforced"; }
function validReadiness(value: unknown): boolean { return isAgentRuntimeContractRecord(value) && hasExactAgentRuntimeContractFields(value, ["status", "code", "hint"]) && ["ready", "not-ready"].includes(String(value.status)) && (value.code === null || typeof value.code === "string") && (value.hint === null || typeof value.hint === "string") && (value.status === "ready" ? value.code === null && value.hint === null : typeof value.code === "string" && typeof value.hint === "string"); }
function validSession(value: unknown): value is AgentRuntimeSessionDto { return isAgentRuntimeContractRecord(value) && hasExactAgentRuntimeContractFields(value, ["runtimeSessionId", "providerSessionId", "instanceId", "installationId", "kindId", "definitionSnapshotRef", "definitionSnapshot", "liveness", "attachCapability", "streamCursor", "associations", "activity"]) && typeof value.runtimeSessionId === "string" && (value.providerSessionId === null || typeof value.providerSessionId === "string") && [value.instanceId, value.installationId, value.definitionSnapshotRef].every((item) => typeof item === "string" && item.length > 0) && ["claude", "codex"].includes(String(value.kindId)) && validDefinitionSnapshot(value.definitionSnapshot) && value.definitionSnapshot.instanceId === value.instanceId && value.definitionSnapshot.installationId === value.installationId && liveness.includes(String(value.liveness)) && attachCapabilities.includes(String(value.attachCapability)) && /^stream:\d+$/u.test(String(value.streamCursor)) && Array.isArray(value.associations) && value.associations.every(validAssociation) && isAgentRuntimeContractRecord(value.activity) && hasExactAgentRuntimeContractFields(value.activity, ["lastObservedAt", "outcome", "exitCode", "resultRef"]) && typeof value.activity.lastObservedAt === "string" && (value.activity.outcome === null || ["succeeded", "failed", "unknown"].includes(String(value.activity.outcome))) && (value.activity.exitCode === null || Number.isInteger(value.activity.exitCode) && (value.activity.exitCode as number) >= 0) && (value.activity.resultRef === null || typeof value.activity.resultRef === "string"); }
function validDefinitionSnapshot(value: unknown): value is AgentDefinitionSnapshot { return isAgentRuntimeContractRecord(value) && hasExactAgentRuntimeContractFields(value, ["schema", "configVersion", "instanceId", "installationId", "kindId", "providerId", "model", "reasoningEffort", "baseUrl", "authMode"]) && value.schema === "agent-definition-snapshot/v1" && value.configVersion === 1 && [value.instanceId, value.installationId, value.providerId, value.model].every((item) => typeof item === "string" && item.length > 0) && ["claude", "codex"].includes(String(value.kindId)) && (value.reasoningEffort === null || typeof value.reasoningEffort === "string") && (value.baseUrl === null || typeof value.baseUrl === "string") && ["subscription", "api-key"].includes(String(value.authMode)); }
function validAssociation(value: unknown): value is AgentRuntimeAssociationDto { return isAgentRuntimeContractRecord(value) && hasExactAgentRuntimeContractFields(value, ["taskId", "executionId", "holder", "lease"]) && typeof value.taskId === "string" && typeof value.executionId === "string" && (value.holder === null || isAgentRuntimeContractRecord(value.holder) && hasExactAgentRuntimeContractFields(value.holder, ["personId", "executorId"]) && typeof value.holder.personId === "string" && (value.holder.executorId === null || typeof value.holder.executorId === "string")) && (value.lease === null || isAgentRuntimeContractRecord(value.lease) && hasExactAgentRuntimeContractFields(value.lease, ["phase", "expiresAt"]) && ["reserving", "held", "released", "orphaned"].includes(String(value.lease.phase)) && typeof value.lease.expiresAt === "string"); }
function safeKeys(value: unknown): boolean { if (Array.isArray(value)) return value.every(safeKeys); if (!isAgentRuntimeContractRecord(value)) return true; for (const [key, nested] of Object.entries(value)) { if (["credential", "credentialValue", "secret", "token", "environment", "argv", "transcript", "stdout", "stderr"].includes(key) || !safeKeys(nested)) return false; } return true; }
export const serializeAgentRuntimeOverview = (value: unknown): string => serialize(value, validateAgentRuntimeOverview), serializeAgentRuntimeSession = (value: unknown): string => serialize(value, validateAgentRuntimeSession), serializeAgentRuntimeEvents = (value: unknown): string => serialize(value, validateAgentRuntimeEvents);
export function serialize(value: unknown, validate: (candidate: unknown) => readonly string[]): string { const errors = validate(value); if (errors.length) throw coded("invalid_result", errors.join("; ")); return `${JSON.stringify(value)}\n`; }
function isAgentRuntimeContractRecord(value: unknown): value is Record<string, unknown> { return value !== null && typeof value === "object" && !Array.isArray(value); }
function hasExactAgentRuntimeContractFields(value: Record<string, unknown>, fields: readonly string[]): boolean { return Object.keys(value).length === fields.length && fields.every((field) => Object.hasOwn(value, field)); }
