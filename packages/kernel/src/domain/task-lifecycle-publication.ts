import type { ExecutionV1 } from "./execution.ts";
import type { ReviewConsentV1, ReviewV1 } from "./review.ts";
import type { LifecycleDocumentClaim, TaskEventV1 } from "./task-lifecycle-event.ts";
import type { TaskLifecycleSnapshot } from "./task-lifecycle.contract.ts";
import { freezeDeclaredWritePlan, isFrozenWritePlan, type FrozenWritePlan, type WriteTarget } from "./write-chain.contract.ts";
import { sha256Text, stableStringify } from "../integrity/stable-hash.ts";
import { normalizeRelativeDocumentPath } from "../layout/portable-path.ts";
import { eventObjectTarget } from "../layout/ledger-object-layout.ts";
import { formatRelationFlowRecord } from "./entity-relation.ts";
export interface LifecycleDocumentState { readonly path: string; readonly body: string; readonly blobSha256: string }
export interface LifecycleContentBlob { readonly sha256: string; readonly size: number; readonly mediaType: "text/markdown" | "application/json"; readonly body: string }
export function lifecycleDocumentPaths(event: TaskEventV1, packagePath: string): readonly string[] { if (event.type === "task_created" || event.type === "lease_renewed") return []; if (!("execution" in event.payload)) return [`${packagePath}/INDEX.md`, `${packagePath}/task-contract.json`, ...(event.payload.task.metadata?.moduleKey ? [`${packagePath}/module.md`] : [])]; const paths = [`${packagePath}/INDEX.md`, `${packagePath}/executions/${event.payload.execution.executionId}.md`]; if (event.type === "review_recorded" || event.type === "review_consent_recorded") paths.push(`${packagePath}/reviews/${event.payload.review.reviewId}.md`); if (event.type === "code_doc_reconciled") paths.push(`${packagePath}/code-doc-anchors.json`); return paths; }
export function compileTaskLifecycleWrite(input: { readonly event: TaskEventV1; readonly snapshot: TaskLifecycleSnapshot; readonly packagePath: string | null; readonly currentDocuments: readonly LifecycleDocumentState[] }): { readonly event: TaskEventV1; readonly plan: FrozenWritePlan; readonly blobs: readonly LifecycleContentBlob[]; readonly changedPaths: readonly string[] } { const paths = input.packagePath ? lifecycleDocumentPaths(input.event, input.packagePath) : [], current = new Map(input.currentDocuments.map((value) => [value.path, value.body])), bodies = paths.map((path) => ({ path, body: renderLifecycleDocument(input.event, input.snapshot, path, current.get(path) ?? null) })), claims: LifecycleDocumentClaim[] = bodies.map(({ path, body }) => ({ path, sha256: sha256Text(body), size: Buffer.byteLength(body), mediaType: path.endsWith(".json") ? "application/json" : "text/markdown", policyId: "typed-machine-writer/v1" })); for (const claim of claims) if (normalizeRelativeDocumentPath(claim.path) !== claim.path) throw new Error(`invalid lifecycle path ${claim.path}`); const event = { ...input.event, payload: { ...input.event.payload, documentClaims: claims } } as TaskEventV1, blobs = claims.map((claim, index) => ({ sha256: claim.sha256, size: claim.size, mediaType: claim.mediaType, body: bodies[index]!.body })); return { event, plan: taskLifecycleWritePlan(event), blobs, changedPaths: claims.map((claim) => claim.path) }; }
export function taskLifecycleWritePlan(event: TaskEventV1): FrozenWritePlan { const targets: WriteTarget[] = [{ kind: "event_file", path: eventObjectTarget(event.opId), operation: "create" }, { kind: "event_head", path: "harness/events/head.json", operation: "replace" }, { kind: "projection_invalidation", projection: "task-lifecycle/v1", key: event.taskId }]; for (const claim of event.payload.documentClaims ?? []) targets.push({ kind: "authored_file", path: claim.path, operation: "replace", sha256: claim.sha256, size: claim.size, mediaType: claim.mediaType }, { kind: "content_blob", sha256: claim.sha256, size: claim.size, mediaType: claim.mediaType }, { kind: "projection_invalidation", projection: "document/v1", key: claim.path }); for (const change of event.payload.carriedDocumentClaims ?? []) targets.push({ kind: "authored_file", path: change.path, operation: "replace", sha256: change.candidate.sha256, size: change.candidate.size, mediaType: change.candidate.mediaType }, { kind: "content_blob", sha256: change.candidate.sha256, size: change.candidate.size, mediaType: change.candidate.mediaType }, { kind: "projection_invalidation", projection: "document/v1", key: change.path }); if (event.type === "execution_started") targets.push(lease(event.taskId, "reserve"), lease(event.taskId, "activate"), lease(event.taskId, "release")); if (event.type === "execution_submitted" || event.type === "lease_released") targets.push(lease(event.taskId, "release")); return freezeDeclaredWritePlan({ commandType: event.type, targets }, [event.type]); }
export function assertTaskLifecycleWritePlan(event: TaskEventV1, plan: FrozenWritePlan | undefined): void { const shape = (value: FrozenWritePlan) => stableStringify({ commandType: value.commandType, targets: value.targets.map(stableStringify).sort() }); if (!plan || !isFrozenWritePlan(plan) || shape(plan) !== shape(taskLifecycleWritePlan(event))) throw new Error("lifecycle write plan must exactly declare event, authored documents, blobs, lease, and projections"); }
export function renderLifecycleDocument(event: TaskEventV1, snapshot: TaskLifecycleSnapshot, path: string, base: string | null): string { if (path.endsWith("/INDEX.md")) return renderIndex(event, snapshot, path, base); if (path.endsWith("/task-contract.json")) return renderContract(snapshot, base); if (path.endsWith("/module.md")) return renderModule(snapshot); if (path.endsWith("/code-doc-anchors.json") && event.type === "code_doc_reconciled") return `${stableStringify(event.payload.witness)}\n`; if (path.includes("/reviews/")) { const review = event.type === "review_recorded" || event.type === "review_consent_recorded" ? event.payload.review : snapshot.reviews.find((value) => path.endsWith(`/${value.reviewId}.md`)); return renderReview(review as ReviewV1, snapshot.consents.find((value) => value.reviewId === review?.reviewId) ?? null); } const execution = "execution" in event.payload ? event.payload.execution : snapshot.executions.find((value) => path.endsWith(`/${value.executionId}.md`)); return renderExecution(execution as ExecutionV1, snapshot); }
function renderIndex(event: TaskEventV1, snapshot: TaskLifecycleSnapshot, path: string, base: string | null): string { const task = snapshot.task!, executionId = "execution" in event.payload ? event.payload.execution.executionId : "", gateStatus = (gateId: string) => gateId === "code-doc-reconciliation" ? snapshot.codeDocWitnesses.some((value) => value.executionId === executionId) : snapshot.gateWitnesses.some((value) => value.executionId === executionId && value.gateId === gateId), missingGate = task.completionGateIds.find((gateId) => !gateStatus(gateId)), next = task.status === "active" ? `Run \`ha task submit ${task.taskId} --execution-id <id> --from-file <submission.json>\`.` : task.status === "in_review" && !snapshot.reviews.some((value) => value.executionId === executionId) ? `Run \`ha task review-execution ${task.taskId} --execution-id <id> --review-id <id> --from-file <review.json>\`.` : task.status === "in_review" && !snapshot.consents.some((value) => value.executionId === executionId) ? `Run \`ha task review-consent ${task.taskId} --execution-id <id> --review-id <id> --consent-id <id>\`.` : missingGate === "ci" ? `Run \`ha task complete ${task.taskId} --execution-id <id> --ci passed\`.` : missingGate === "code-doc-reconciliation" ? `Run \`ha task code-doc reconcile ${task.taskId} --execution-id <id> --commit-sha <sha> --iteration <n> --path <path>\`.` : task.status === "done" ? "Task complete." : task.status === "cancelled" ? "Task cancelled; create follow-up work with `ha task supersede`." : `Run \`ha task complete ${task.taskId} --execution-id <id>\`.`, gates = task.completionGateIds.length ? task.completionGateIds.map((gateId) => `- ${gateId}: ${gateStatus(gateId) ? "pass" : "blocked"}`).join("\n") : "- none", metadata = task.metadata; let initial = base ?? `---\ntaskId: ${task.taskId}\nstatus: ${task.status}\nowner: machine\n---\n# ${task.title}\n\n## Next\n\n${next}\n`; if (metadata) { const body = (base?.replace(/^---\n[\s\S]*?\n---\n/u, "") ?? `# ${task.title}\n\n## Next\n\n${next}\n`).replace(/^# .*$/mu, `# ${task.title}`), packagePath = path.slice(0, -"/INDEX.md".length); initial = `---\nschema: task-package/v2\ntask_id: ${task.taskId}\ntitle: ${JSON.stringify(task.title)}\n${metadata.parentTaskId ? `parent: ${metadata.parentTaskId}\n` : ""}lifecycle:\n  engine: kernel/task-lifecycle/v1\n  status: ${task.status}\npackageDisposition: ${task.packageDisposition ?? "active"}\n${task.supersededBy ? `supersededBy: ${task.supersededBy}\n` : ""}${metadata.workKind ? `workKind: ${metadata.workKind}\n` : ""}${metadata.riskTier ? `riskTier: ${metadata.riskTier}\n` : ""}${metadata.urgency ? `urgency: ${metadata.urgency}\n` : ""}vertical: ${metadata.verticalId}\npreset: ${metadata.presetId}\nprofile: ${metadata.profileId}\npackagePath: ${packagePath}\nowner: machine\nrelations:\n${(task.relations ?? []).map(formatRelationFlowRecord).join("\n")}\n---\n${body}`; } return initial.replace(/^status:.*$/mu, `status: ${task.status}`).replace(/^  status:.*$/mu, `  status: ${task.status}`).replace(/## Next\n[\s\S]*$/u, `## Next\n\n${next}\n\n## Gate Checks\n\n${gates}\n`); }
function renderContract(snapshot: TaskLifecycleSnapshot, base: string | null): string { const task = snapshot.task!, current = base ? JSON.parse(base) as Record<string, unknown> : {}, metadata = JSON.parse(stableStringify(task.metadata ?? null)) as unknown, relations = JSON.parse(stableStringify(task.relations ?? [])) as unknown; return `${JSON.stringify({ ...current, schema: "task-contract/v1", contractVersion: task.contractVersion ?? 1, taskId: task.taskId, title: task.title, taskClass: task.taskClass, pinned: task.pinned ?? false, metadata, relations }, null, 2)}\n`; }
function renderModule(snapshot: TaskLifecycleSnapshot): string { const key = snapshot.task?.metadata?.moduleKey ?? "unassigned"; return `# Module\n\nModule key: ${key}\nModule title: ${key}\n`; }
function renderExecution(value: ExecutionV1, snapshot: TaskLifecycleSnapshot): string { const packet = value.submission, review = snapshot.reviews.find((candidate) => candidate.executionId === value.executionId), consent = snapshot.consents.find((candidate) => candidate.executionId === value.executionId), witness = snapshot.codeDocWitnesses.find((candidate) => candidate.executionId === value.executionId), gates = snapshot.gateWitnesses.filter((candidate) => candidate.executionId === value.executionId); return `# Execution ${value.executionId}\n\nManaged by \`ha task start/submit\`; hand edits are rejected.\n\n- Task: ${value.taskId}\n- Iteration: ${value.iteration}\n- State: ${value.state}\n- Claimed: ${value.claimedAt}\n- Submitted: ${value.submittedAt ?? "pending"}\n- Closed: ${value.closedAt ?? "open"}\n- Commit: ${packet?.commitSha ?? "pending"}\n- Completion claim: ${packet?.completionClaim ?? "pending"}\n- Review: ${review ? `${review.reviewId}/${review.verdict}` : "pending"}\n- Consent: ${consent?.consentId ?? "pending"}\n- Checker witnesses: ${gates.length ? gates.map((value) => `${value.gateId}/${value.receiptId}`).join(", ") : "pending"}\n- Code-doc witness: ${witness?.witnessId ?? "pending"}\n\n## Deliverables\n\n${list(packet?.deliverables ?? [])}\n\n## Outputs\n\n${list(packet?.outputs ?? [])}\n\n## Verification\n\n${list(packet?.verificationNotes ?? [])}\n\n## Known gaps\n\n${list(packet?.knownGaps ?? [])}\n\n## Residual risks\n\n${list(packet?.residualRisks ?? [])}\n`; }
function renderReview(value: ReviewV1, consent: ReviewConsentV1 | null): string { return `# Review ${value.reviewId}\n\nManaged by \`ha task review-execution\`; legacy \`review.md\` is not authoritative.\n\n- Task: ${value.taskId}\n- Execution: ${value.executionId}\n- Verdict: ${value.verdict}\n- Commit: ${value.commitSha}\n- Iteration: ${value.iteration}\n- Content digest: ${value.contentDigest}\n- Reviewed at: ${value.reviewedAt}\n- Consent: ${consent ? consent.consentId : "pending"}\n- Consent actor: ${consent?.actor.principal.personId ?? "pending"}\n- Consent source: ${consent ? stableStringify(consent.source) : "pending"}\n\n## Reason\n\n${value.reason}\n\n## Evidence checked\n\n${list(value.evidenceChecked)}\n`; }
function list(values: readonly string[]): string { return values.length ? values.map((value) => `- ${value}`).join("\n") : "- none"; }
function lease(taskId: string, operation: "reserve" | "activate" | "release"): WriteTarget { return { kind: "lease_sqlite", table: "lease_cas", taskId, operation }; }
