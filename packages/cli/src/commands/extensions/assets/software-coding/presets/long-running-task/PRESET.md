---
schema: preset-document/v1
description: Add an explicit coordination contract for work that spans multiple sessions, workers, or implementation waves.
whenToUse: Use when the task cannot be safely completed in one bounded execution and needs durable checkpoints and handoffs.
---

# Long Running Task

Adds a long-running task contract alongside the standard task documents while retaining the normal completion gates.
