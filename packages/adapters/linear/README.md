# @harness-anything/adapter-linear

Linear snapshot/comment adapter package.

## Implementation Status

Status: `placeholder`.

This package slot reserves the public package name and dependency boundary for
the M4 external-adapter roadmap. It does not yet implement Linear import,
snapshot refresh, or publish-note behavior.

Do not treat this package as a shipped adapter until an M4 task packet adds the
minimum read-only implementation and fixture-backed tests.
